/**
 * Schema Version — bump this number on every schema-breaking deploy.
 * The AdminLayout checks this against localStorage on boot.
 * If stale: shows a "reload to update" banner.
 */
export const SCHEMA_VERSION = 1;
