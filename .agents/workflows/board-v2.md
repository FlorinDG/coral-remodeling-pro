---
description: CoralOS LIVE BOARD v2 — single source of truth for execution. Supersedes pro-hardening.md as the active tracker. Read AFTER pd.md.
---

# CoralOS — Live Board v2
> Reset 2026-06-04 from Florin's full "state of the union" walkthrough of the deployed app.
> **This file is now the active tracker.** `pro-hardening.md` is archived history (many items there were marked done but are shallow/broken in production — do NOT trust its statuses; THIS board is ground truth).
> North star: **FINANCIAL CORRECTNESS first.** Sequencing: **FOUNDATION-FIRST** (fix shared primitives that cause many symptoms before chasing individual symptoms).

---

## 🔒 EXECUTION PROTOCOL (binding — the trust reset)
1. **ONE workstream item at a time.** Complete it, STOP. No multi-item commits.
2. **Two-stage status, Florin gates.** Coder may reach `🟢 DONE — awaiting verify` (the furthest the coder goes). ONLY Florin sets `✅ VERIFIED`. **No new item starts until the current one is `✅ VERIFIED`.** Failed verify → `🟡 IN PROGRESS` → fix → resubmit.
3. **Commit naming = checksum.** One item = one commit: `<ITEM-ID>: <short imperative>` (e.g. `FIN-1: credit-note links by parentInvoiceId only`). Same ID on follow-ups. No creative/bundled names. So `git log --oneline` is glanceable against this board.
4. **Measure, don't claim.** "Done" = you tested the real behavior on the deploy and stated HOW in feedback. "Implemented" ≠ done.
5. **Regression rule.** An increment must not break anything previously `✅ VERIFIED`. If it does → it's not done.
6. **Branch:** all work on `develop`; promote to `main` + tag only at version checkpoints (below).
7. **SEPARATION OF POWERS (binding — prevents lost-update clobbers).** `board-v2.md` is the **Planner's file ONLY**. The **coder must NEVER edit `board-v2.md`** (no "docs: update board" commits) — its status channel is the deterministic `TASK-ID:` commit name (rule 3), which IS how it reports. The **Planner** owns the board and reconciles the commit log into it. **Florin** screenshots the board for the human view and cross-checks against `git log --oneline`. Two writers on one file = silent lost updates (happened 2026-06-07: coder commits reverted Planner edits). One writer per artifact.

---

## 🟢 HAPPY PATH (the spine — every increment must move a real user one verified step further)
FREE tier → first paid:
**signup → company data correct & stable → create invoice (engine usable) → send invoice (email works) → log expense (scan stores file) → receive Peppol (SMP live) → accountant export.**
Financial correctness underpins the middle of this path → that's why FIN workstream goes first.

### 🚀 FREE-TIER LAUNCH READINESS (Planner audit 2026-06-18 — features mostly EXIST; blockers are RELIABILITY, not missing features)
Per-step real status (code-inspected). **Launch blockers = the ⛔ rows.**
1. **Signup → company data stable** — ✅ exists; PROFILE-2 committed (numbering/branding persistence). PROFILE-3-stationery = polish, not a blocker.
2. **Create invoice (engine usable)** — ✅ engine exists (`ClientInvoiceEngine`, AGG done). ⛔ **GRID-12** (typing lands in the wrong record) corrupts data entry across clients/suppliers/invoices → **must fix before launch**. ⛔ **FIN-7b-APPLY** (per-tenant numbering db push) → financial correctness.
3. **Send invoice (email works)** — ✅ works. SEND-3 (bcc/replyTo) **Planner-verified by inspection** (both `send-quote.ts` + `send-invoice.ts` L78-79). SEND-5b (public link) committed+verified. FIN-9 (legal block above totals, `wrap={false}`) **Planner-verified by inspection** (`InvoicePDFTemplate` L374/615, `QuotationPDFTemplate` L383/577; legal renders before totals) → done, NOT a blocker. SEND-6 (compose modal) = nice-to-have, NOT a blocker.
4. **Log expense (scan stores file)** — ✅ SCAN-* committed (PDF accept, unpdf, manual capture, Blob store). DRIVE-OUT-1 (stop empty-folder spam) = hygiene.
5. **Receive Peppol (SMP live)** — PEPPOL_RECEIVE is FREE-tier (confirmed `feature-flags.ts`). ⛔ **PEPPOL-4-FIX-C committed but will FAIL verify until PI-3** — the UBL parser `parseFloat`s attributed amount elements → NaN, so totals/prices are still wrong. **PI-3 is part of making receive actually work.** Then Florin's live `?debug=1` check. Plus PI-1/2/4-9 for a usable inbound invoice.
6. **Accountant export** — ✅ a `journal` module + an accountant CSV export (with `accountantExportedAt` stamping) EXIST. ⚠️ **ASSESS-WITH-FLORIN:** is the CSV a *usable* accountant handover (structured sales+purchase journals w/ VAT base per rate), or just a raw property dump? Likely needs a proper "Boekhouder export" (sales journal + purchase journal + PDF zip for a date range). NOT building blind — needs Florin's format call. **No code until decided.**
**⇒ Free-tier launch blockers (FIN-9 + SEND-3 now Planner-verified done, removed): GRID-12 (data corruption), PI-3 (inbound totals/prices NaN — makes PEPPOL receive actually correct), FIN-7b-APPLY (numbering db push), PEPPOL-4-FIX-C verify (Florin live check, after PI-3), and the accountant-export assessment (Florin format call). PI-1/PI-2 (download + manual-edit safety net) are quick high-value adjacents. Everything else in the queue (NOTIF-*, PROJ-8/9/10 Kanban, GRID-2/3/4/5/9 cosmetics, CROSS-1, GRID-9b, GRID-13a drop-indicator, SEND-6, DRIVE-OUT-2/3/4) is POST-launch — coder clears the 4 blockers first.**

---

## 🔢 VERSION CHECKPOINTS (a tag = "everything up to here works, nothing prior regressed")
- **v0.1 — Financial core correct.** FIN workstream fully ✅: invoice/credit-note correctness, numbering, sent-doc edit-lock exemption, credited %. The money is trustworthy.
- **v0.2 — Database foundation solid.** GRID workstream ✅: editing no longer janky, flyouts edge-safe, detail-views removed app-wide, selects searchable. The app stops feeling half-broken.
- **v0.3 — Real editor + blocks.** EDITOR workstream ✅: Notion-grade rich text everywhere it's needed.
- **v0.4 — Free-tier happy path.** Capture→invoice→send→expense→receive→export all verified on mobile + desktop. **Launchable free tier.**
- **v0.5+ — Modules deepened** (Projects, Inbox, Calendar, Files, Sales) + localization complete.
- Patch bumps (v0.x.N) for the cosmetic/isolated tail.

---

## 🔧 CURRENT DEBUG QUEUE (Florin's live batch — run in THIS order, one-at-a-time, ✅-gated)
1. **FIN-8** 🟢 coded+committed (1568399), awaiting Florin verify — CN-from-invoice 404 (added `credit-notes/[id]` route). Verify: create CN → opens editor, no 404.
2. **FIN-7 / FIN-7b** 🟡 REOPEN — invoice-number fallback removed (28b4acb), BUT Coral outbox evidence (2026-06-07) shows CN numbering still broken: duplicate `CN-2026-007`, out-of-sequence, inconsistent padding → non-atomic counter. See **FIN-7b** for the atomic-numbering fix. (Also a coder timestamp-fallback to drop.)
3. **QUOTE-7** 🟢 coded+committed (4fc9b13), awaiting Florin verify — now maps to canonical `prop-art-*` ids. Verify: save article w/ discount → Discount col set, Leverancier relation untouched; reload into quote → discount applies.
4. **CROSS-7** 🟢 coded+committed — runtime version sync. Sub-items: **7a** version stamp+`/api/version` (000e9e0) ✅ **7b** client version watcher (b0c74c1) ✅ **7c** SW lifecycle fix (24eb70d) ✅ **7d** Vercel Skew Protection = FLORIN MANUAL toggle in Vercel dashboard **7e** env+SHA badge (243dee7) ✅. Sidebar store bumped to v19 (0b013c7) — forces all browsers to reset to full 16-item sidebar.
— **PROFILE-2** 🟢 coded+committed (216ca27), owned by Florin's direct debug pointing — tracked separately, NOT in this coder queue. Verify: set numbering+color+template, hard-reload, save before load → no reset; glance email still saves (coder folded in email normalization).

## ⚡ CODER — UNATTENDED QUEUE (cron-triggered)
**PER-RUN STEPS (do IN THIS ORDER every cron window):**
0. **PERSIST THE BOARD FIRST (fixes the lost-update clobber — Planner edits live only in the working tree and your later `git checkout` wipes them).** Run `git status --porcelain .agents/workflows/board-v2.md`. If it reports changes, IMMEDIATELY `git add .agents/workflows/board-v2.md && git commit -m "docs(planner): persist board"` — BEFORE reading the queue or touching code. You are NOT authoring/editing the board (Rule 6 still forbids that); you are only SAVING the Planner's pending edits so step 3's revert can't destroy them. This is the ONLY board commit you ever make, and it must change nothing in the file's content.
1. Read the board; take the FIRST not-yet-committed QUEUE item (top to bottom).
2. Do ONLY that item. Never expand scope.
3. `tsc` + lint must be green. If your code change breaks them and you can't fix in-window → `git checkout .` (now safe: the board was already persisted in step 0). Do NOT commit broken code.
4. Commit the code as exactly `<ID>: <imperative>` (rule 3). One item = one commit.
5. **LOOP — do NOT stop after one item (the cron may only fire once, so keep working).** Go back to step 0 and take the NEXT uncommitted item. Re-read the board EVERY iteration — the Planner edits it between your commits, so the top of the queue is authoritative each loop (a new 🔴 item may have jumped ahead). Keep going, one-item-one-commit, until EITHER (a) the queue has no uncommitted items left above the `── SUPERVISED LINE ──`, OR (b) you reach your time/compute budget for this window. Only THEN stop.
6. **CHECK-IN before stopping:** when you do stop (queue dry or budget reached), leave the working tree CLEAN (all commits made or reverted, board persisted). If you stopped because the queue is dry, your last action is simply to stop and wait for the next cron — the Planner will have refilled the board by then. If the same item failed `tsc`/lint twice in a row, SKIP it permanently this session (don't loop forever on one broken item) and move on.
(Bootstrap note for Florin: commit this protocol change ONCE; from then on the coder self-persists the board each run AND loops through multiple items per fire.)

**UNATTENDED SAFETY PROTOCOL (binding):**
- **One item = one commit** named exactly `<ID>: <imperative>`. Do ONLY that item per commit — never bundle. After committing, LOOP to the next item (PER-RUN steps 5–6); do NOT stop after a single item. Stop only when the queue is dry (above the SUPERVISED LINE) or your window budget is spent.
- **tsc + lint must be green before committing.** If your change breaks them and you can't fix in-window → `git checkout .` (revert) and leave it; do NOT commit broken code.
- **If an item is ambiguous, needs a decision, or its files are mid-change → SKIP it, move to the next.** Never guess on scope. Never expand scope.
- **NEVER EDIT/AUTHOR `board-v2.md` content** (Planner-only, pd.md rule 6) — no rewording, no status changes, no marking ✅ (only Florin sets ✅). The ONE allowed board git action is the **content-preserving persist commit in PER-RUN STEP 0** (saving the Planner's pending edits verbatim). Report your own work only via `<ID>:` commit names.
- Items below the `── SUPERVISED LINE ──` are OFF-LIMITS unattended (need Florin) — do not start them.

**ALREADY CODE-COMPLETE (do NOT redo): AGG-1..4, QUOTE-7, SCAN-2, GRID-0/1, CROSS-7, SEND-4, FIN-8 · GRID-12a/b · SCROLL-TRAP · MOBILE-DRAG · SELECT-PORTAL · POSTAL-SEARCH · PROJ-DBKEY · QUOTE-CONVERT-404 · INV-OGM · PAY-1/2/3/4 · INV-HEADER-CLIP · INV-PDF-POLISH · INV-DUEDATE · PEPPOL-SEND-VALIDATE-1/2/3 · SELECT-UNIFY · FIN-NUM · FIN-7b-APPLY · FIN-9 · GRID-2/3/4/5/9/10/13 · SEND-3/5/5b/6 · PROFILE-3 · DRIVE-OUT-1/2/3/4/5 · PROJ-FILTER-SAFETY · PREVIEW-PDF · QUOTE-LINES · QUOTE-LINES-FIX · ACCT-EXPORT · PI-1/2/3/4-7/8/9 · PI-RICH · PI-2-FIX-A/B/C · PROJ-8/9/10 · CROSS-1/2 · EDITOR-1 · DATE-LOC · QUOTE-LINK-404 · WORKSPACE-DB-GHOST · MODAL-LAYOUT · MOB-LOGOUT · RECEIPT-THUMB · SCAN-TEXT-LOC · SCAN-FREE-OCR · FILES-FIND(mobile) · FILES-FIND(desktop)/FILES-FIND-2 · QUOTE-DUPE-BLOCKS · JOURNAL-PLAINTEXT · JOURNAL-UNBOX · QUOTE-DETAIL-GRAPH · ENGINE-ROW-BREAK · PROJ-FILTER-SAFETY · **SEND-UPLOAD · SIGN-DOWNLOAD** (Planner audit 2026-06-23 — full git scan confirms all committed; SEND-UPLOAD `e847758` + SIGN-DOWNLOAD `64c1ca6` added from coder batch 2026-06-23).**

**QUEUE (in order):**

### ~~🔴🔴 QUOTE-DUPE-BLOCKS~~ — COMMITTED `d013d79` (Planner audit 2026-06-23). KanbanView duplicate now deep-clones blocks with fresh ids and passes them as 4th arg. Awaiting Florin verify: duplicate a quote with line items → Copy opens with all blocks present, editing copy does not mutate original.

---

### ~~🟠 JOURNAL-PLAINTEXT~~ — COMMITTED `d6eb222` (with JOURNAL-UNBOX, Planner audit 2026-06-23). htmlToText helper added to JournalCard; HTML tags stripped from block.content in renderBlock. Awaiting Florin verify: journal entries show clean text, no visible tags; code blocks unchanged.

### ~~🟠 QUOTE-DETAIL-GRAPH~~ — COMMITTED `dff40c0` (Planner audit 2026-06-23). QuoteSummaryGraph component renders recharts breakdown + totals strip in place of BlockEditor when databaseId includes 'db-quotations'. Awaiting Florin verify: quote detail modal shows summary graph left + journal right; non-quote records unchanged.

---

### ~~🟠 JOURNAL-UNBOX~~ — COMMITTED `d6eb222` (with JOURNAL-PLAINTEXT, Planner audit 2026-06-23). JournalCard max-h + overflow-hidden removed; PageModal journal wrapper overflow-y-auto + minHeight:'400px' dropped. Awaiting Florin verify: journal flows full height in modal/ProjectDetailView/RecordDetailPage; no inner scroll box.

---

### ~~🟠 ENGINE-ROW-BREAK~~ — COMMITTED `0f1e138` (Planner audit 2026-06-23). Container queries applied to both FinancialRowRenderer copies: three-state layout (side-by-side @≥980px / stacked strip @600–980px / labeled rows @<600px). Awaiting Florin verify: description never wraps beside columns; resize quote+invoice engine wide→narrow to confirm three clean states. ⚠️ Note: FinancialRowRenderer files appear dirty in `git status --short` but `git diff` is clean — likely an index.lock artefact from a concurrent coder run; files match HEAD.

---

### ~~🟠 SEND-UPLOAD~~ — COMMITTED `e847758` (Planner audit 2026-06-23). `QuoteSendModal.tsx` now has a "+ Upload" dropdown button in the Bijlagen header; file picker (`multiple`, PDF/image), per-file `uploadFileAction` calls, uploading rows with spinner, auto-checked in `selectedFileKeys`. Awaiting Florin verify: in the compose modal, upload a local file → it appears pre-checked in the attachment list; sent email arrives with the attachment.

---

### ~~🟠 SIGN-DOWNLOAD~~ — COMMITTED `64c1ca6` (Planner audit 2026-06-23). `QuotationViewer.tsx` now has a Download button in both SIGNING and SUCCESS states; client-side `generatePdfBlob` + `QuotationPDFTemplate`, triggers `URL.createObjectURL` download. Awaiting Florin verify: open a quote link → Download button present; clicking it downloads the quote PDF; after signing, shows the signed copy.

### 🟡 SEND-PWA-CAPTURE — installed CoralOS PWA hijacks the emailed quote link on desktop (Planner 2026-06-22, Florin live: "launches coralOS to view and sign; i have to right-click → open with…"). NOT client-facing-blocking — flagged lower; needs care (touches manifest + routing).
**Measured root:** `public/manifest.json` (`scope:"/"`, start_url `/admin/dashboard`) and `public/manifest-workhub.json` (`scope:"/"`) make the installed PWA capture the ENTIRE origin → on a machine where CoralOS is installed, a public `/{locale}/quote/…` (or `/invoice/…`) link deep-links into the APP window instead of the browser. **A real client (no PWA installed) is UNAFFECTED — they get the browser; mobile already correct.** So this only bites tenants who installed the app (e.g. Florin testing). **Why it's fiddly:** a manifest `scope` is a single path prefix and CANNOT exclude a sub-path, and the app's routes are locale-prefixed (`/nl/admin`, `/fr/admin`…) so there's no clean app-only prefix to scope to. **MEASURE-FIRST, then pick (Florin to weigh in):** (a) narrow the installed-app `scope`/`start_url` if a prefix exists that covers the app surface but NOT `/quote|/invoice` (likely not, given locale prefixes) — investigate; (b) **bulletproof option:** serve the public quote/invoice viewers from a host with no PWA (e.g. the marketing/`www` host or a dedicated `sign.coral-group.be`) so those links ALWAYS open in the browser — bigger change, touches the email link builder (`send-quote.ts`/`send-invoice.ts`) + routing; (c) try the `launch_handler`/`capture_links` manifest hints (limited browser support) as a lightweight mitigation; (d) accept + document if none is clean, since clients are unaffected. **Do NOT guess-change the manifest scope and risk breaking the installed app's routing — REPRODUCE + measure which option holds.** **VERIFY:** on a machine with CoralOS installed, clicking an emailed quote link opens the DEFAULT BROWSER public signing page, not the app window; the installed app still launches/route correctly from its own icon. **Commit (per chosen path):** `SEND-PWA-CAPTURE: public viewer links open in browser not installed PWA`.

---

### 🟠 JOURNAL-EDITOR — New Journal Entry modal: replace the dead textarea + block-type buttons with the real editor, in a more generous modal (Planner 2026-06-22, Florin live: "the formatting options don't do anything… instead of a text field in a bounding box, make the modal more generous and put the proper text editor in there"). Partial down-payment on the EDITOR-3 "journal as editor" vision, but scoped to REUSE the existing BlockEditor — NOT the full Tiptap workstream (that stays supervised).
**ROOT (measured, `src/app/[locale]/admin/journal/page.tsx`):** the New Journal Entry modal is a single `<textarea>` (L550, bound to `newEntryContent`) plus a `selectedBlockType` button row (TEXT/H1/H2/H3/BULLET/NUMBER/TODO/QUOTE/CALLOUT/CODE/DIVIDER). On publish (L238-273) it builds ONE block `{type:selectedBlockType, content:newEntryContent}` and one page — so the "formatting" buttons only pick the type of a SINGLE block; there's no inline formatting, no multi-block, nothing the user recognises as an editor. The app ALREADY has the proper editor: **`BlockEditor`** (`components/admin/database/components/BlockEditor.tsx`, multi-block + slash menu, used in records). **FIX:** (1) make the modal generous — wider + taller writing area (e.g. `max-w-3xl`, comfortable min-height), editor grows naturally and the MODAL scrolls if needed (NO cramped inner scroll box — consistent with JOURNAL-UNBOX). (2) Replace the textarea + the BLOCK TYPE button row with `<BlockEditor databaseId={GENERAL_DB_ID} pageId={draftId} />`. **Mechanism (BlockEditor is store-bound to a page):** on modal open, create a DRAFT journal page in `GENERAL_DB_ID` and mount BlockEditor against it; "PUBLISH ENTRY" finalises (keep the page; persist TITLE/AUTHOR/link props as today); "CANCEL" or close → DELETE the draft page so no orphan is left (avoid the WORKSPACE-DB-GHOST-class litter; reuse the existing `createDatabase` guard for the General Journal DB so it isn't spuriously minted). Keep TITLE, LINK TO RECORD, AUTHOR fields as-is. Drop the now-dead `selectedBlockType`/`newEntryContent` state + the button row. (Alt if the coder prefers to avoid draft pages: edit blocks in local state and pass them as the 4th `initialBlocks` arg to `createPage` on publish — but the draft-page route reuses BlockEditor directly with least new code.) **VERIFY:** open New Journal Entry → a roomy modal with the full block editor; add a heading + bullets + a todo as REAL separate blocks with working formatting; Publish → the entry renders all blocks (and reads cleanly per JOURNAL-PLAINTEXT); Cancel → no draft page or ghost DB left behind. **Commit:** `JOURNAL-EDITOR: full BlockEditor in a roomier journal entry modal`.

---

### ~~🔴 FILES-FIND-2~~ — COMMITTED `d6cb069` (Planner audit 2026-06-22). The desktop `FileManager.tsx` WAS reconciled to flat Blob in that commit: `listAllTenantFiles()` added to `files.ts`, store updated to use it in global mode, FileManager rewritten with grouped-by-context display and no folder/breadcrumb nav. Awaiting Florin verify: desktop Files module shows project + quote files grouped by context, no empty "drive root".

---

### 🔴 SELECT-MODAL-DISMISS — SearchableSelect inside a dialog closes the modal when you click its search (Planner 2026-06-22, Florin live: "search is unclickable, it closes the modal" — Schedule New Shift → Project). Regression introduced by SELECT-PORTAL; component-level fix.
**ROOT (measured):** SELECT-PORTAL made `src/components/ui/SearchableSelect.tsx` portal its dropdown to `document.body` (`createPortal`, `fixed z-[99999]`, L138-190) — correct for un-clipping. BUT the portaled wrapper (L141) does NOT stop pointer-event propagation. The Schedule New Shift modal is a Radix `Dialog`/`DialogContent` (`CreateShiftForm.tsx` uses `SearchableSelect` for Project; rendered by `EditShiftDialog`/the shadcn Dialog). Because the dropdown DOM lives OUTSIDE the `DialogContent` subtree, Radix's DismissableLayer sees a pointerdown on the search input/options as an OUTSIDE interaction → fires `onOpenChange(false)` → the whole modal closes before the click lands. So the search appears "unclickable." **FIX (in `SearchableSelect.tsx`, component-level = Rule 7, repairs EVERY SearchableSelect inside any Radix Dialog/Popover):** add `onPointerDown={(e)=>e.stopPropagation()}` AND `onMouseDown={(e)=>e.stopPropagation()}` to the portaled dropdown container (L141) so interactions inside the dropdown don't bubble to `document` and trip an ancestor dismissable layer's outside-close. Keep the existing inner option/clear `stopPropagation` and the keyboard handling; do NOT remove the portal (it's what fixes SELECT-PORTAL clipping — only stop the events). **VERIFY:** Schedule New Shift → open Project → click the search field + type → modal STAYS open, list filters, picking a project works and the dropdown still escapes container clipping; a SearchableSelect NOT in a dialog (engine/project dashboard) still works unchanged. **Commit:** `SELECT-MODAL-DISMISS: stop dropdown pointer events from closing parent dialog`.

---

### 🟠 MULTI-UPLOAD — enable multiple-file selection + upload on attachment surfaces (Planner 2026-06-22, Florin live: "we need multiple file upload, this is 2026"). App-wide, but scoped to the right surfaces.
**Measured:** `uploadFileAction(formData, recordType, recordId?)` (`actions/files.ts` L7) is SINGLE-file (`formData.get('file')`) — keep it; multi-upload = the CLIENT iterating the `FileList` and calling it per file (collect keys, show per-file progress). No server/schema change. Most `<input type="file">` across the app lack `multiple` and their `onChange` reads `files[0]`. **FIX — add `multiple` + iterate `Array.from(e.target.files)` (upload each, append results, then reset the input) on the GENERAL attachment/library surfaces:** `FileManager.tsx` L304 (Files library); `PageModal.tsx` L999 (record attach); `TaskDetailPanel.tsx` L631; the time-tracker shift/entry attachment inputs (`CreateShiftForm` L982/1447, `EditShiftDialog` L694, `Schedule.tsx` L396, `LateEntryCard` L413, `ClockOutForm` L135) WHERE they're general attachments. Use one shared pattern (a small `uploadMany(files, recordType, recordId)` helper that maps over `uploadFileAction` — sequential or `Promise.all`, with a per-file uploading indicator). **EXPLICITLY EXCLUDE (must stay single — semantic):** signature uploads (`QuotationViewer` L547, `InvoiceViewer` L417), logo + stationery (`DocumentTemplatesModule` L390/482), and the single-receipt camera/scan in `TicketCaptureModal` L527/528 (one receipt → one ticket; batch-receipts is a separate flow, NOT here). The quote/invoice `PDFImportModal` and `PurchaseInvoiceEngine` source-doc inputs: leave single unless their import flow already handles a batch — do NOT rework those flows here. **(SEND-UPLOAD already specs `multiple` for the quote compose modal — same pattern; don't duplicate.)** If this is too much for one clean commit, split per surface keeping the `MULTI-UPLOAD` prefix, but the attachment/library trio (FileManager, PageModal, TaskDetailPanel) is the priority. **VERIFY:** in the Files library, a record's attachments, and a task's files, select 3 files at once → all 3 upload and appear; signature/logo/stationery inputs still accept exactly one. **Commit:** `MULTI-UPLOAD: multiple file selection + upload on attachment surfaces`.

---

### 🟠 FILES-CAROUSEL — carousel prev/next navigation in the file viewer (Planner 2026-06-22, Florin live: "move through the files without exiting each time"). Best AFTER FILES-FIND-2 (the reconciled list), but works against the current list too.
**Measured:** `src/components/admin/file-manager/FileViewerModal.tsx` takes a single `{ file, onClose }` (no navigation); `FileManager.tsx` opens it at L362 with one selected file. To move to the next file you must close + reopen. **FIX:** make the viewer carousel-capable. (1) `FileViewerModal`: change props to receive the ORDERED list + position — `files: FileNode[]`, `index: number`, `onIndexChange(i)` (or `onPrev/onNext` + `hasPrev/hasNext`). Render left/right chevron buttons overlaid on the content (vertically centered), a position counter in the header (e.g. "3 / 12"), and bind **ArrowLeft/ArrowRight** keys to prev/next plus **Esc** to close. Navigating swaps the displayed file IN PLACE (re-derive `isImage`/`isPdf`/`previewUrl` from the new file) — never unmount/close. Clamp at the ends (disable the arrow at first/last) — no wrap. Optional nice-to-have: left/right swipe on touch for the mobile carousel feel. (2) `FileManager`: track the selected file's INDEX within the current visible/filtered list (display order — the same flat, context-grouped order FILES-FIND-2 renders) and pass `files`+`index`+`onIndexChange`; prev/next walk that list. **VERIFY:** open a file in the Files module → left/right arrows AND ←/→ keys move through the files with the viewer staying open and the counter updating; arrows disable at the first/last file; Esc closes; images and PDFs both preview correctly as you move. **Commit:** `FILES-CAROUSEL: prev/next navigation in file viewer modal`.

---

### ~~🔴🔴 QUOTE-LINK-404~~ — COMMITTED `55acce4` (Planner 2026-06-22). Awaiting Florin verify: open emailed `/nl/quote/{id}` in incognito → public signing page loads, no 404.

---

### 🔝 FINISH-FIRST — SKIPPED-CLUSTER RE-PASS (Planner 2026-06-22). ALL COMMITTED (Planner audit 2026-06-22 updated).
**Status:** WORKSPACE-DB-GHOST `254f5e2` ✓ · RECEIPT-NOVAT + GHOST-HAMBURGER `d12c817` ✓ · SCAN-FREE-OCR `73fd2f2` ✓ · SCAN-TEXT-LOC `c941c11` ✓ · RECEIPT-THUMB `02911b6` ✓ · MODAL-LAYOUT `998bed3` ✓ · MOB-LOGOUT `3aa3dcb` ✓ · **FILES-FIND(desktop) `d6cb069` ✓** (Planner re-audit: the commit that posted as FILES-FIND did do the full desktop FileManager Blob reconciliation). **ONE STILL SKIPPED:**
- **MOBILE-RECORD-MODAL — SKIPPED:** no commit. Full spec below (line ~549): expense→PurchaseInvoiceEngine; invoice modal+file; contact generic modal; responsive full-screen at 380px. **Commit:** `MOBILE-RECORD-MODAL: per-type record open (responsive modal; expense→engine; invoice shows file)`.

1. **FILES + DB hygiene:** FILES-FIND(mobile) `f926452` ✓ · FILES-FIND(desktop) `d6cb069` ✓ · WORKSPACE-DB-GHOST `254f5e2` ✓
2. **TicketCaptureModal:** RECEIPT-NOVAT ✓ · SCAN-FREE-OCR ✓ · SCAN-TEXT-LOC ✓ · RECEIPT-THUMB ✓
3. **PageModal / record-open:** MODAL-LAYOUT ✓ · **MOBILE-RECORD-MODAL — SKIPPED (one remaining)**
4. **Mobile shell/nav:** GHOST-HAMBURGER ✓ · MOB-LOGOUT ✓

### 🔴 DO NEXT — SELECT-MODAL-DISMISS then MOBILE-RECORD-MODAL (Planner audit 2026-06-23).
Coder completed SEND-UPLOAD + SIGN-DOWNLOAD (both committed 2026-06-23). MOBILE-RECORD-MODAL remains a persistent skip.
1. **SELECT-MODAL-DISMISS** (🔴 regression — topmost uncommitted item in file) — SearchableSelect portaled dropdown trips Radix DismissableLayer outside-close. Fix: `onPointerDown + onMouseDown e.stopPropagation()` on portaled container in `SearchableSelect.tsx` L141. Full spec above (line ~119). **Commit:** `SELECT-MODAL-DISMISS: stop dropdown pointer events from closing parent dialog`.
2. **MOBILE-RECORD-MODAL** (persistent skip, last outstanding FINISH-FIRST item) — `m/expenses/page.tsx` L288: route expenses to `PurchaseInvoiceEngine` (mirror desktop AGG-1, `onOpenRecord` pattern). Invoice modal: show the document/file below properties. `PageModal` at mobile width: full-screen single-column, no split panes, scrollable. Full spec at line ~549 below. **Commit:** `MOBILE-RECORD-MODAL: per-type record open (responsive modal; expense→engine; invoice shows file)`.
3. Then JOURNAL-EDITOR → MULTI-UPLOAD → FILES-CAROUSEL (specs above the SUPERVISED LINE).

**After these three, proceed to items below the SUPERVISED LINE only (supervised). The REGRESSION WATCH + PI-FIX + DRIVE-OUT sections are ALL committed as of 2026-06-22 (see ALREADY CODE-COMPLETE). Full specs kept below for Florin verify reference only.**

---

**⚠️ REGRESSION WATCH + DRIVE-OUT + PI-FIX — ALL COMMITTED (Planner audit 2026-06-22). Specs preserved below for Florin verify/reference. Coder: check git log for each ID — commits exist; do NOT redo. Skip to items above the SUPERVISED LINE if any remain, otherwise stop and wait for queue refill.**

**⚠️ RECOVERED 2026-06-18 (these were clobbered by a working-tree revert; Florin re-committed). GRID-12a/b ✓ · SCROLL-TRAP ✓ · MOBILE-DRAG ✓ · SELECT-PORTAL ✓ · POSTAL-SEARCH ✓ · SEND-6 ✓ · DRIVE-OUT-1/2/3/4/5 ✓ · GRID-13 ✓ — ALL committed.**

👉 **GRID-12 — DO NEXT — DATA-INTEGRITY (suppliers DB; SHARED grid primitives → all DBs).**
- **GRID-12a** (`hooks/useVatLookup.ts`): (i) **stall** — `isValidVat` L105 `/^[A-Z]{2}\d{8,12}$/` requires a country prefix → a bare Belgian number (`0768798123`) never reaches `'loading'` → flyout stuck. FIX: accept bare BE (no 2-alpha prefix + 9–10 digits → normalize `BE0…`); keep prefixed path. (ii) **wrong row** — cell→record via DOM sibling index (L95-102 `rowData[rowIndex]`) drifts by one. FIX: add `data-page-id` to each grid row in `NotionGrid`, read `cell.closest('[data-page-id]')?.dataset.pageId` for `rowId` + `applyVatLookup`.
- **GRID-12b — modal edits leak to the live grid (data corruption).** No guard disables the grid under an open `PageModal`; DSG keeps an active cell + processes keystrokes; the modal stops mouse/click but NOT keydown + no focus trap → typing Email/Name writes to the next row. FIX: (1) `DatabaseClone` — when `openParam`/`projectIdParam` set, make grid container `inert` + `pointer-events-none`; (2) `PageModal` — `onKeyDown={e=>e.stopPropagation()}` on the portal wrapper + focus trap; (3) clear/blur DSG active cell on open.
- **VERIFY:** (a) type `0768.798.123` AND `BE0768798123` in a VAT cell → fires, fills THAT row; (b) edit a supplier in the modal → only that record changes. **Commits:** `GRID-12a: VAT lookup id-based row targeting + BE bare-number trigger` · `GRID-12b: inert grid + focus-trap under record modal`.

**REGRESSION WATCH 2026-06-19 (Florin):**
- **SEND-5b STILL opens the app — likely DEPLOY/CACHE, not code.** Code is correct on branch (`8afa1cc`: `noRewriteSegs` has `quote`/`invoice`; public page clean; `sw.js` pass-through). NOT a code task unless it reproduces in a FRESH INCOGNITO window. If incognito works → cache/Vercel-Skew (CROSS-7d) on Florin's browser. If incognito ALSO fails → confirm prod deploy includes `8afa1cc`, then re-open for a deeper intl/locale trace of `/nl/quote/{id}`. **No coder action until incognito test result.**
- **👉 PROJ-DBKEY — ROOT CAUSE FOUND: projects "gone" + handover-created project not showing = the projects view reads the WRONG database key.** Confirmed: the canonical Projects DB is **`db-1`** (`lib/lockedDbUtils.ts` L21 `'db-1':'projects'`; `systemDatabases.ts` L22; used by handover `ClientQuotationEngine` L627, journal, planning, NewProjectButton, portals, quote-service, relation configs — ALL `db-1`). But `projects-management/page.tsx` reads **`db-projects`** (L71 `databaseId="db-projects"`, L81 `resolveDbId('db-projects')`) — an UNREGISTERED key (not in lockedDbUtils/systemDatabases) → resolves to nothing → empty view. All projects live in `db-1`; the view looks at the wrong DB. **FIX:** revert to canonical `db-1` in the 3 spots the projects-work introduced `db-projects`: `projects-management/page.tsx` L71 + L81 → `db-1`; `dashboard/page.tsx` L161 `countPages('db-projects'…)` → `db-1`; `PageModal.tsx` L687 `databaseId === 'db-projects' || startsWith('db-projects')` → `db-1`. (Full rename to `db-projects` everywhere = NOT now; data + all code is on `db-1`.) **VERIFY:** projects-management shows all existing projects; quote handover → created project appears immediately. **Commit:** `PROJ-DBKEY: projects view reads canonical db-1 not db-projects`.
- **👉 QUOTE-CONVERT-404 — green "Invoice" button in the quote engine 404s.** `ClientQuotationEngine.handleConvertToInvoice` L714 navigates to `/${locale}/admin/invoices/${id}` — **that route does NOT exist.** The real sales-invoice viewer is `/admin/financials/income/invoices/[id]` (confirmed present). Invoice IS created correctly in `db-invoices`; only the navigation target is wrong. **FIX:** L714 → `router.push('/'+locale+'/admin/financials/income/invoices/'+newInvoice.id)`. **ALSO CHECK (sibling):** the duplicate-quote nav L732 `/admin/quotations/${id}` — verify `[locale]/admin/quotations/[id]/page.tsx` exists; if not, point it at the correct quotations viewer too. **VERIFY:** accept a quote → Invoice button → opens the created sales invoice, no 404. **Commit:** `QUOTE-CONVERT-404: invoice button navigates to income/invoices route`.
- **INV-PDF-POLISH — sales-invoice PDF cleanup (Florin 2026-06-19, `InvoicePDFTemplate.tsx`; apply to BOTH render variants — the ~L387-434 block AND the ~L629-670 block).** (a) **Remove the stray "invoice_legal" text** — the small grey centered element renders the raw i18n key `invoice_legal` (leaked/untranslated). Remove that element (the autoliquidation legal paragraph already shows the real legal text; the leaked key is redundant). (b) **€ spacing** — the calc/summary block uses `€{n}` with NO space (L422/429/434/552/560/665/670); line items use `€ {n}`. Standardize to a single space `€ {n}` EVERYWHERE (also fix the double-space `€  ` at L205/213). (c) **Calc-block legibility** — bump the summary block: labels ~10-11px darker (not 8.5px grey), values ~12px bold, MONTANT DÛ ~16-18px, add row padding/contrast. (d) **Move the legal + totals + Stripe block to the BOTTOM of the page** — page as flex column, push that block down via `marginTop:'auto'`; keep the FIN-9 `wrap={false}` so it never splits. **VERIFY:** invoice PDF shows no "invoice_legal" text, `€ 651.75` with a space, a legible totals block, all sitting at the page bottom. **Commit:** `INV-PDF-POLISH: remove leaked key, € spacing, legible totals, bottom-align block`.
- **👉 INV-OGM — sales invoices missing the structured communication / OGM (Florin 2026-06-19: "hell of a lot important"). Infra EXISTS, wiring is broken.** `lib/ogm.ts generateOGM()` (valid +++/mod-97) + record field `structuredComm` + `InvoicePDFTemplate` display (receives `structuredComm` at L610/724/1346/1450) all exist. Generation only happens in the `pages.ts` server action (L110: `databaseId.startsWith('db-invoices') && !structuredComm → generateOGM()`). **GAPS:** (1) the quote→invoice conversion (`ClientQuotationEngine.handleConvertToInvoice` L682) creates the invoice via the client-store `createPage` with explicit props and **no `structuredComm`**, bypassing the server gate → converted invoices have NO OGM. (2) `generateOGM()` is called with no arg → random timestamp, not tied to the invoice number (not stable/deterministic). (3) On the PDF the OGM only shows in the tiny footer line (L573/589), not prominently. **FIX:** (a) GUARANTEE `structuredComm` on EVERY sales invoice across ALL creation paths — generate it where the invoice NUMBER is allocated (or in the conversion handler) seeded from the invoice sequence number → `generateOGM(sequenceNumber)` (deterministic + unique). (b) LAZY BACKFILL: when an invoice is opened/rendered without `structuredComm`, generate from its number + persist (so existing invoices like "Facture 2026-26" get one). (c) DISPLAY PROMINENTLY in the PDF payment block next to the IBAN — e.g. "Gestructureerde mededeling / Communication structurée: +++xxx/xxxx/xxxxx+++" in bold — in addition to the footer. **VERIFY:** a converted invoice AND a directly-created one both show a valid OGM prominently; reopening the same invoice shows the SAME OGM (stable). **Commit:** `INV-OGM: guarantee + backfill structured communication, show prominently`.
- **STRIPE-PAY-REALITY — the "Pay via Stripe" QR on invoices is NON-FUNCTIONAL (Florin asked 2026-06-19).** L400/643 encode a fabricated URL `checkout.stripe.com/pay/{invoiceNumber}` — NOT a real Stripe checkout session (real = `/c/pay/cs_live_…`). It cannot take payment; nothing reaches the bank. The only real Stripe code (`/api/stripe/checkout`) is the tenant's OWN subscription billing; there is NO Stripe Connect / per-invoice checkout / payout-to-tenant-bank flow. **DECISION (Florin 2026-06-19): payments are CONDITIONAL per tenant. DEFAULT = bank-transfer QR + instructions (no Stripe needed); Stripe REPLACES it for tenants who opt in. Short onboarding asks if they have/want a Stripe account. Build for Coral now, but SCAFFOLD for expansion (future Stripe reseller/Connect + paid feature).** → **PAY** workstream below (supersedes the old single-Stripe spec).
- **👉 PAY-1 — DEFAULT bank-transfer QR + instructions (HIGH, Tier-1: makes EVERY invoice payable now, no Stripe).** Replace the fabricated Stripe QR (`InvoicePDFTemplate` L400/643) with a real **EPC / SEPA Credit-Transfer QR** ("BCD" payload: service tag, beneficiary = tenant `companyName`, IBAN = tenant `iban`, amount = `totalIncTax` EUR, remittance = the **OGM** from INV-OGM). Plus a textual "Betalen via overschrijving / Virement" block: IBAN, bedrag, gestructureerde mededeling (OGM). Uses `tenant.iban` (exists). This is the default payment path for ALL tenants incl. Coral. **VERIFY:** invoice PDF shows a scannable SEPA QR that a banking app reads (beneficiary/IBAN/amount/OGM pre-filled) + the text block. **Commit:** `PAY-1: SEPA/EPC bank-transfer QR + payment instructions (default)`.
- **PAY-2 — payment settings + provider gating (scaffold for expansion).** New `settings/payments/page.tsx`: per-tenant `paymentProvider` = `'bank_transfer'` (default) | `'stripe'`. Add tenant fields: `paymentProvider`, `stripeSecretKey` (ENCRYPTED), `stripePublishableKey` (these are for RECEIVING customer payments — distinct from the existing `stripeCustomerId`/`stripeSubscriptionId` which are Coral's SaaS billing). Add a `STRIPE_PAYMENTS` flag to `feature-flags.ts` (`canAccess('STRIPE_PAYMENTS', planType)`; FOUNDER/Coral bypass) so Stripe can become a paid tier / reseller model later. **Build a `PaymentProvider` abstraction** (interface: `getInvoicePayment(invoice) → { qrPayload, instructions, payUrl? }`) with two implementations (bank_transfer, stripe) selected by the tenant setting — this is the expansion scaffold. **Commit:** `PAY-2: payments settings + provider abstraction + stripe key storage`.
- **PAY-3 — Stripe provider (opt-in; replaces the QR for opted-in tenants).** When `paymentProvider==='stripe'`: server action `createInvoiceCheckout(invoiceId)` using the tenant's key → `stripe.checkout.sessions.create({mode:'payment', line_items amount=totalIncTax EUR, metadata:{invoiceId,tenantId}, success/cancel_url})` → real `session.url` (stored on the record at send-time so the QR is stable); QR + "Betaal online" link encode it; webhook `/api/stripe/webhook` on `checkout.session.completed` → mark invoice **Betaald + date + method=stripe** (ties PI-RICH-D). Funds land in the tenant's Stripe→bank (tenant keys now; Connect = the reseller-era upgrade, slots into the same provider abstraction). **VERIFY:** Coral opts into Stripe → invoice QR = real checkout → test payment flips invoice to Paid. **Commit:** `PAY-3: stripe checkout provider (opt-in) + webhook marks paid`.
- **PAY-4 — short onboarding.** On first invoice / in payment settings: "Heeft u een Stripe-account? [Ja → sleutels invoeren] / [Nee → er een aanvragen? (Stripe signup) of voorlopig overschrijving gebruiken]". Sets `paymentProvider`. **Commit:** `PAY-4: payment onboarding (stripe vs bank transfer)`.
- **ACCT-EXPORT — accountant handover (Florin 2026-06-19 "build it"; completes the free-tier spine endpoint "accountant export"). Today only a raw per-DB CSV dump (`useExportCSV`) exists — not a usable journal. Build real Belgian journals. PHASED.**
  - **Format assumptions (Planner's call, Florin to correct in debug):** Dutch column headers; **semicolon `;` delimiter** + UTF-8 BOM (Belgian Excel); VAT base split per rate column; period via the existing acctDate presets (last-month / last-trimester / this-year / custom). Accountant-software-specific import formats (WinBooks/Octopus/Yuki/Exact/CodaBox) = LATER, not MVP.
  - **ACCT-EXPORT-A (quick win, big value) — two structured journal CSVs for a chosen period.** A "Boekhouder export" action (in `/admin/journal` or financials) with a period picker. Produces: **`verkoopdagboek_{period}.csv`** (sales, from db-invoices in range) cols: `Datum; Factuurnr; Klant; BTW-nr klant; Maatstaf 21%; Maatstaf 6%; Maatstaf 0%; Medecontractant; BTW-bedrag; Totaal incl; Gestructureerde mededeling`; **`aankoopdagboek_{period}.csv`** (purchases, from db-expenses in range) cols: `Datum; Leverancier; BTW-nr; Factuurnr; Maatstaf; BTW-bedrag; Totaal incl`. Credit notes included as NEGATIVE rows. Reuse the acctDate range logic from `NotionGrid` (L493-551). **VERIFY:** export last-trimester → both CSVs open in Excel with correct per-rate VAT bases + totals matching the records. **Commit:** `ACCT-EXPORT-A: sales + purchase journal CSVs for a period`.
  - **ACCT-EXPORT-B — bundle the source documents.** Server route that gathers the period's records + fetches each PDF from Blob (purchase `receiptUrl`; sales = generated invoice PDF) + zips with the two journal CSVs → one download. Stamp exported records (`accountantExportedAt`) for incremental re-exports. **Commit:** `ACCT-EXPORT-B: zip journals + source PDFs`.
  - **ACCT-EXPORT-C (later, supervised) — accountant-software import format(s)** once Florin names his accountant's tool. **Not now.**
- **DATE-LOC (FUNCTIONAL — Florin 2026-06-19: the `@` date-picker has US localisation).** `GlobalMentionDateInterceptor.tsx` L84 inserts `format(selectedDate, 'MMM d, yyyy')` → "Apr 15, 2026" (US). And the `<Calendar>` (L145) gets no `locale` → English labels + Sunday week-start. **FIX:** (1) insert as Belgian `dd/MM/yyyy` (match `formatDateBE`/the app locale — ideally locale-aware: nl/fr → `dd/MM/yyyy`); (2) pass the date-fns locale (`nl-BE`/`fr-BE` per UI language) + `weekStartsOn={1}` (Monday) to `<Calendar>` so month/weekday labels localise; (3) localise the quick buttons (Vandaag/Morgen/Volgende maandag/Over 30 dagen) via `t()`. **VERIFY:** type `@`, pick a date → inserts `15/04/2026`; the calendar shows Dutch/French labels, Monday-first. **Commit:** `DATE-LOC: @ date picker uses Belgian locale + format`.
- **👉 INV-HEADER-CLIP (FUNCTIONAL UI BUG — Florin 2026-06-19: the invoice header where Client/Project/Quote are added is "completely cut off by the size of the container… acts like a cutting mask").** ROOT: those selectors use absolute-positioned dropdowns (`ClientInvoiceEngine.tsx` client L967, project L1008, offerte L1045-1065, doctype L895/935) that get clipped by an `overflow-hidden` (likely `rounded-2xl … overflow-hidden`/`isolate`) ANCESTOR wrapping the engine — and/or the header row itself is clipped by the container. **FIX:** find the clipping ancestor between the header and the viewport; either (a) portal the dropdowns to `document.body` (positioned via the trigger's rect) so they escape the clip, or (b) remove `overflow-hidden` from the header zone (use `overflow-visible`) and let the header wrap rather than be masked. Apply the SAME fix to `ClientQuotationEngine` header if it shares the pattern. **VERIFY:** open the client / project / offerte dropdowns in the invoice header → fully visible, not cut off, at any container size. **Commit:** `INV-HEADER-CLIP: header selectors escape overflow clip`.
- **PREVIEW-PDF (Florin 2026-06-19) — add a "Preview PDF" button next to "Export PDF" in BOTH engines.** Both already build the blob via `generatePdfBlob(doc, tenant)` (quotation L488/1216/1266, invoice L614/1453). Preview = same blob → `window.open(URL.createObjectURL(blob), '_blank')` (open inline, no download) instead of triggering a download. Add the button beside Export PDF in `ClientQuotationEngine` (~L1241) and `ClientInvoiceEngine`. **VERIFY:** Preview opens the PDF in a new tab/viewer without downloading; Export still downloads. **Commit:** `PREVIEW-PDF: preview button beside export in quote/invoice engines`.
- **QUOTE-LINES-FIX (FUNCTIONAL — Florin 2026-06-19: "the line options present are enough, but not all are working"). Higher priority than the editor/feature items.** AUDIT every insertable line/block type in the quote + invoice engines (`FinancialRowRenderer.tsx`, `QuotationRow.tsx`/`InvoiceRow.tsx`, `ClientQuotationEngine`/`ClientInvoiceEngine`): `article`, `bestek`, `line`, `post`, `section`, `subsection`, `text`, `divider`. For EACH verify: inserts, renders, drags/reorders, persists, and renders in the PDF. Fix the ones that don't. (Florin can name the specific broken ones to narrow scope; otherwise audit all.) **VERIFY:** insert + drag + save + PDF each line type; all work. **Commit:** `QUOTE-LINES-FIX: repair non-working quote/invoice line types`.
- **QUOTE-LINES — add `space` + `page-break` line types to the quote/invoice engine (Florin 2026-06-19; functional-first, this is mise-en-page polish — queue BEHIND the bug fixes).** Two new block types, insertable + DRAGGABLE/reorderable exactly like existing lines. (1) **`space`** = a blank spacer row (optional height control) to put vertical gaps between rows. (2) **`page-break`** = forces the following content onto a new PDF page. **Touch points:** add `'space' | 'page-break'` to `BlockType` (`database/types.ts` L63); add them to the slash/insert menu + the quote & invoice row renderers (`quotations/FinancialRowRenderer.tsx`, `invoices/FinancialRowRenderer.tsx`, `QuotationRow.tsx`/`InvoiceRow.tsx`) — render `space` as an empty gap row with a drag handle, `page-break` as a visible dashed "Page break" divider; both draggable via the existing dnd. **PDF** (`QuotationPDFTemplate.tsx` + `InvoicePDFTemplate.tsx`): `space` → `<View style={{height:N}}/>`; `page-break` → react-pdf `<View break />` (forces new page). **CRITICAL:** both are non-financial — exclude them from `calculateInvoiceTotals` / line numbering / VAT (they have no qty/price); they must NOT affect totals. **VERIFY:** insert a space + a page-break, drag to reorder, generate the PDF → gap renders, content after the break starts on a new page, totals unchanged. **Commit(s):** `QUOTE-LINES: space + page-break block types (editor + pdf)`.
- **EDITOR-STRATEGY (Florin 2026-06-19 — definitive direction; supervised; AFTER launch-functional).** **Principle: KEEP the custom block model everywhere — do NOT replace blocks; expand them.** UI is SECOND to function → library choice = **easiest to maintain long-term + best compatible with our block logic → Tiptap** (ProseMirror: its document model IS a node/block tree → maps cleanly onto our blocks; headless, mature, stable, huge ecosystem). BlockNote (on Tiptap) = faster to ship but a thinner/opinionated layer → worse on "maintain over a long time" → NOT chosen. Three phases, in order:
  - **EDITOR-1 — block-level rich text on the EXISTING blocks (Notion-style, "not more").** Add inline/block formatting (bold/italic/links/headings per block) to the current custom blocks; keep the block model + slash menu. Goal: richer block editing without changing the block architecture. (Tiptap-per-block or a clean formatting layer.)
  - **EDITOR-2 — quote/financial engines.** (a) **QUOTE-LINES-FIX (FUNCTIONAL — see separate item; do in the functional tier):** the present line types are enough but not all work — audit + fix. (b) **Text-field line upgrade ONLY:** richer editing on the free-text line (it's isolated from the financial functions — safe to enhance); the financial rows (article/bestek/line/post + pricing) stay exactly as-is, untouched.
  - **EDITOR-3 — the Journal = full-blown editor (the ERP's jewel) + connected notes.** The Journal becomes the canonical full Tiptap editor. Other databases' "note" field = a LINKED journal entry: opening a record's note opens/edits a note that LIVES in the journal; it is surfaced in the connected DB but has NO influence on that DB's structured data (editing the note edits the journal entry only). Spec: a note→journal relation on records; render the journal editor in-context; persistence on the journal entry. **Florin gates each phase.**
- **GRID-11 — Notion-grade DB upgrade (Florin scope: Tier-1 + dependencies + financial rollups + BASIC automations; supervised, post-launch). MEASURED: more exists than expected — rollups (`columns/RollupColumn.tsx`) + formulas (`columns/FormulaColumn.tsx` + `formulaReference.ts` catalog) are IMPLEMENTED + wired in `useGridColumns` (L119/147); relations work (`LinkedRecords.tsx`). General automations do NOT exist (the `trigger` scaffold at types.ts L230 is payment-plan-only). So this is mostly VERIFY/EXPOSE + build 2 gaps.**
  - **GRID-11-1 — financial rollups + formulas: verify end-to-end + expose in the UI (engine already exists, do NOT rebuild it).** Confirm `rollupColumn(relationProp, targetProp, aggregation)` computes sum/avg/count/min/max correctly across a relation, and `formulaColumn(expr)` evaluates. Ensure the column-creation UI (`AddColumnFlyout`/`DbPropertiesPanel`) lets a user actually CONFIGURE a rollup (pick relation → target property → aggregation) and a formula. **VERIFY (real financial case):** a project rollup = SUM of related invoices' `totalIncVat` shows the correct euro total; a formula (e.g. margin %) computes. Fix only what's missing in compute or config-UI. **Commit:** `GRID-11-1: expose + verify financial rollups and formulas`.
  - **GRID-11-2 — dependencies (NEW).** Task/project dependencies: a self-relation `blockedBy`/`blocks` (or a dependency property type) + a visible "blocked" indicator on records + basic surfacing in the timeline/gantt (`ProjectTimelineView`). Advanced auto-date-shift cascade = LATER. **VERIFY:** mark task B blockedBy A → B shows blocked; both appear linked in the timeline. **Commit:** `GRID-11-2: task/project dependencies + blocked indicator`.
  - **GRID-11-3 — BASIC automations (NEW).** One simple trigger→action rule per database: e.g. "when `status` → Done, set `completedDate = today`" and "when `status` → X, create a notification" (ties to NOTIF). Stored as a small rule list on the database; evaluated on property change. Keep it BASIC (single trigger, single action, no conditions/branches) — advanced rule builder is a later workstream. **VERIFY:** changing a status fires the configured action. **Commit:** `GRID-11-3: basic per-database automations`.
- **👉 SCROLL-TRAP — containers eat the scroll when the cursor/finger is over them (Florin 2026-06-19, desktop + mobile).** ROOT: `globals.css` L137 sets `overscroll-behavior: none` GLOBALLY (added to kill edge rubber-band) → disables scroll-CHAINING everywhere; combined with `NotionGrid.tsx` L774 `overflow-hidden overscroll-none` + the L806 `onWheel` handler, the wheel/touch is captured over those containers and never scrolls the page/parent. **FIX:** (a) STOP the global `overscroll-behavior:none` — scope it ONLY to the specific scroll areas that genuinely need rubber-band suppression (the grid body), not the body/all; prefer `overscroll-behavior: contain` over `none` where chaining should still work; (b) `NotionGrid` `onWheel` (L806): only intercept HORIZONTAL wheel when there IS horizontal overflow, and let vertical wheel bubble (never `preventDefault` vertical) so the page scrolls; (c) audit other `overflow-hidden`/`overscroll-none` containers that aren't meant to scroll. **VERIFY:** with the cursor/finger over the grid, a kanban, or any inner container, the page still scrolls vertically. **Commit:** `SCROLL-TRAP: scope overscroll-behavior + let vertical wheel bubble`.
- **👉 MOBILE-DRAG — tap-and-drag broken on mobile (kanban + all @dnd-kit drag) (Florin 2026-06-19).** ROOT: `KanbanView.tsx` L524-526 sensors = `PointerSensor` + `KeyboardSensor`, **no `TouchSensor`**, and draggables set `touch-action: 'pan-x pan-y'` (L287/421) → on touch the browser claims the gesture for panning, dnd-kit never starts. **FIX:** add `useSensor(TouchSensor, { activationConstraint: { delay: 200, tolerance: 8 } })` (long-press to drag → normal swipe/scroll still works); set the drag-handle `touch-action: none` (or rely on the delay sensor). Apply to ALL @dnd-kit drag surfaces (kanban now; reuse a shared sensors config so future drag surfaces inherit touch). **VERIFY:** on mobile, long-press a kanban card → drag it across columns; a normal swipe over the board still scrolls. **Commit:** `MOBILE-DRAG: add dnd-kit TouchSensor (long-press) for mobile drag`.
- **👉 FILES-FIND — the Files UI never migrated to Blob (Florin 2026-06-20: "transition happened programmatically, not in UI" — saved a project file + a quote-to-drive, NO trace in UI, but they ARE in Blob). CONFIRMED ROOT: DRIVE-OUT-2 migrated the STORE/actions to flat keyed Blob, but `FileManager.tsx` still wears the Drive folder paradigm AND reads only the global context.** Two confirmed problems: (1) **wrong context** — Files page renders `<FileManager />` with no context → `global` mode (L173) → lists only `t_{tenant}/global/`; the user's files are under `project/{id}` / `quotation/{id}` → invisible. (Save-to-drive DID write to Blob correctly — `ClientQuotationEngine` L575-576 — just unfindable.) (2) **dead Drive folder model** — FileManager still uses `currentFolderId`/`parentId`/folder icons/breadcrumbs/"navigate into folder" (L167/190/194), but Blob is FLAT/keyed with NO folders → it renders an empty *root* that looks like the old Drive. **FIX (UI reconciliation, not a tweak):** (a) global Files aggregates ALL tenant files — `listAllTenantFiles()` over `t_{tenant}/`, presented GROUPED BY CONTEXT (Project X / Quote Y / Receipt Z / record) — context grouping REPLACES the old folder hierarchy; (b) DROP the Drive-era folder/root navigation (`currentFolderId`/`parentId`/breadcrumbs/folder type) — Blob has no folders → flat, grouped, searchable file list; (c) each context detail (project/quote/record) shows its own files (right `contextType`/`contextId`). **VERIFY:** save a quote-to-drive + add a project file → both appear in the Files module (grouped by context) AND in the project/quote detail; no empty "drive root". **Commit:** `FILES-FIND: reconcile Files UI to flat Blob model, aggregate + group by context`.
- **👉 WORKSPACE-DB-GHOST — a garbage "New Workspace" database keeps auto-appearing (Florin 2026-06-20: "again a new workspace database").** A known problem: `api/admin/schema-cleanup/route.ts` L24 has `GARBAGE_NAMES = ['New Workspace','New Database']` + a cleanup route — but that only DELETES them, it doesn't STOP the creation. Something mints a default-named GlobalDatabase spuriously (a `createDatabase` with a default name — check callers on mount: `GlobalDatabaseSyncer`, `provisionLockedDatabases`, store `createDatabase` L333). **FIX:** find the auto-creation source and prevent it (don't auto-create empty/default-named DBs on load; guard `createDatabase` against minting "New Workspace"/"New Database"). Then the cleanup route is a one-time sweep, not a recurring need. **VERIFY:** reload the app repeatedly → no new "New Workspace"/"New Database" DB appears. **Commit:** `WORKSPACE-DB-GHOST: stop auto-creating garbage default databases`.
- **DRIVE-OUT-5 (follow-up — Planner found 2026-06-19): dangling `/api/drive` references after the route deletion.** DRIVE-OUT-4 deleted `src/app/api/drive/*` but 3 files still call it → runtime 404/error if invoked: `src/components/LeadForm.tsx`, `src/components/admin/settings/SettingsModule.tsx`, `src/lib/google-drive-oauth.ts`. **FIX:** for each, remove the dead `/api/drive` call (or repoint to the Blob seam if it's still a needed file action; `google-drive-oauth.ts` is likely dead → delete if unreferenced). **VERIFY:** `grep -rE "/api/drive" src/` is empty; LeadForm + Settings load without console errors. **Commit:** `DRIVE-OUT-5: remove dangling /api/drive references`.
- **CLEANUP-DUPES (hygiene, low priority — Planner found 2026-06-19).** 27+ files committed with a `" 2"` suffix (`button 2.tsx`, `calendar 2.tsx`, `dialog 2.tsx`, `m/page 2.tsx`, `api/financials/export 2/route.ts`, …) — these are macOS/cloud-sync conflict copies that got committed = dead cruft (bloats repo, risk of an accidental stale import). **FIX:** for each `" 2"` file, `grep` the codebase for any import referencing it; if NONE reference it (expected), DELETE all `" 2"` duplicates. Add `*\ 2.tsx`, `*\ 2.ts` to `.gitignore`. **VERIFY:** `git ls-files | grep ' 2\.'` is empty; build still green. **Commit:** `CLEANUP-DUPES: remove committed sync-conflict duplicate files`.
- **PROJ-FILTER-SAFETY (secondary hardening, lower priority).** In `NotionGrid` `filteredPages`: when an `activeView.filters` entry references a `propertyId` absent from `database.properties`, IGNORE it (don't zero the grid); show a "Filters active (N) — Clear" affordance when `filteredPages.length===0 && database.pages.length>0`. So a stuck/over-restrictive filter can never silently hide every row. **Commit:** `PROJ-FILTER-SAFETY: ignore dangling filters + clear-affordance on empty grid`.
- **LOCALE-AUDIT — comb the app for localisation consistency (Florin 2026-06-19: "isn't yet 100% accurate"). Workstream; subsumes CROSS-2.** Signals: ~104 untranslated `placeholder=` literals in admin components; leaked raw i18n keys (CROSS-2 `manualTicket`, the `invoice_legal` leak); hardcoded `/nl/` locale in `send-quote.ts`. **Three passes (separate commits):** (1) **LOCALE-AUDIT-1 — key parity:** ensure `messages/{nl,fr,en,ro}` have IDENTICAL key sets (a key missing in one locale renders the raw key); diff the 4 files, fill gaps. (2) **LOCALE-AUDIT-2 — hardcoded strings:** sweep `src/components` for user-facing literals not via `t()` (placeholders, labels, headings, toasts) → i18n keys; start with the ~104 placeholders. (3) **LOCALE-AUDIT-3 — leaked keys + formatting:** fix any raw-key render; hardcoded locales (`send-quote.ts` `/nl/` → document language); dates/numbers/currency per active locale (extends DATE-LOC). **VERIFY:** switch UI nl/fr/en/ro → no raw keys, no stray literals, locale-correct formatting. **Commits:** `LOCALE-AUDIT-1/2/3`.

**PI-FIX — Purchase-Invoice engine + inbound parser (Florin 2026-06-18, 8 bugs). VAT-relevant (inbound invoices still wrong). Files: `PurchaseInvoiceEngine.tsx`, `lib/e-invoice-inbox.ts` (parser), `api/peppol/inbox/route.ts`. Do PI-3 + PI-1 + PI-2 FIRST (worst data + the manual-fix safety net).**
- **PI-3 (ROOT-CAUSE, do first) — line prices/qty + header totals all parse to NaN.** `parseUBLToInvoice` config `ignoreAttributes:false` → UBL amount elements carry attributes (`<PriceAmount currencyID="EUR">12.50</PriceAmount>`, `InvoicedQuantity unitCode=…`) so fast-xml-parser yields `{'#text':'12.50','@_currencyID':'EUR'}`. Then `parseFloat(line.Price?.PriceAmount)`, `parseFloat(line.LineExtensionAmount)`, `parseFloat(line.InvoicedQuantity)`, `parseFloat(legalTotal.TaxExclusiveAmount/PayableAmount)`, `TaxAmount` ALL `parseFloat` an OBJECT → **NaN** (this is why prices AND totals are wrong even after FIX-C). **FIX:** add helper `const num = (v:any)=>{const s=(v&&typeof v==='object')?v['#text']:v; const n=parseFloat(String(s??'').replace(',', '.')); return isNaN(n)?0:n;}` and `const txt=(v:any)=>String((v&&typeof v==='object')?(v['#text']??''):(v??'')).trim();` Use `num()` for EVERY amount/qty/price/percent and `txt()` for names/descriptions. **Both render paths share this parser** — the stored `financial-row` blocks (built in the inbound route) AND the engine's "Legacy Peppol Line Items" table (fed live by `GET /api/peppol/inbox/[docId]` → `data.parsed`). So this one fix repairs both; the legacy path re-parses on open, so existing invoices show correct lines immediately (no backfill). **VERIFY (real case, Florin 2026-06-19): the BigMat Haren invoice (header €156.55/€32.88/€189.43 correct, but every line Price+Total = €NaN) — after fix, reopening shows real per-line prices/totals, no NaN.** **Commit:** `PI-3: UBL numeric/text extraction handles attributed elements`.
- **PI-1 — Download-PDF button 404.** `PurchaseInvoiceEngine.handleExportPDF` L215 `window.open(page.properties.receiptUrl)` opens the raw Blob KEY → 404. The iframe L510 already prefixes correctly. **FIX:** `const u = receiptUrl.startsWith('http') ? receiptUrl : '/api/files/'+receiptUrl; window.open(u,'_blank')`. Keep the regenerated-PDF fallback when no receiptUrl. **VERIFY:** PDF button opens the stored original, no 404. **Commit:** `PI-1: download opens served blob url not raw key`.
- **PI-2 — Peppol invoices not editable (the manual-fix safety net Florin needs).** L467 gates Edit/Save with `{!isPeppol && …}` → inbound invoices that arrive wrong CAN'T be corrected. **FIX:** remove the `!isPeppol` gate; AND expand editable fields beyond dates+totals to supplier name/VAT, object/betreft, OGM, contact, and line items (desc/qty/price/vat). `handleSaveEdit` persists all edited props + rebuilds the `financial-row` blocks from edited lines. **VERIFY:** open a Peppol invoice → Edit → change supplier/object/price/line → Save → persists + redisplays. **Commit:** `PI-2: allow manual edit of peppol invoices + all fields`.
- **🔴🔴 FIN-NUM — MAJOR: document numbering ignores the tenant profile (Florin 2026-06-19 "heywire, does not respect the set profile"). ROOT CAUSE FOUND.** TWO competing numbering systems: (1) `getNextDocumentNumber` (`actions/next-document-number.ts`) — CORRECT: reads the tenant profile (`{docType}Prefix/Connector/DateFormat/NumberWidth/NextNumber`), atomic, profile-respecting; used by CreateInvoice/QuotationButton. (2) the database **store's `createPage` HARDCODED auto-numbering** (`store.ts` L633-666 + the duplicate L818-854): quotations → `CEO-${year}-${seq}.00`, invoices → `FAC-${year}-${seq}` — these **ignore the profile entirely** and run a second counter that drifts. Any path that creates a db-invoices/db-quotations page WITHOUT a pre-set title falls into the hardcoded path — notably the quote→invoice conversion (`ClientQuotationEngine.handleConvertToInvoice`, comment "title is auto-numbered by store"). Result: some docs get `INV-2026-001` (profile), others `FAC-2026-001`/`CEO-2026-001.00` (hardcoded) → inconsistent, profile ignored. **FIX — single source of truth = `getNextDocumentNumber`:** (a) DELETE the hardcoded numbering blocks in `store.ts` (L633-666 + L818-854) — the store must NOT mint document numbers; (b) make the quote→invoice conversion `await getNextDocumentNumber('invoice')` and pass it as `title` before/at `createPage`; (c) AUDIT every `createPage(db-invoices…/db-quotations…)` caller — each must supply a `getNextDocumentNumber`-derived title (so nothing creates a numberless or hardcoded-numbered doc). Keep the OGM block (L670) untouched. **VERIFY:** set a custom profile (e.g. prefix `F`, format `YYYY`, width 4, connector `/`) → create an invoice via the button AND via quote→invoice conversion → BOTH produce `F/2026/0001`-style numbers matching the profile, sequential, no `FAC`/`CEO`. **Commit:** `FIN-NUM: single profile-respecting numbering source, remove store hardcoded numbers`.
- **POSTAL-SEARCH — postal-code/city search in the sales module (Florin 2026-06-19).** A searchable postcode↔city combobox in the sales client/address entry (`CreateClientModal` + the client/address fields in the quote/invoice engines): type a Belgian postal code (4 digits) OR a town name → filtered matches → selecting one auto-fills BOTH `postalCode` and `city`. Reuse `SearchableSelect`; back it with a small BUNDLED BE postcode→municipality dataset (static JSON, ~2.8k entries — no API key, works offline). (FR/NL/LU datasets later if needed.) **VERIFY:** type `1320` → "Beauvechain" suggested + fills city; type "Gent" → 9000 fills. **Commit:** `POSTAL-SEARCH: BE postcode/city autocomplete in sales address entry`.
- **👉 SELECT-PORTAL — selects still clipped by container "cutting masks" (Florin 2026-06-20: select elements not displaying fully, e.g. project dashboard). SYSTEMIC fix — fix the COMPONENT, not each container.** ROOT: `ui/SearchableSelect.tsx` renders its dropdown INLINE (`absolute z-[100] mt-1 w-full`, L101) → clipped by ANY `overflow-hidden`/scroll/`isolate` ancestor (e.g. project dashboard card `rounded-2xl … overflow-hidden relative isolate`, `page.tsx` L68; modal panes; engine cards). `SelectDropdown` already does this RIGHT — it portals (`createPortal` + `getBoundingClientRect` → `fixed`, L41/115). **FIX:** make `SearchableSelect` PORTAL its dropdown to `document.body`, `position: fixed` anchored to the trigger's bounding rect, with edge-aware flip (open upward if no room below), reposition on scroll/resize, close on outside-click — mirror `SelectDropdown`. Since SELECT-UNIFY put `SearchableSelect` in the engines, this one fix un-clips client/project/offerte AND the project dashboard AND the new MODAL-LAYOUT panes at once. Do NOT remove the containers' `overflow-hidden` (intentional for rounded cards/grid) — ESCAPE it via the portal. **VERIFY:** open a `SearchableSelect` inside the project dashboard card, an engine, and a modal pane → the full list shows, never clipped, at screen edges too. **Commit:** `SELECT-PORTAL: SearchableSelect renders dropdown via body portal`.
- **👉 SELECT-UNIFY — one searchable select component in the engines (Florin 2026-06-19: "three distinct selects for project/client/offerte → reduce to one component; replace the system select with our custom one; I really need the search field").** Canonical component ALREADY EXISTS and is the right choice: **`src/components/ui/SearchableSelect.tsx`** (has a search field) — and `ClientInvoiceEngine` already imports it (L33) + uses it for the offerte picker (L1074). But client + project still use native `<select>`/`<option>` (3 native selects in the invoice engine, 6 in the quote engine) → no search, inconsistent. **FIX:** replace ALL the native entity `<select>`s (client, project, offerte, and other entity/long-list pickers) in BOTH `ClientInvoiceEngine` and `ClientQuotationEngine` with `SearchableSelect` — same component for all three, each with the search field. Remove the native `<select>`/`<option>` from these engines. Keep onChange semantics + multi-select where it currently is (offerte). **VERIFY:** in both engines, the client / project / offerte pickers are the same custom dropdown with a working search field; no native OS select remains. **Commit:** `SELECT-UNIFY: client/project/offerte use SearchableSelect in both engines`.
  - **SELECT-UNIFY-2 (broader, later — Florin's "just one select component in the app"):** audit the app for other native `<select>` + duplicate select components (`ui/select`, `SelectDropdown`, `time-tracker/ui/select`) and converge on `SearchableSelect` as the single primitive. Post-launch workstream. **Commit:** `SELECT-UNIFY-2: app-wide single select primitive`.
- **👉 INV-DUEDATE — invoice payment-term selector + due-date (vervaldag) calc REGRESSED OUT (Florin 2026-06-19: "had 15/30 days to choose, now gone → document stops calculating the expiry date").** The QUOTATION engine still has the payment-terms selector (`ClientQuotationEngine` `paymentTerms` = `prop-payment-method` || 'pay-30', selector L939). The INVOICE engine (`ClientInvoiceEngine`) has NO payment-terms selector and NO due-date computation — it only reads a stored `dueDate`, which nothing sets → vervaldag stays empty. **FIX:** restore a betaaltermijn selector in the invoice header (15 / 30 / 60 dagen + ideally "einde maand"/net-X mirroring the existing `PaymentTerms` model `dueModel:'net'|'eom'|'fixed'|'receipt'` + `dueDays`), and compute `dueDate = invoiceDate + term` whenever the invoice date OR the term changes; persist both the chosen term and the computed `dueDate`. Mirror the quotation engine's selector. **VERIFY:** create an invoice, pick "30 dagen" → Vervaldag auto-fills to invoiceDate+30; change date or term → it recomputes; shows on the PDF. **Commit:** `INV-DUEDATE: restore payment-term selector + auto due-date calc`.
- **👉 PEPPOL-SEND-VALIDATE — outbound Peppol send fails "Failed to validate JSON document" (Florin 2026-06-19). HIGH — blocks sending sales invoices. Two parts:**
  - **Part 1 (diagnostic — do first, stop guessing):** `api/peppol/send/route.ts` surfaces only the generic top-level message. e-invoice.be returns the SPECIFIC Peppol BIS rule violations in the error body — capture the FULL response (`await res.json()`/text), `console.error` it server-side, and return the detail to the client so the exact failing rule(s) are visible. **Commit:** `PEPPOL-SEND-VALIDATE-1: surface detailed e-invoice validation errors`.
  - **Part 2 (likely root cause — customer country missing):** the payload sets `customer_address: client.address` (L186) as free text with **NO country code**, while the vendor gets `'Belgium'`/`supplierCountry:'BE'` (L181/249). Peppol BIS **BR-09** requires the Buyer postal address country. **FIX:** add `customer_country` (from `client.country` or default `'BE'`) to `invoicePayload`, and pass structured buyer street/city/postal/country (mirror the vendor). Re-test send. **Commit:** `PEPPOL-SEND-VALIDATE-2: add buyer country to peppol payload`.
  - **VERIFY:** send a B2B invoice to a Belgian client → succeeds (no "Failed to validate"). If it still fails, Part 1's surfaced detail names the next rule to fix.
  - **PEPPOL-SEND-VALIDATE-3 (UPDATE 2026-06-19: country fix worked for one client, another still fails → now CLIENT-DATA-specific). Florin wants to TEST sendouts before actually sending.** Build a pre-send "Test / Valideer" path (the send route ALREADY has the pieces: `POST /api/validate/json` → `{is_valid, issues[]}` at L335, with good issue-parsing at L356-384). Three parts:
    - **3a — extract a shared payload builder.** Move the `invoicePayload` construction + `flattenBlocksToLineItems` + `customer_country` etc. out of `api/peppol/send/route.ts` into `lib/peppol-payload.ts` (`buildPeppolPayload(...)`), so the test path and the real send build the **IDENTICAL** payload (otherwise "test passes but send fails"). Pure refactor, no behaviour change. **Commit:** `PEPPOL-SEND-VALIDATE-3a: extract shared buildPeppolPayload`.
    - **3b — validate-only endpoint + "Test verzending" button.** `POST /api/peppol/validate` builds the payload via 3a + calls ONLY `/api/validate/json` (no create/send) → returns `{is_valid, issues}`. Add a "Test / Valideer verzending" button in the invoice engine (beside the Peppol send) that calls it and shows ✓ "Geldig — klaar om te versturen" OR the specific Peppol BIS issues list — WITHOUT dispatching. **Commit:** `PEPPOL-SEND-VALIDATE-3b: pre-send validate endpoint + Test button`.
    - **3c — instant local pre-flight (no API round-trip).** Before the API call, check required fields and show a friendly Dutch message naming what's missing: client name, client VAT (BR-CO-09 format `BE`+digits if B2B), client country, postal/city/street, ≥1 line with amount>0, tenant VAT + IBAN. **Commit:** `PEPPOL-SEND-VALIDATE-3c: local pre-flight required-field checks`.
    - **VERIFY:** on the failing client, click Test → it names the exact missing/invalid field (likely VAT) without sending; fix the field → Test passes → real send succeeds.
- **👉 PI-2-FIX — refinements to the shipped PI-2 editing (Florin 2026-06-19 tested it: "great", 3 issues). HIGH.**
  - **PI-2-FIX-A — value-read NaN (CONFIRMED bug).** The read-only "Legacy Peppol Line Items" table renders `line.price` (`PurchaseInvoiceEngine.tsx` L540) and `line.totalExVat` (L542), but the data fields are `unitPrice` / `lineTotal` (the `/api/peppol/inbox/[id]` route returns `unitPrice`/`lineTotal`; the engine's editData maps to those names) → `Number(undefined)` = **€NaN**. **FIX:** L540 → `line.unitPrice ?? line.price`, L542 → `line.lineTotal ?? line.totalExVat` (defensive both names). **VERIFY:** the read-only line table shows real € values, no NaN. **Commit:** `PI-2-FIX-A: read-only line table reads unitPrice/lineTotal`.
  - **PI-2-FIX-B — header totals auto-calculate from lines.** Lines each recompute `lineTotal = qty × unitPrice` (L479/486), but the header totals (`editData.totalExVat/totalVat/totalIncVat`) are separate manual fields that never re-sum → "lost auto-calculate." **FIX:** when any line changes (and on load when lines exist), derive: `totalExVat = Σ lineTotal`, `totalVat = Σ(lineTotal × vatRate/100)`, `totalIncVat = totalExVat + totalVat`; show them as auto-computed (allow manual override only when there are NO line items). **VERIFY:** edit a line qty/price → header Excl/VAT/Incl update live and match the sum. **Commit:** `PI-2-FIX-B: header totals auto-derive from line items`.
  - **PI-2-FIX-C — attach a PDF when missing.** The right-pane "No original document attached" state (~L515) has no upload. **FIX:** add an "Upload document" button there (and in BIJLAGEN) → `uploadFileAction(fd, 'purchase-invoice', pageId)` → set `receiptUrl = result.key` on the record → the iframe then renders it. (This is PI-RICH-E pulled forward.) **VERIFY:** open an invoice with no document → upload a PDF → it appears in the viewer + persists. **Commit:** `PI-2-FIX-C: upload original document when missing`.
- **PI-9 — purchase credit note not recognised.** `peppol/inbox/route.ts` stores all docs as invoices; the inbox item carries `doc.document_type ∈ {INVOICE,CREDIT_NOTE,DEBIT_NOTE}` (and the UBL root is `CreditNote`). **FIX:** when `document_type==='CREDIT_NOTE'` (or parsed root CreditNote) set a `docType:'credit-note'` property + store totals NEGATIVE for the purchase ledger; engine shows a CREDIT NOTE badge. **VERIFY:** an inbound credit note displays as a credit note (badge + negative), not a normal invoice. **Commit:** `PI-9: recognise + flag inbound purchase credit notes`.
- **PI-4/5/6/7 — missing fields (extract in parser → store → display in engine).** Add to `ParsedPurchaseInvoice` + the inbound mapping + the engine left pane: **(4) Object/Betreft** = `txt(doc.Note) || txt(doc.BuyerReference)` → store `betreft`, show "Object". **(5) Supplier contact** = `cac:AccountingSupplierParty/cac:Party/cac:Contact` {Name, Telephone, ElectronicMail} → store on the record + write email/phone onto the auto-created db-suppliers page; show a Contact block. **(6) Full supplier details** = show structured street/postal/city/country (already parsed — keep structured, don't only concatenate) + VAT + contact. **(7) OGM** = `cac:PaymentMeans/cbc:PaymentID` (fallback `cbc:InstructionID`) → store `ogm`, show "OGM / Gestructureerde mededeling". **VERIFY:** object, contact, full supplier, and OGM all populate for a Peppol invoice that carries them. **Commit:** `PI-4-7: extract+display object, contact, full supplier, OGM`.
- **PI-RICH — bring the PurchaseInvoiceEngine to Billit-level richness (Florin 2026-06-19: "insufficient treatment… a lot of work happens in the details pane"). CHECKED: not uncommitted (no stashes; these fields absent from code) → BUILD. Current engine has supplier/VAT/dates/totals/lines/PDF/status/mark-paid; everything below is MISSING. Build on PI-2 (editable) + PI-3 (line NaN) + PI-4/PI-7 (betreft/OGM) + PI-8 (pan). High priority, AFTER the PI-FIX bug items. Stay on it until Billit-parity.**
  - **PI-RICH-A — header meta fields (display + editable):** Leveringsdatum (delivery date), Onze referentie / our order ref (e.g. "CDE: 401902"), Munteenheid (currency), Btw-regime label (Binnenland / Medecontractant / Intracom), Te betalen (amount due = total − paid). Plus full supplier block (name + address + IBAN + BIC, from PI-5/6). **Commit:** `PI-RICH-A: invoice meta fields (delivery, our-ref, currency, regime, amount-due)`.
  - **PI-RICH-B — ERP connections in the details pane:** show + EDIT the Project relation here even though it's in the table ("a lot of work happens in the details pane"); render as a relation picker linking to db-1 projects; plus any other relevant relations. **Commit:** `PI-RICH-B: project + ERP relations editable in invoice detail`.
  - **PI-RICH-C — accounting / categorization (for the accountant):** Categorie (expense category, e.g. "Aankopen goederen", editable, a select) + Grootboekrekening (general-ledger account) at header AND per-line (the ORDERLIJNEN "Rekening" column). These must be EXPORTED in ACCT-EXPORT (purchase journal) so the accountant gets them. **Commit:** `PI-RICH-C: expense category + ledger account (header + per-line)`.
  - **PI-RICH-D — internal + payment status (Ponto-ready):** Opmerkingen (internal notes, a basic multiline text field — NOT shared with supplier/accountant); Betaald toggle + Betaaldatum (paid date); Betaalwijze (payment method select: overschrijving/SEPA, kaart, cash, domiciliëring…). These prep bank-reconciliation for the future Ponto integration. **Commit:** `PI-RICH-D: internal notes + paid status/date + payment method`.
  - **PI-RICH-E — BIJLAGEN (attachments) section:** list attached files incl. the source UBL (`Bron UBL`) + uploaded docs, with download + an "Upload" button (reuse `uploadFileAction('purchase-invoice', id)`). **Commit:** `PI-RICH-E: attachments panel (source UBL + uploads)`.
  - **VERIFY (whole PI-RICH):** open a Peppol purchase invoice → it shows ALL of: meta (delivery/our-ref/currency/regime/amount-due), linked Project (editable), Categorie + Grootboekrekening, internal notes, Betaald+date+method, ORDERLIJNEN with real numbers (PI-3) + per-line ledger, BIJLAGEN with the UBL, and a pannable zoomed PDF (PI-8) — i.e. Billit-parity. All editable + persisted (PI-2).
- **PI-8 (lowest priority) — PDF viewer no pan/zoom.** Right-pane `<iframe>` (L509) gives only the native viewer. Add app zoom + drag-to-pan: wrap the document in a zoomable/scrollable container with +/− and grab-to-pan (or render via pdf.js with zoom/pan). **VERIFY:** zoom in → drag to pan around the page. **Commit:** `PI-8: zoom + drag-pan in document viewer`.

**GRID-13 — NotionGrid coordinate-space desync (all DBs).**
- **GRID-13a (CONFIRMED):** custom header (L785, `headerRowHeight={0}`) has a 123px leading gutter (L789: 43+40+40); the orange `col-drop-indicator` X + `colDropTarget` hit-test (L816-818) don't offset by it in the `translateX(-scrollLeft)` space (L756) → ~one gutter-width right. FIX: compute indicator X + drop-target from pointer X − 123px gutter (+ scrollLeft). VERIFY: drag column → line on real boundary.
- **GRID-13b (mechanism confirmed; MEASURE px at runtime, do NOT guess):** custom header overlays top 36px, `headerRowHeight={0}`, `height=wrapperH−36` (L203-207,L988-989). click1 selects → click2 focuses DSG hidden input → `scrollIntoView` jumps because input Y is offset. INSTRUCTION: reproduce, log computed cell Y/scrollTop vs bounding rect, then align DSG coord origin or suppress the focus jump. VERIFY: 1 click select, 2nd edits in place, no jump; 2+ DBs. **Commits:** `GRID-13a: column drop indicator accounts for gutter offset` · `GRID-13b: fix cell click/edit row-jump (DSG coord alignment)`.

**DRIVE-OUT-1 — QUICK WIN: stop auto-folder creation** (Drive holds NO critical files; spawns empty folders on every record open). Remove the 3 `initializeContextFolder` effects: `ProjectDetailView.tsx` L377, `RecordDetailPage.tsx` L78, `PageModal.tsx` L496 (+ dead `boundDriveId`/`updatePageDriveId` wiring). Blob is folderless. Don't touch the FileManager store (DRIVE-OUT-2). VERIFY: open records repeatedly → ZERO new folders, no errors. **Commit:** `DRIVE-OUT-1: stop auto-creating drive context folders`. (DRIVE-OUT-2/3/4 = workstream below AGGREGATE PLAN.)

**SEND-6 — FEATURE (lower priority): quote compose modal.** Today `ClientQuotationEngine.tsx` L492 sends with `customMessage=undefined`. **6a** `QuoteSendModal.tsx`: editable To + Subject + Body (pre-fill `t('email_quote_body',lang)` with `{clientName}/{projectName}/{total}/{company}` resolved, one-off) + attachment checkboxes from `listRecordFiles('project',projectId)`+`('quotation',quoteId)`; quote PDF always attached; on confirm pass `{to,subject,body,attachmentKeys}`. **6b** extend `sendQuotationToClient`: add `subjectOverride?`, `attachmentKeys?[]`; assert each key `t_${sessionTenantId}/` prefix, fetch from Blob, attach. **Commits:** `SEND-6a: quote compose modal (editable body + project-file attachments)` · `SEND-6b: send-quote accepts edited subject + selected attachments`.

**🔥 VAT-URGENT (Florin: trimester VAT next month; manual + PDF capture + inbound mapping must work). Strategy (Florin 2026-06-17): configure BLOB first, then storage-dependent fixes write to Blob — do NOT patch onto the dying Drive path.**
**GROUP 1 — storage-independent, do NOW (no Blob needed):**
- **SCAN-5.1** — `TicketCaptureModal.tsx` L501: `accept` → `'image/*,application/pdf'` in BOTH modes. **Commit:** `SCAN-5.1: receipts accept PDF`.
- **SCAN-1** — `TicketCaptureModal.handleSave` uses hardcoded `'db-expenses'`/`'db-tickets'` (L261/274); use the resolved `targetDatabaseId`. **Commit:** `SCAN-1: manual save uses resolved db id`.
- **PEPPOL-4-FIX-A** — inbound supplier "untitled": auto-created in the `Supplier` TABLE but the relation targets the **`db-suppliers` GlobalDatabase**. Match + auto-create the supplier as a **`db-suppliers` GlobalPage** (`getLockedDbId('db-suppliers')`+`createPage`) and link to that page id. **Commit:** `PEPPOL-4-FIX-A: inbound supplier as db-suppliers page`.
- **SCAN-5.2** — `api/scan/route.ts` PDF parse throws on ALL PDFs (`getDocument` L78, `workerSrc=''`). MEASURE the real error, then switch to a serverless extractor (`unpdf`). **Commit:** `SCAN-5.2: serverless PDF parsing (unpdf)`. (SKIP if you can't add a dep/test in-window.)
**GROUP 2 — BLOB FOUNDATION (do before Group 3 — the store is already created PRIVATE + token provisioned):**
- **FILES-ISO + FILES-0** — stand up the private Blob storage seam: `StorageProvider` + `BlobStorageProvider (access:'private')` + the authenticated `GET /api/files/[...key]` serving route + key scheme (full spec in FILES section; this is just the seam, NOT the Drive→Blob data migration). **Commit(s):** `FILES-ISO: private blob serving route` / `FILES-0: storage provider seam`. (Tenant-isolation is security-relevant — follow FILES-ISO exactly.)
**GROUP 2 STATUS: ✅ done + Planner-verified (isolation solid: private store, session-tenant + trailing-slash prefix assert + 403 + unit test). ONE follow-up before Group 3 displays docs:**
- **FILES-0b** — the serving route `GET /api/files/[...key]` does NOT set `Content-Type` on the streamed response (only Content-Disposition + Cache-Control) → browsers may not render PDFs/images inline (needed for the document viewer). Set `Content-Type` from the blob's `contentType` (Vercel `get()` returns it) or infer from the key extension, in BOTH the body-stream branch and the downloadUrl fallback. **Commit:** `FILES-0b: serving route sets Content-Type for inline render`.

**GROUP 3 — ✅ DONE + Planner-verified 2026-06-17 (do NOT redo): FILES-0b (Content-Type set), PEPPOL-4-FIX-B (inbound PDF→Blob, Drive gate removed), SCAN-5.3+SCAN-4 (manual/scan attach via `uploadFileAction`→Blob), backfill route built. Awaiting Florin verify (inbound original viewable; manual receipt+PDF stored). PEPPOL-5 backfill = Florin runs `/api/admin/backfill-peppol` (dry-run→execute) to re-hydrate already-received invoices. Original specs below for reference:**
- **PEPPOL-4-FIX-B** — store the inbound original document **to Blob** via the seam (NOT the Drive-gated path at L282 which silently skips when `driveFolderId` is null). **Commit:** `PEPPOL-4-FIX-B: store inbound original to blob`.
- **SCAN-5.3** — manual ("enter manually") entry attaches the original photo/PDF, stored **to Blob** on the record (legal requirement). **Commit:** `SCAN-5.3: manual entry attaches source document`.
- **SCAN-4** — store the ORIGINAL receipt file to Blob (display depends on it). **Commit:** `SCAN-4: store original receipt to blob`.
**AFTER the VAT/Blob cluster above (Florin 2026-06-17):**
- **GRID-10-FIX** — 🩹 projects STILL open in the generic modal; the advanced `ProjectDetailView` is never reached (Florin 2026-06-17, still broken). MECHANISM ALREADY EXISTS — this is the EXACT MIRROR of AGG-1 (which the coder already did for the expenses page), applied to projects. In `src/app/[locale]/admin/projects-management/page.tsx`: add `selectedProjectId` state; pass `onOpenRecord={(id) => setSelectedProjectId(id)}` to `<DatabaseCloneDynamic databaseId="db-projects" …>`; render `<ProjectDetailView databaseId={resolveDbId('db-projects')} pageId={selectedProjectId} locale={locale} onClose={() => setSelectedProjectId(null)} />` (dynamic import, ssr:false) when set — exactly as the expenses page renders `PurchaseInvoiceEngine` via `selectedInvoiceId`. Add an `onClose` prop to `ProjectDetailView` if it lacks one. Do NOT route projects through the generic `PageModal`. **VERIFY:** click a project row → the advanced ProjectDetailView (timeline/tasks/folders/P&L split view) opens, NOT the generic modal. **Commit:** `GRID-10: projects open ProjectDetailView via onOpenRecord (mirror AGG-1)`.
- **SEND-5** — 🩹 the quote magic link bounces the CLIENT into the CoralOS login instead of the public signing page. ROOT CAUSE (`src/middleware.ts`): it virtualizes any non-`/admin` path to `/admin/*` (L206-208) → `/quote/[id]` is treated as PROTECTED → unauthenticated client redirected to login. The public viewers (`/quote/[id]` signing page, `/invoice/[id]` viewer) must be PUBLIC. **FIX:** add `pathname.includes('/quote/') || pathname.includes('/invoice/')` to the `isPublicPage` checks in BOTH middleware branches (~L145 and ~L203). (`/invoice/` singular won't match the admin `/invoices/` plural.) **VERIFY:** open the emailed magic link in a LOGGED-OUT / incognito browser → the public quote viewer loads and the client can sign — NO login redirect. (Minor aside: the link locale is hardcoded `/nl/` in `send-quote.ts` L33 — ideally use the document language; not required for this fix.) **Commit:** `SEND-5: public quote/invoice viewer routes bypass auth`.

1. **FIN-7b-APPLY** — schema already has `@@unique([tenantId, quoteNumber])` on `Quotation` but the DB constraint isn't live. **This project runs on `db push`, NOT migrations** (migration history abandoned at `init_cms`; the DB evolved via db push → `migrate dev` wrongly wants to RESET = data loss; coder correctly skipped). **RUN `npx prisma db push`** — the change is a LOOSENING constraint (global `@unique` → per-tenant composite) → non-destructive, NO `--accept-data-loss` needed. Then `npx prisma generate`. **GUARDRAIL:** if `db push` reports it would DROP/ALTER anything OTHER than the `Quotation_quoteNumber_key` index, STOP and report — do not force. **Verify:** db push applies the one index change; `tsc` green. **Commit:** `FIN-7b: apply per-tenant quote unique via db push`.
2. **SEND-3** — `send-quote.ts` + `send-invoice.ts`: add `bcc:[tenant.email]` + `replyTo: tenant.email` to `resend.emails.send` (source tenant email like `companyName` already is; skip if empty; don't change `from`). **Commit:** `SEND-3: bcc + reply-to tenant on sent docs`.
3. **FIN-9** — `QuotationPDFTemplate.tsx` + `InvoicePDFTemplate.tsx`, ALL variants: legal group immediately BEFORE totals; wrap legal group AND calc block each in `<View wrap={false}>`. **Commit:** `FIN-9: legal above totals, no page-split`.
4. **GRID-9** — `RecordDetailPage.tsx`+`PageModal.tsx`+`ProjectDetailView.tsx`: values ≥14px, labels 13px, headings 16-18px; remove every `text-[9/10/11px]`; labels `text-neutral-600 dark:text-neutral-300`, values `text-neutral-900 dark:text-neutral-100`. **Commit:** `GRID-9: detail-view legibility scale`.
5. **GRID-2** — name-property "open" button is dead when the cell is selected; make it work whether or not the cell is selected (stop the selection layer from eating the click). **Commit:** `GRID-2: open button works when cell selected`.
6. **GRID-4** — remove the elastic/rubber-band overscroll at grid edges; stop hard at the edge. **Commit:** `GRID-4: remove edge rubber-band`.
7. **GRID-5** — flyouts/dropdowns clip at screen edges; add edge-aware positioning (measure viewport, flip when overflowing) in the shared dropdown/flyout component. **Commit:** `GRID-5: edge-aware flyout positioning`.
8. **GRID-3** — column resize (FULL spec in GRID section): live drag in CSS, commit width once on release, NO key-remount. **Commit:** `GRID-3: live CSS resize, persist on release`.
9. **PROFILE-3-STATIONERY** — the stationery-mode header variant doesn't render the tenant company block; render companyName/VAT/IBAN/email/address there too (the widened select already provides the data). **Commit:** `PROFILE-3: tenant block in stationery header`.
10. **CROSS-2** — untranslated i18n keys leak to UI (e.g. `Admin.nav.pages.manualTicket`); add the missing keys to `messages/{nl,fr,en,ro}.json`. **Commit:** `CROSS-2: add missing i18n keys`.
11. **PROJ-8** — Kanban card click doesn't open the project; on click open a flyout/panel showing the project's properties, editable + savable (Notion card behavior). File: `KanbanView.tsx`. **Commit:** `PROJ-8: kanban card opens editable property flyout`.
12. **PROJ-9** — Kanban card three-dot menu does nothing; wire its actions (open/duplicate/delete as appropriate). **Commit:** `PROJ-9: fix kanban card three-dot menu`.
13. **PROJ-10** — Kanban cards: **PER-USER configurable card properties + inline edit** (Florin decided 2026-06-17). (a) a PER-USER setting (stored per user — user prefs / localStorage keyed by `userId`+`viewId`) for WHICH properties appear on cards; (b) those values are **editable INLINE on the card** (e.g. click status → change). Both. File: `KanbanView.tsx` + a small per-user pref store. **Commit:** `PROJ-10: per-user kanban card properties + inline edit`.
14. **CROSS-1** — replace the black crash screen with a themed error screen. **Commit:** `CROSS-1: themed crash screen`.
15. **GRID-9b-a** — inline `@` DATE insert (NO reminder yet, NO NOTIF dependency): typing `@` opens a date picker (today / tomorrow / calendar) and inserts a formatted date inline; build a reusable `@`-date component and mount it across text inputs/cells (Florin: "everywhere text is entered"). **Use the APP's in-app date-picker / calendar component (reuse the existing `components/ui/calendar` or the grid `DateColumn` picker) — NOT the native/system `<input type="date">` picker.** NO notification wiring in this item. **Commit:** `GRID-9b-a: inline @ date insert`.
16. **NOTIF-0** — expose `session.user.id` in the auth session callback (full spec in NOTIF section). **Commit:** `NOTIF-0: expose session.user.id`.
17. **NOTIF-1** — `Notification` Prisma model + migration + `lib/notifications.ts` + the 2 tenant-guarded API routes (full spec NOTIF section). **Commit:** `NOTIF-1: notification model + API`.
18. **NOTIF-2** — event emitters (quote-accepted / invoice paid·sent·overdue / credit-note), `userId=null`, idempotent (full spec NOTIF section). **Commit:** `NOTIF-2: notification emitters`.
19. **NOTIF-3** — persistent toast on app-open + 30s poll/focus; cleared only by opening the item (full spec NOTIF section). **Commit:** `NOTIF-3: persistent notification toast`.
20. **NOTIF-4** — bell menu + history + unread badge (full spec NOTIF section). **Commit:** `NOTIF-4: bell menu + history`.
21. **GRID-9b-b** — wire `@` REMINDER → NOTIF: an `@`-reminder date creates a notification that fires at/near that date via the NOTIF system (built in 16-20). **Commit:** `GRID-9b-b: @ reminder fires via NOTIF`.

## 🏗️ PROJ-COCKPIT — Project details view redesign (design target from Florin 2026-06-20, "BuildOps Project cockpit" reference). SUPERVISED, post-launch — this is the Pro/Enterprise project module's flagship view, NOT free-tier-launch-critical. Phased; builds on the existing tab-based `ProjectDetailView` (1526 lines: overview/tasks/journal/files/vorderingen; has tasks via `prop-task-project`, task %, `prop-budget`, start/end dates, status, client).
**TARGET layout (single-page cockpit):** left rail (active project + status "At risk · N days behind" + Progress/Spent % + Milestones tree) · top KPI row (Overall progress · Schedule variance days · Budget spent % · Cost performance CPI · Open change orders · Safety days-clear) · Master schedule gantt/critical-path (phases across quarters + TODAY marker + "N days behind") · Budget-vs-actual bar chart per category (+ forecast/variance/margin) · Change-orders list · Clocked-hours weekly chart (this week / on-site-now / approved-pending-trades) · bottom module cards (Schedule · Procurement · Documents · Timesheets · Invoicing, with counts).
- **PROJ-COCKPIT-1 — cockpit shell + cards from EXISTING data.** Redesign the Overview into the cockpit grid. KPI cards from data we have: Overall progress = task done/total %; Budget spent % = `prop-budget` vs sum of project-linked expenses/invoices (via GRID-11 rollups); Schedule variance = planned `prop-end-date` vs projected (from task progress/dates). Module cards link to existing sub-surfaces: Documents=files, Invoicing=vorderingen/linked invoices, Schedule=tasks. Left rail = milestones/tasks tree (exists). **Commit(s):** per card/section.
- **PROJ-COCKPIT-2 — Master schedule gantt + Budget-vs-actual chart.** Gantt/critical-path from project tasks+milestones with dates (reuse `planning/ProjectTimelineView`) + TODAY marker + behind-badge. Budget-vs-actual per category bar chart (needs a per-category budget breakdown + actual from linked expenses; Chart.js). **Depends on:** GRID-11 financial rollups (budget actual).
- **PROJ-COCKPIT-3 — NEW concepts (data models needed).** Change-orders (new records linked to the project: id, desc, €, status) + list card. Clocked-hours card (**depends on TT** time-tracker consolidation — hours per project). CPI (earned-value: needs % complete × budget vs actual) + Safety days-clear (a project safety/incident field). These are net-new data — spec each when prioritised. **Supervised, Florin gates each.**

## 📊 DASH-REPORTS — Dashboard → advanced Report Center (BACKLOG / vision, Florin 2026-06-20). SUPERVISED, post-launch.
**Vision:** retire the current dashboard's overlooked "presentation" tiles and rebuild it as a **report center** — everything synthesised, filtered, and CROSS-REFERENCED across modules into fully personalised reports, alongside a host of standard templates. Leverages GRID-11 rollups/formulas/relations + the financial data (ACCT-EXPORT journals).
- **DASH-REPORTS-1 — standard report templates (prebuilt, filterable by period/client/project):** revenue, expenses, VAT summary (per rate / medecontractant), P&L, project profitability, aged receivables/payables, time→billing, cash-flow. Each = table + chart, period picker.
- **DASH-REPORTS-2 — report BUILDER (personalised):** pick data source(s) across modules → filters → group/aggregate → CROSS-REFERENCE via relations/rollups → visualise (table/chart) → SAVE as a named personal report. (Could render via the live-artifact mechanism.)
- **DASH-REPORTS-3 — delivery:** export (PDF/CSV/xlsx), scheduled email digests (ties to scheduled tasks), sharing. **Depends on:** GRID-11 (rollups/cross-ref), ACCT-EXPORT (financial data). Florin gates each phase.

**── SUPERVISED LINE (do NOT start unattended — need Florin): FIN-10 (duplicate detection), full GRID-10 consolidation/retire RecordDetailPage, QUOTE-6c/6d/6e, GRID-11, TT-*, MOBILE-*, PEPPOL-5 (inbound backfill), DB-BASELINE (baseline Prisma migrations: the project runs on `db push` with an abandoned migration history at `init_cms` — introduce a proper migration baseline matching the live DB so `migrate dev` works + there's an audit trail; careful op, NOT under deadline). ──**

## 🗄️ DRIVE-OUT — Google Drive → Vercel Blob cutover (Florin 2026-06-18)
**KEY FACT:** Drive holds NO critical data (empty auto-folders, no real attachments, all docs re-exportable) → **NOT a data migration, a pure CODE cutover.** Blob is keyed/folderless → `initializeContextFolder`/`driveFolderId` are DELETED, not ported. Foundation + new-writes already live.
**⚠️ HARD CAVEAT:** `googleapis` is ALSO used by Calendar (`/api/calendar/*`) + Gmail (`/api/email/connect/google`). **Do NOT remove `googleapis`; do NOT touch calendar/email OAuth.** Only Drive-FILE code/routes go.
**DECISION (Florin):** `createGoogleFile` (in-app Google Doc/Sheet) is REMOVED — can't exist without Drive. Reversible later.
**MEASURED FACTS:** store `file-manager/store.ts` (Drive ops → `/api/drive*`); `FileNode` uses `{id,name,type,mimeType?,size?,url?}`; `uploadFileAction(fd,recordType,recordId?)` already Blob; `storage.list/delete/get`; `driveFolderId` = nullable col on 5 schema models (L21,136,165,469,682 — leave, drop under DB-BASELINE).
- **DRIVE-OUT-2 — store + actions on Blob.** ADD to `actions/files.ts`: `listRecordFiles(recordType,recordId)` = `storage.list('t_{sessionTenant}/{recordType}/{recordId}/')` mapped to `{id:key,key,name:basename,type:'file',size,url}`; `deleteFileAction(key)` asserting `t_{sessionTenant}/` prefix then `storage.delete`. Rewrite store: `fetchNodes`→`listRecordFiles`; `uploadFile`→`uploadFileAction` then refetch; `deleteNode`→`deleteFileAction`. DELETE `createFolder`/`createGoogleFile`/`initializeContextFolder`. `FileManager.tsx`: remove New-Folder + New-Google-Doc buttons; keep upload/list/delete + `node.url`. VERIFY: list/upload/delete via Blob; no `/api/drive` calls. **Commit:** `DRIVE-OUT-2: file manager store + actions on blob seam`.
- **DRIVE-OUT-3 — viewers off Drive.** `QuotationViewer`, `InvoiceViewer`, `ClientInvoiceEngine`, `ClientQuotationEngine`: Drive asset URLs → `/api/files/[...key]`. VERIFY: quote + invoice (incl. Peppol) render docs from Blob. **Commit:** `DRIVE-OUT-3: viewers serve assets from blob`.
- **DRIVE-OUT-4 — retire Drive-FILES (KEEP googleapis).** Pre-check `grep -rE "/api/drive|initializeContextFolder|createGoogleFile" src/` clean; DELETE `src/app/api/drive/*`; remove residual `driveFolderId` reads/writes (`store.ts`, `global-databases.ts`, `types.ts`, update-page-drive-id) — SCHEMA COLUMNS STAY. KEEP `googleapis`; don't touch calendar/email. VERIFY (hard gate): `next build` green; no `/api/drive` refs; Calendar + Gmail connect still work. **Commit:** `DRIVE-OUT-4: remove drive-files routes + driveFolderId wiring`.

## 🎯 AGGREGATE PLAN — Purchase-Invoice Detail View + Inbound Mapping (SINGLE SPEC — follow exactly, no interpretation)
**This supersedes the purchase-invoice parts of GRID-10 + PEPPOL-4. One coherent result. Reference = Florin's 2 screenshots: image 1 = the WRONG generic modal (sparse, useless); image 2 = the CORRECT full split view (BigMat).**
**TARGET RESULT:** Opening ANY purchase invoice (manual, scanned, or Peppol-inbound) shows the FULL `PurchaseInvoiceEngine` split view — LEFT: all properties + **ORDERLIJNEN** (line-item table) + **BIJLAGEN** (attachments incl. source doc); RIGHT: the **original document rendered** (PDF). The generic `PageModal` is NEVER used for `db-expenses`. Inbound invoices arrive with supplier linked, line items structured, and the original document stored.

**MEASURED FACTS (don't re-investigate):**
- `expenses/invoices/page.tsx` renders `<DatabaseClone databaseId="db-expenses">` (grid) AND has `<PurchaseInvoiceEngine pageId onClose>` (L232) — but the engine opens ONLY via `selectedInvoiceId`, which is set ONLY by `handleNewManual` (new manual). **Existing/inbound row-clicks fall through to DatabaseClone's internal `PageModal` (the wrong sparse modal).**
- `PurchaseInvoiceEngine.tsx` = the correct split view (image 2). It exists and works for new-manual.
- Inbound mapping in `api/peppol/inbox/route.ts` `globalPage.create` (L264-289): `supplier` relation empty unless VAT pre-matched; lines stored as `invoiceLines: JSON.stringify(...)` + first line in `betreft`; NO original document stored (`blocks:[]`, only `peppolDocId`).
- `PurchaseInvoicePDF.tsx` = the regenerated stub ("Generated by CoralOS / No line item detail") — the download Florin got. Not the original.

**EXECUTE — 4 parts, each its own commit:**
1. **AGG-1 — Route row-clicks to PurchaseInvoiceEngine (kills the generic modal for expenses).** Add an optional `onOpenRecord?: (pageId: string) => void` prop to `DatabaseClone`; when provided, a row "open" calls it INSTEAD of opening the internal `PageModal` (leave PageModal behavior for DBs that don't pass the prop — don't break others). In `expenses/invoices/page.tsx`, pass `onOpenRecord={(id) => setSelectedInvoiceId(id)}`. Result: clicking any purchase-invoice row opens `PurchaseInvoiceEngine`, never the generic modal. **Verify:** open an existing AND an inbound invoice → both show the split view, not the sparse modal.
2. **AGG-2 — Inbound mapping (`api/peppol/inbox/route.ts`).** (a) Supplier: when `matchSupplier(vendorVat)` finds nothing, **auto-create the supplier contact** from `vendor_name`/`vendor_tax_id`/`vendor_address` and link it (idempotent on VAT — no dupes). (b) Line items: store as the SAME structured line shape `PurchaseInvoiceEngine`'s ORDERLIJNEN reads (not a JSON string; stop dumping line text into `betreft`). (c) Original document: fetch via `getInboxDocument(apiKey, doc.id)` and STORE it on the record as an attachment (Blob/`BIJLAGEN`). **Verify:** inbound from a NEW supplier → supplier linked (FROM filled), ORDERLIJNEN populated, source doc in BIJLAGEN.
3. **AGG-3 — PurchaseInvoiceEngine renders all three regions for ALL sources.** Confirm/ensure: LEFT = properties + ORDERLIJNEN (reads the structured lines from AGG-2) + BIJLAGEN; RIGHT = original document rendered (from the stored attachment). Must work for inbound, scanned, and manual. **Verify:** image-2 layout for an inbound invoice — line items + original PDF both visible.
4. **AGG-4 — Document action serves the ORIGINAL.** The PDF/view/download action serves the STORED original document; only fall back to the regenerated `PurchaseInvoicePDF` stub when NO original exists (and don't present the stub as the supplier's invoice). **Verify:** download on an inbound invoice = the supplier's actual document, not a CoralOS stub.

**FINAL ACCEPTANCE (all true):** inbound Peppol invoice opens in the split view (NOT the generic modal); supplier shown + linked; ORDERLIJNEN populated; original document on the right + downloadable as source; generic `PageModal` never appears for `db-expenses`. **DESIGN NOTE (Florin): dislikes intrusive centered modals — the engine is a full/large split surface, not a cramped centered modal.**

# ════════════════ THE BOARD ════════════════
# Workstreams ordered by execution priority. Each item: ID · state · one-line.
# States: ⬜ not-built · 🟧 shallow (exists, inadequate) · 🩹 broken · 🟢 done-awaiting-verify · ✅ verified

## FIN — Financial correctness (v0.1) — DO FIRST
- **FIN-1** ✅ VERIFIED (Florin deploy + Planner code-confirm 2026-06-04): substring fallback REMOVED; credit notes link by explicit `parentInvoiceId` ONLY (CN creation stamps `parentInvoiceId:[id]`). Wrong-attach corruption closed. (was B3)
- **FIN-1b** ✅ VERIFIED (Florin, 2026-06-04 — real credit note CN-2026-006 correct: right parent link, numbering, VAT/totals). Phrasing leak ("Creditnota voor" on FR doc) is NOT a FIN bug → routed to LOCALE-2 (stored-text-in-wrong-language root fix). Purge of string-guessing of document identity:
  1. ✅ (code-confirmed) Remove `CN-` title fallback from `isCreditNote` in `ClientInvoiceEngine.tsx` (line 315 now `docType === 'opt-credit-note'` only) — ALSO do/confirm it in `InvoicePDFTemplate.tsx`.
  2. `create-invoice.ts` — accept explicit `type` param instead of inferring from title prefix. (verify landed)
  3. Guard auto-status: never overwrite `opt-paid` or `opt-uncollectible` with a credited status.
  4. ✅ (code-confirmed) `creditedTotal` uses `Math.abs()` (line 225) for negative-stored credit totals.
  - Florin verify: create a credit note + an invoice titled with "CN" coincidentally → detection still correct; a paid/uncollectible invoice does NOT get flipped to credited; credited totals correct sign. (Florin-scoped cleanup 2026-06-04)
- **FIN-2** 🟡 REOPENED by FIN-7. CN numbering looked ✅ on a manually-created CN (CN-2026-006), BUT the CN-from-invoice BUTTON path falls back to the invoice number → the `creditnote` numbering action is failing on the deploy. See **FIN-7**. `DocType` incl. `'creditnote'` w/ DB-derived sequence (next-document-number.ts:126) is correct in code; the failure is environmental. ⏳ Florin still verify the INVOICE-import-no-skip half (import keeps own number, counter doesn't advance). (was B4 + CN numbering)
- **FIN-3** 🟢 DONE — awaiting Florin verify. Lock model = `isLocked` + snapshot (legal immutability); project/offerte relations sit outside locked content. **Florin verify:** open a SENT/locked invoice → project + offerte STILL editable, line items/amounts LOCKED. (was Q2 part4)
- **FIN-4** ✅ VERIFIED (Planner code-confirm): persists `creditedPercent` as `"xx% — €x.xxx,xx"` (nl-BE), guards terminal statuses (paid/uncollectible) from credited overwrite. (also completes FIN-1b item 3.) ⏳ Florin glance grid display.
- **FIN-5** 🟢 DONE — awaiting Florin verify (status-select hit target in engine; click-test on deploy).
- **FIN-6** VAT/totals consolidation correct (one calculator, optional-line VAT bug, rounding matches Peppol UBL). VERIFY it actually landed (was L5 — committed but unverified). **[VAT-DISPLAY RULE keeps getting clobbered — restore + COMMIT: VAT amount shown ONLY in per-rate breakdown at bottom, NEVER per line; EN16931/Art.226; BR-CO-17 round per rate.]**
- **FIN-9** 📐 **PDF layout: legal text BEFORE calculation block, and NEITHER may split across pages (Florin 2026-06-09).** Files: `src/components/admin/quotations/QuotationPDFTemplate.tsx` + `src/components/admin/invoices/InvoicePDFTemplate.tsx`. **Apply to EVERY template variant + BOTH render branches** (invoice has branches ~L357 & ~L597; quotation summary ~L558 + medecontractant/VAT legal ~L384-396; t1/t3/t4) — a fix to one branch that leaves others splitting = FAIL.
  1. **ORDER:** in each variant, the legal-text group (`legalText` + `footer_medecontractant_legal` + `footer_vat6_legal`/0% clause) must render IMMEDIATELY BEFORE the totals/calculation (`summaryBox`) block. Invoice already comments "Legal texts — above totals" — verify ALL variants comply; Quotation: move the medecontractant/VAT legal text OUT of the summary region to ABOVE the summary box.
  2. **NO PAGE SPLIT — legal block:** wrap the entire legal-text group in a single `<View wrap={false}>`. (Currently the legal block is NOT wrap-protected → it can split.)
  3. **NO PAGE SPLIT — calculation block:** wrap the WHOLE `summaryBox`/totals block in `<View wrap={false}>` (not just one row). Invoice has `wrap={false}` on a totals row (L377/L617) and quotation on L558 — confirm the boundary encloses the ENTIRE calc block (subtotal → VAT breakdown → grand total), not a subset.
  4. **@react-pdf semantics:** `wrap={false}` keeps a `<View>` intact — if it doesn't fit on the current page it moves wholesale to the next. Legal + calc are short, so safe. (If Florin wants legal+calc ALWAYS on the same page, wrap BOTH in ONE `wrap={false}` parent — but the literal ask is each-intact, so two adjacent `wrap={false}` blocks satisfies it.)
  **VERIFY:** generate a quote AND invoice long enough to push the end across a page boundary (many line items) → in every template variant: legal text sits directly above totals, and neither the legal block nor the calc block is ever cut by the page break (each moves intact to the next page if needed). Check light + the credit-note/proforma variants too.
- **FIN-10** 🩸 **DUPLICATE-BOOKING DETECTION for financial documents (Florin 2026-06-09: "we really, really need this").** Today NO dedup exists → the same supplier invoice can be booked twice (paid/accounted twice) — a serious financial-integrity hole. Worst case: an invoice arrives via **Peppol** AND is also **scanned/uploaded** manually → two records, one real bill. **Scope: PURCHASE INVOICES first** (highest risk), then receipts.
  - **IDENTITY KEY (exact-match, NOT fuzzy — recurring rule):** a purchase invoice's true identity = **`supplierVat` + supplier's `invoiceNumber`**. Same pair = same document = duplicate. Corroborate with `totalInclVat` + `issueDate`. Fields already exist on the record (`supplierVat`, `invoiceNumber`, `issueDate`, `totalInclVat`). When VAT or number is missing, fall back to (`supplierName` + `total` + `issueDate`) as a WEAKER warn-only match.
  - **Shared helper** `findDuplicatePurchaseInvoice({ tenantId, supplierVat, invoiceNumber, total, issueDate, excludeId? })` → returns existing match(es), tenant-scoped. SINGLE chokepoint called from EVERY ingestion path so cross-channel dups are caught: scan/upload save (`TicketCaptureModal` invoice mode + `/api/scan`), **Peppol inbox import** (`/api/peppol/inbox`), and manual create. (Matching across Peppol + scan is the key win.)
  - **(A) PREVENTIVE — at scan/upload (warn before save):** if a match exists → show a blocking confirm: "A purchase invoice from {supplier} #{invoiceNumber} for €{total} ({issueDate}) already exists. [View existing] [Save anyway] [Cancel]." Don't hard-block (legitimate re-issues happen) but require EXPLICIT override; default = don't duplicate.
  - **(B) DETECTIVE — system finds them later:** a dedup scan that groups existing purchase invoices by identity key and flags duplicate groups → surface in the Financials module (a "possible duplicates" banner/list) + a **NOTIF** notification ("N possible duplicate invoices found"). User reviews → keep one / delete the other.
  - **NOT a hard DB unique constraint** (nulls + legitimate edge cases break it) — soft detection + explicit user decision. (Optional stretch: partial unique index on `(tenantId, supplierVat, invoiceNumber)` where both non-null.)
  - **INTEGRATES:** BANK-1 (OGM payment-match breaks if duplicate invoices exist), NOTIF (detective alert), Peppol inbox (cross-channel). **VERIFY:** upload the same supplier invoice twice → 2nd warns with View/Save-anyway/Cancel; receive a Peppol invoice then scan the same one → flagged as duplicate; run the detective scan over seeded dups → groups + notifies.
- **FIN-7** 🩹 **CN-from-invoice button mints the INVOICE number as the credit-note number — numbering scheme ignored.** ROOT CAUSE (measured): `ClientInvoiceEngine.tsx` `handleCreateCreditNote` (~line 346-350):
  ```
  const numResult = await getNextDocumentNumber('creditnote');
  const cnNumber = numResult.success && numResult.number ? numResult.number : `CN-${invoiceNum}`;
  ```
  The proper path calls the right action, BUT the **fallback embeds the invoice number** (`CN-${invoiceNum}`). Florin sees the invoice number → the fallback is FIRING → `getNextDocumentNumber('creditnote')` is returning `success:false` in the deploy. The action itself (`next-document-number.ts:126`) looks correct, so the failure is environmental — **MEASURE it (per /pd): surface `numResult.error`** (console/toast). Strong hypothesis to check first: `creditnote*` columns missing / migration not applied on the deploy → the Prisma `select:{creditnotePrefix:true,…}` throws → caught → success:false. (Invoice/quotation numbering work because their columns exist.) **FIX:** (1) confirm `creditnote*` columns exist + migration ran; (2) **remove the invoice-number fallback entirely** — a credit note must NEVER inherit the invoice's number. On numbering failure: abort creation with a clear error (don't mint a wrong number). This supersedes FIN-2's optimistic "CN numbering ✅" — that was verified on a manually-created CN, not this button path.
- **FIN-7b** 🩹 **CN numbering produces DUPLICATES + out-of-sequence (EVIDENCE: Coral outbox via peppol debug, 2026-06-07).** Two sent docs both `CN-2026-007`; sequence `006,007,008` then `025` (025 created earlier); inconsistent padding (`CN-2026-006` vs `CN-2026-23`). NOT the timestamp fallback → **max-sequence read is racing**: rapid CN creation re-reads the same max / `creditnoteNextNumber` not advanced ATOMICALLY → concurrent creates collide. **FIX:** atomic numbering (DB txn + atomic `increment` on `creditnoteNextNumber`, or `MAX(seq)+1` in a serialized txn) + **UNIQUE constraint on (tenantId, invoiceNumber)** so a collision throws not duplicates; consistent zero-padding. Legal docs MUST be unique+sequential. Verify: create several CNs rapidly → all unique, consecutive, padded. [re-added after concurrent-write clobber 2026-06-07]
- **FIN-8** 🩹 **CN-from-invoice 404s after creation (document IS created, shows in Credit Notes table).** ROOT CAUSE (confirmed): `handleCreateCreditNote` does `router.push('/admin/financials/income/credit-notes/${id}')` but **that dynamic route doesn't exist** — `income/credit-notes/` has only `page.tsx` (the table), no `[id]/`. **FIX:** create `src/app/[locale]/admin/financials/income/credit-notes/[id]/page.tsx` mirroring `income/invoices/[id]/page.tsx` exactly: `return <ClientInvoiceEngine id={id} locale={locale} />;` (CNs live in `db-invoices`; the engine already handles `isCreditNote`). Verify the Credit Notes table row links also target this route. (FIN-7 + FIN-8 are both in `handleCreateCreditNote` but are distinct fixes → two commits.)

## GRID — Database/editing foundation (v0.2) — fixes MANY symptoms at once
- **GRID-0** 🟢 DONE (working tree, uncommitted) — **NotionGrid.tsx decomposition (1,689→1,237 lines).** Extracted 5 units into `database/hooks/`: `useGridColumns.tsx`, `useFilteredPages.ts`, `useVatLookup.ts`, `useExportCSV.ts` + `NotionGrid.css`. **WHY: TypeScript limits.** Project `tsc --noEmit` runs >44s (heavy); a 1.7k-line component over `react-datasheet-grid`'s generic `Column<T>` is a classic source of slow type-check / build OOM / `ts2589`/`ts2590`. Decomposition + explicit param interfaces + deliberate `Column<any,any>` at the DSG boundary shrink the inference surface — the correct remedy, NOT a hack. **Planner-validated:** `useGridColumns` keeps a complete `useMemo` dep array, grid `key={databaseIdRef}` and `rowData` memo intact → GRID-1/2/3 memoization PRESERVED, low regression risk. **GATE = the build:** run `next build` / full `tsc --noEmit` → must compile clean AND show reduced type-check time/mem (that's the proof the limit is relieved). **Caution logged:** both files `eslint-disable react-hooks/preserve-manual-memoization` — dep arrays are complete today (omitted store actions/setters are stable), but with that lint off a future edit could silently add a stale-closure dep and reintroduce GRID-1 edit-dropouts → add a code comment warning. **Levers if still tight:** `NODE_OPTIONS=--max-old-space-size=4096` for build; `skipLibCheck`/`incremental` already on; **Vercel Pro's larger build resources directly help** (ties to the Pro upgrade). Apply same recipe to the next monoliths (`ClientInvoiceEngine.tsx`).
- **GRID-1** 🟢 DONE — awaiting Florin verify (optimized NotionGrid column memoization and stabilized key to eliminate remounts and cell edit dropouts).
- **GRID-2** 🩹 Name-property "open" button only works when the cell is NOT selected; if selected, button dead. Fix.
- **GRID-3** 🩹 **Column resize is broken: dragging a column makes it DISAPPEAR mid-drag, then it springs back at the new width only on refresh (Florin 2026-06-12).** ROOT CAUSE (Planner-diagnosed): DUAL width source-of-truth. The floating header `<div>` (`NotionGrid.tsx` ~L792/868) is bound LIVE to `resizeOffset` state → tracks the cursor. But DataSheetGrid gets width only via the column config `minWidth` (`useGridColumns` ~L84-85) and **DSG does NOT re-flow already-mounted cell layout when only `minWidth` changes between renders** — it recomputes widths on MOUNT + on its own container ResizeObserver. So mid-drag the cell column gets a bad/zero effective width (→ disappears) and the correct width only lands on a REMOUNT (refresh). Header (cheap, live) and grid (no reflow) diverge → the jank. **Why prior fix failed:** it propagated the value into the `columns` prop, but it never reaches DSG's *layout* without a remount — and you can't remount on resize (GRID-1 stabilized `key={databaseIdRef}` to stop edit-dropout remounts). **FIX (intent — coder picks the mechanism, /pd measure first: confirm DSG cell width changes during drag vs only on remount):** drive the LIVE drag purely in CSS (direct DOM width / CSS var on the grid column container) for 60fps and zero React churn, then on release commit the width to the store ONCE and trigger DSG's own remeasure WITHOUT a key-remount (e.g. dispatch a resize so its ResizeObserver fires, or the minimal layout-invalidation DSG exposes). Unify to ONE width source so header + cells cannot diverge. **VERIFY:** drag a column → it stays visible and resizes smoothly in real time; on release the new width persists with no refresh; no remount/edit-dropout.
- **GRID-4** Remove the elastic/rubber-band drag animation at view edges — just stop at the edge.
- **GRID-5** 🩹 Flyouts/dropdowns clipped at screen edge everywhere. ONE fix: edge-aware positioning (measure viewport, flip orientation when overflowing). Applies app-wide (billing card, selects, menus). (was U2 + general)
- **GRID-6** Remove the standalone **details view** app-wide; standardize on the Notion-style **modal** only. Keep open-button + drag-handle actions. Applies to: Sales, Contacts, Suppliers, Articles, Receipts, Bestek. (ONE decision, ~6 modules)
- **GRID-7** Reusable **searchable select** component (type-to-filter). Used by: Quotations (project, client, pipeline), Projects, Financials. Build once.
- **GRID-8** Filter/sort UX: sort sometimes not applied (fix); filter must adapt to property TYPE — select property → show its options with is/is-not; "contains" → text field. Not always a text input.
- **GRID-9** 🩹 **Notion-style detail view — text too small + low contrast for the large window (Florin 2026-06-09).** MEASURED: the detail-view components render labels/values at **9–12px** (`text-[9px]`, `text-[10px]`, `text-[11px]`, `text-xs`) in **muted greys** (`text-neutral-300/400/500`, `dark:text-neutral-400/500`) inside a large modal → poor legibility. Affects ALL THREE: `src/components/admin/database/components/RecordDetailPage.tsx`, `PageModal.tsx`, `ProjectDetailView.tsx`. **FIX — fundamental, not ad-hoc:** define ONE legible type+contrast scale for the detail view and apply it consistently across the three files (don't tune individual classes by hand). Target scale: **property VALUES ≥ 14px (`text-sm`/`text-base`), labels 13px, section headings 16–18px; ELIMINATE every `<12px` (`text-[9/10/11px]`)** in these views. **Contrast:** labels → `text-neutral-600 dark:text-neutral-300`; values → `text-neutral-900 dark:text-neutral-100`; retire `text-neutral-300/400` for primary content (keep muted only for true hints/metadata, and even then ≥ neutral-500/dark:neutral-400). Type should feel proportionate to the large window. **VERIFY:** open a record detail (large modal) → labels and values are comfortably readable at arm's length in BOTH light + dark mode; no sub-12px text; values clearly darker/stronger than labels. (Sits with GRID-6 which routed detail views into the parent-module modal.)
- **GRID-9b** SPLIT (Florin 2026-06-17): **9b-a** = inline `@` DATE insert everywhere (no NOTIF dep) → in the unattended queue NOW. **9b-b** = `@` reminder fires a notification → AFTER NOTIF (queued post-NOTIF). NOTIF itself is now in the unattended queue (16-20). (renamed from a 2nd GRID-9 collision)
- **GRID-10** 🩹 **Projects lost their ADVANCED detail view + detail-renderer FRAGMENTATION (Florin 2026-06-12: "fixed and broken multiple times — make it smooth once and for all").** ROOT CAUSE (Planner, measured): the advanced `ProjectDetailView.tsx` is rendered ONLY inside the standalone `RecordDetailPage.tsx` (L208). **GRID-6 standardized on the modal (`PageModal`) and removed standalone record pages** → project records now open in `PageModal`, which is NOT type-aware and renders only the generic property view → the advanced project view is orphaned (still in the repo, never reached). That's the "simple view survived, advanced one for projects didn't."
  - **⚠️ CORRECTED DIRECTION (Florin 2026-06-15, with screenshots — supersedes the earlier "render in the modal" framing):** GRID-6's "remove the standalone detail view, standardize on the generic modal" was the REGRESSION. The new generic `PageModal` is "completely useless" for rich document/record types — it shows a few props, no line items, no original document. **KEEP / RESTORE the OLD full detail view** for rich types. Florin's example (purchase invoice, BigMat): a **full-height split layout** — LEFT = ALL document properties + **ORDERLIJNEN (line items table)** + **BIJLAGEN (attachments incl. the source UBL)**; RIGHT = the **original document rendered** (PDF). That is the target for **projects** (`ProjectDetailView`) AND **purchase invoices/expenses** (`PurchaseInvoiceEngine` split view). Do NOT cram these into the cramped centered modal.
  - **THE FIX (the *how*: rich types use their FULL view; generic modal only for simple records):** route by base-db identity (`getLockedDbId`): **projects → `ProjectDetailView`; purchase-invoice/expenses → the split-pane purchase view (props+ORDERLIJNEN+attachments left, original doc right); simple records (contacts/articles/simple lists) → the lightweight view.** The rich views are full-page / large split panels, NOT centered modals. Restore the standalone rich views GRID-6 bypassed rather than re-implement them in the modal.
  - **DESIGN NOTE (Florin):** he dislikes intrusive centered modals — prefer full-page or side-panel detail surfaces for rich records. Weigh this when choosing the host (panel/page over modal).
  - **CONSOLIDATION (the "reflects EVERYWHERE" ask) still holds:** parallel hosts drift (`NotionGrid`+`DatabaseClone`; `RecordDetailPage`+`PageModal`). Designate ONE type-aware detail ENTRY that dispatches to the right view per type; every surface delegates to it. The point isn't "one modal" — it's "one dispatch, correct view per type." (Apply GRID-9 legibility in those views once.)
  - **VERIFY:** open a PROJECT → advanced ProjectDetailView; open a PURCHASE INVOICE → split view with line items + original document on the right (NOT the sparse generic modal); open a Contact → light view; PDF/download serves the ORIGINAL document, not a CoralOS-regenerated sparse one; no useless generic modal for rich types. (Ties to PEPPOL-4 which populates supplier/lines/original-doc for inbound.)
- **GRID-11** 📋 **Improve our database model — research Notion + ClickUp, then enhance (Florin 2026-06-12).** AFTER GRID-10 consolidation (improve the ONE renderer, not the fragments). Planner to research & recommend: Notion DB patterns (property types incl. relation/rollup/formula/status, views: table/board/calendar/timeline/gallery, grouping, filters/sorts presets, templates, inline sub-items, `@`-mentions) + ClickUp adds (statuses-as-workflow, custom fields, multiple assignees, dependencies, nested subtasks, list/board/gantt). Map gap vs ours; recommend a prioritized enhancement set that fits the multi-tenant `GlobalDatabase`/`GlobalPage` model. **Planner deliverable = a recommendation doc, NOT code; Florin picks scope; then phased coder instructions.** (Deferred design pass — flagged so it's tracked; do not start before GRID-10.)

## SCHEMA — Data integrity cleanup (v0.1) — SPLIT: stop the cause, THEN clean (data-safe)
> 🩸 Root cause (Planner, code-read 2026-06-04): duplicate "Quotation"/"Contacts" DBs + stray "New Workspace"/"New Database" entries are MANUFACTURED by ≥5 uncoordinated create paths: `provisionLockedDatabases` (intended), `pages.ts:75`, `peppol/inbox/route.ts:215`, `scan/route.ts:417` (each "create-if-missing"), and `DatabaseClone.tsx:713` + `dynamic-db/page.tsx:24` (`createDatabase('New Database'…)`). When `getLockedDbId` resolves a tenant's system-DB id inconsistently (bare `db-quotations` vs `db-quotations-<suffix>`), multiple paths each create → duplicates. **A pure data-clean would re-spawn duplicates immediately. Must stop the cause FIRST.**

- **SCHEMA-1a** ✅ VERIFIED (Planner code-confirm 2026-06-04, commit `SCHEMA-1a`): all 3 server create-paths (`pages.ts`, `peppol/inbox`, `scan`) now resolve canonical id via `getLockedDbId` BEFORE the existence check → create-only-if-missing on the SAME id (no cross-path duplication). `dynamic-db` button confirmed safe (UUID id, not system prefix). DatabaseClone uses `resolveDbId`. Tap is SHUT. · minor note: `findUnique`-then-create (not atomic upsert) → tiny theoretical race on a brand-new tenant's first DB; acceptable, suspect here if a fresh dup ever appears.
- **SCHEMA-1b** 🟢 EXECUTED LIVE 2026-06-04 — ✅ PAGE COUNTS CONSERVED both tenants (Murgu 4→4; Coral 1250→1250). Provisioned db-payments-in/out-cmneyas2, migrated 86 payment pages intact, deleted 7 empty dups + the migrated-empty sources, Murgu 1-ticket migrated. No data loss. **⏳ Florin's ONLY remaining check: open Coral → Received Payments → confirm the 86 are VISIBLE (proves the lockedDbIds repoint resolves; conserved-in-DB ≠ visible-in-UI). If blank → lockedDbIds pointer gap (data safe, snapshot covers). Then ✅.** **Dry-run report (data-safe, merge of EXISTING duplicates):**
  - Build an auditable script: per tenant, group GlobalDatabases by system-type; for each type with >1, pick canonical (the one in `lockedDbIds`, else the one with most pages), **MIGRATE all GlobalPages from duplicates into the canonical**, repoint references (`lockedDbIds`, any `databaseId` FKs), THEN trash the now-empty duplicates. Remove "New Workspace"/"New Database" garbage ONLY if confirmed empty/orphaned.
  - **MANDATORY: dry-run mode that REPORTS what it would merge/delete/migrate (counts + ids) WITHOUT executing. Florin reviews the report, THEN authorizes the live run.** Never blind-delete a DB that holds pages.
  - Acceptance: each tenant has exactly ONE of each system DB; ZERO page loss (page counts before == after, in the canonical); garbage removed; dry-run report archived in feedback.
- **SCHEMA-2** Status/option IDs canonical + consistent across engine and DB views (partially touched; verify).

> ⚠️ Data-mutating on live tenants. 1a is safe (code only). 1b MUST dry-run → Florin-approve → execute. Two separate one-at-a-time items.

## EDITOR — Real rich-text + Notion blocks (v0.3)
- **EDITOR-1** 🟧 The "rich text" line in quotation/financial engines is shallow (too few options). Replace with a PROPER rich-text editor: headings, body, font size, **B/I/U/S**, link block, table block, image block, + standard Notion blocks. (Quotations, Bestek pricing notes, Journal all depend on this — was J1/L3, done shallow.)
  - **RECOMMENDED LIB (Planner): BlockNote** (`@blocknote/react`+`@blocknote/core`, React/ProseMirror) — Notion-style blocks + slash menu + drag-reorder out of the box, least custom work. Alts: TipTap (more wiring) / Lexical (lower-level); coder picks+justifies. FEASIBILITY GOOD/medium: current `JournalCard`/`BlockEditor` is a block LIST (no slash menu/inline/drag) → "far cry from Notion" accurate. Considerations: (1) one-time mapper between existing GlobalPage block JSON ↔ BlockNote schema (keep storage in `page.blocks`); (2) SSR-safe dynamic import; (3) wrap once as shared `<RichBlockEditor>` for Quotations+Bestek+Journal — the FILES-5 image picker plugs into its image block. v0.3, behind GRID. [re-added after concurrent-write clobber 2026-06-07]
  - **RECOMMENDED LIB (Planner, confirmed from history + feasibility 2026-06): BlockNote** (`@blocknote/react` + `@blocknote/core`, MPL-2.0/permissive, React, ProseMirror-based) — Notion-style blocks + slash menu + drag-reorder out of the box; least custom work. Alternatives if more control needed: TipTap (headless, more wiring) or Lexical (Meta, lower-level). **Coder picks + justifies in feedback.** FEASIBILITY: GOOD, medium effort. Current `JournalCard`/`BlockEditor` = a block LIST (quick-add heading/list/todo/quote/callout/code), NO slash menu / fluid inline editing / drag-reorder / nested toggles → "far cry from Notion" is accurate. Considerations: (1) BlockNote stores its OWN block JSON schema → write a one-time mapper from the existing GlobalPage block shape → BlockNote blocks (and back on save), keep storage in the same `page.blocks`/property; (2) must mount inside the multi-tenant GlobalPage model + SSR-safe (dynamic import, `ssr:false`); (3) reused in Quotations + Bestek + Journal → wrap once as a shared `<RichBlockEditor>` so all three share it (the FILES-5 image picker plugs into its image block). Sequence behind GRID (v0.2) since it's v0.3.
- **EDITOR-2** Add ALL Notion block types available, app-wide where blocks are used.
- **EDITOR-3** Journal entries must each be a DISTINCT journal entry (not merged); auto-linked to the host record. (was J1 link part)

## QUOTE — Quotation module (v0.3/0.4)
- **QUOTE-1** Project select → searchable (GRID-7).
- **QUOTE-2** Client select → searchable (GRID-7).
- **QUOTE-3** Sales-pipeline backlink select.
- **QUOTE-4** Offertes-connection select displays **number + subject**.
- **QUOTE-5** Rich-text editor in engine (EDITOR-1).
- **QUOTE-6** 📋 **PAYMENT PLAN & TERMS — elaborate payment module (quote + invoice).** Florin 2026-06-08: build this properly; OGM is ALREADY done (Stripe + QR + `lib/ogm.ts` + structuredComm + `lib/dunning.ts`) → **do NOT re-implement OGM/QR/payment-link/dunning; INTEGRATE with them.** One reusable `PaymentPlan` attached to the document, tenant-level defaults + per-document override. FOUR parts:
  1. **Payment method** (per doc, default from profile): bank transfer (renders IBAN/BIC), Peppol e-invoice, cash, card/Stripe link, QR. Selectable on the document.
  2. **Payment terms:** due-date model (Net X / end-of-month / fixed date / on-receipt) — extends existing `defaultPaymentTermDays` (tenant). Add Belgian B2B legal clauses: statutory late-payment interest + recovery indemnity, localized NL/FR clause text (reuse `lib/dunning.ts` logic/labels — don't duplicate).
  3. **Payment schedule — the core (extends the old QUOTE-6 staged idea):** a schedule builder with modes: single | deposit+balance | **staged/milestone (% of total)** | fixed-amount installments | **progress billing (% completion → vorderingsstaten)**. Each stage = row: label ("Voorschot","Bij aanvang","Bij oplevering"), trigger (date/milestone/% complete), basis (% or €), due-offset, status (planned→invoiced→paid). Validate schedule sums to 100%/total. **Each stage can GENERATE its staged invoice** → quote becomes parent of a series; feeds project P&L + vorderingsstaten.
  4. **Deposit / voorschot:** special first stage, tracked against final invoice, shown as deduction ("reeds gefactureerd voorschot") so the final doc reconciles.
  **MODEL:** `PaymentPlan { method, terms{dueModel,dueDays,lateClause}, schedule[{label,trigger,basis,value,dueOffset,status,generatedInvoiceId}], deposit }` on the document (OGM stays where it is). Tenant defaults in Settings (model the setup UI like the profitability card). **INTEGRATES:** FIN-6 (VAT/totals per rate), existing OGM/QR/Stripe/dunning, BANK-1 (Ponto OGM match closes the loop: staged invoice → OGM → bank feed auto-matches), insurance-claim quote structure. (was Q5; OGM/QR explicitly out of scope — already shipped.)
  **⚠️ BUILD IN PHASES — one sub-item = one commit = one Florin ✅. DO NOT build it all and hide it (that's the batched anti-pattern protocol rule 1 forbids).** Coder's instinct to do the PDF last is CORRECT — that's phase 6e. Each phase below is independently verifiable in-app even before the PDF exists:
  - **QUOTE-6a — Data model only.** Persist `PaymentPlan { method, terms{dueModel,dueDays,lateClause}, schedule[{label,trigger,basis,value,dueOffset,status,generatedInvoiceId}], deposit }` on the document (GlobalPage property / JSON). Read+write helpers. NO UI, NO PDF. **Verify:** a plan written programmatically round-trips on a quote (inspect in DB/devtools).
  - **QUOTE-6b — Tenant defaults in Settings.** Method + terms (extends `defaultPaymentTermDays`) + NL/FR legal clause text (reuse `lib/dunning.ts` labels). **Verify:** defaults save + reload (respect PROFILE-2 hydration rules — no fake-default write-back).
  - **QUOTE-6c — Schedule editor UI** in the quote engine (modes: single | deposit+balance | staged % | fixed installments | progress %). Validates sum=100%/total; new-doc inherits 6b defaults. NO PDF yet — visible/usable in-app only. **Verify:** build each mode in UI → persists (6a) → sums validate → reopen shows it.
  - **QUOTE-6d — Logic: generate staged invoices** from the plan; deposit/voorschot deduction on the final; wire to vorderingsstaten + project P&L; stage status planned→invoiced→paid. **Verify:** generate stage invoices → amounts correct, deposit deducts on final, statuses advance.
  - **QUOTE-6e — PDF rendering (LAST).** Render payment plan + terms + schedule table on quote & invoice templates, VAT per FIN-6 rule. **Verify:** generated PDF shows the schedule, due dates, deposit deduction, legal clause; matches transmitted e-invoice. ← this is the "keep hidden from PDF for now" part — it's simply the final phase, not a hidden flag.
  **So: yes defer the PDF (6e last), but 6a→6d are SEPARATE commits each verified by Florin — not one hidden mega-build.**
- **QUOTE-7** 🩹 **Save-to-Library writes the shop discount into the SUPPLIER column, not the discount column** (margin + other fields fine). ROOT CAUSE (measured — recurring substring-matching failure class): `SaveToLibraryModal.tsx` `findPropId` (line 28-30) returns the first property whose name *contains* any alias. Discount aliases `['korting','remise','discount','disc','lever']` (line 37) — `'lever'` is a substring of **"Leverancier"** (the supplier relation prop `prop-art-supplier`, which is defined BEFORE `prop-art-remise`/"Discount" in the articles schema, `mockData.ts:775` vs `:778`). So `propMap.discount` resolves to `prop-art-supplier` → on save (line 78) the discount % is written into the supplier relation (corrupting it) and the real Discount prop never gets it. Margin works because `['marge','margin']` can't false-positive. **FUNDAMENTAL FIX (use schema identity, not string guessing):** `db-articles` is a SYSTEM db with canonical prop ids — map directly to them (`prop-art-bruto`, `prop-art-remise`, `prop-art-margin`, `prop-art-verkoop`, `prop-art-unit`, `prop-art-type`), exactly as the apply path already does (`FinancialRowRenderer.tsx:222-224`). Only fall back to name-matching for non-system custom DBs, and when doing so use EXACT name equality — never partial/substring — and drop the dangerous `'lever'`/`'disc'` aliases. **Also check the invoices twin** `src/components/admin/invoices/SaveToLibraryModal.tsx` for the same poisoned alias list. **Verify:** save an article with a discount → Discount column gets it, Leverancier relation untouched; reload into a quote → discount applies.

## PROJ — Projects module (v0.4/0.5)
- **PROJ-1** ALL | Operations | Administration | Business Dev → move to TAB BAR as **filtered views** of the same DB (NOT new databases).
- **PROJ-2** Keep only ONE timeline (de-duplicate).
- **PROJ-3** Detail view layout: financial analysis → top of side section; financial documents → middle section.
- **PROJ-4** Detail view: linked financial documents + connected relations cards → middle section, side-by-side, ABOVE tasks card.
- **PROJ-5** 🩹 Billing card select flyout clipped by card edge (GRID-5 should fix; verify here).
- **PROJ-6** Detail view: connected-tasks → a Notion-style DB VIEW of connected tasks (replace connected-records section).
- **PROJ-7** Vorderingstaten = linkable documents from a DB; create the vorderingstaten DB if missing; add as a tab in Projects.
- **PROJ-8** 🩹 Kanban: card doesn't open the project → flyout with editable+savable property list.
- **PROJ-9** 🩹 Kanban: three-dot menu doesn't work.
- **PROJ-10** Kanban cards: **PER-USER** configurable visible card properties (stored per user) + values **editable inline** on the card (Florin decided 2026-06-17 — both: choose-visible AND edit-values; per-user scope). Spec in the CODER queue.
- **PROJ-11** 🩹 Timeline: drag handles hard to grab (name field extends to card edge).
- **PROJ-12** Timeline becomes the **PLANNING TIMELINE** tab in Projects.
- **PROJ-13** Timeline: add tasks to projects + render task entries on timeline; project = dropdown, tasks visible in expanded project (Notion task/subtask timeline style).

## FILES — Storage: MIGRATE Google Drive → Vercel Blob (v0.2)
**DECISION (Florin, 2026-06-06):** App is the only door to files (no external Drive browsing). **Full migration to Vercel Blob.**
**✅ PREREQS SATISFIED (Florin, 2026-06-06):** PRIVATE Blob store created (`store_yhNt9hDHlPfEV3x6`); `BLOB_READ_WRITE_TOKEN` provisioned in Vercel env (values NOT in repo — env only). `BLOB_WEBHOOK_PUBLIC_KEY` available but webhooks are OPTIONAL/later (not a v0.2 dep). Stream is unblocked to run AFTER v0.1 is tagged. Drive's folder model was the root of "creates a folder per item / saved-but-not-findable / scattered roots" — Blob is flat key-based storage, which structurally eliminates that entire bug class. The old FILES-1/2/3 (Drive-folder patches) are RETIRED by this decision — do NOT work them.
**EXECUTION ORDER (one item = one commit, ✅-gated):** FILES-ISO → FILES-0 → FILES-1 → FILES-2 → FILES-3 (data migration, dry-run→Florin-approve) → FILES-4 (retire dead Drive storage) → FILES-5 (quote image picker). SCAN-4 rides on FILES-0/1.
**SCOPE BOUNDARY — read before touching anything:** Migrate ONLY the file-STORAGE surface. Google **Calendar** + **Gmail** OAuth STAY (they are not storage). Do NOT remove `api/calendar/*`, `api/email/connect/google/*`, `googleToken.ts`, or shared Google OAuth token infra. Storage surface to migrate: `lib/google-drive.ts`, the file-storage parts of `lib/google-drive-oauth.ts`, `api/drive/*` (init, route, upload, list, callback, auth, migrate-f4), `components/admin/file-manager/*`, `components/admin/drive/DriveFileExplorer.tsx`, `components/admin/expenses/TicketCaptureModal.tsx`, `useScheduleAttachments`, `useProjectAttachments`, `components/portal/DocumentManager.tsx`. **Coder: measure-before-deduce (/pd) — grep the real call sites first; this list is the planner's map, not gospel.**

**NORTH-STAR DESIGN — adapter seam (the general measure, not 49 detail edits):** Introduce ONE storage interface and route everything through it. The Drive→Blob swap then lives at a single boundary.

- **FILES-ISO** 🔒 **TENANT ISOLATION — read FIRST, it shapes FILES-0.** The `t_{tenantId}/` prefix is ORGANIZATION, not a security boundary. Two non-negotiables: **(1) Use a PRIVATE Blob store** (`access: 'private'`, Vercel Private Storage — built for invoices/contracts; public-beta Feb 2026). Public blobs get an unguessable-but-public URL that bypasses all auth if leaked — unacceptable for an ERP. **Store privacy is fixed at store creation and CANNOT be changed after — Florin must create the store as private.** Private blobs have no public URL; retrieve only via SDK `get()`. **(2) Records store the KEY, never a URL.** Serve files through an authenticated route `GET /api/files/[...key]` that (a) resolves session tenantId, (b) asserts the requested key startsWith `t_{sessionTenantId}/` — reject otherwise (this is the identity-check that replaces Drive's `isFolderOwnedByTenant` parent-walk; one prefix assert, fail-closed), (c) streams the blob. EVERY `get/list/delete` derives tenantId from session and refuses keys outside the prefix — NEVER trust a client-supplied key. (Recurring failure class: validate identity, not the string.) **(3) ISOLATION UNIT TEST (required, Florin 2026-06-17):** add a test that asserts a session for tenant A requesting a key under `t_{tenantB}/` is REJECTED (and a valid same-tenant key passes). This proves isolation in code — chosen over per-tenant Blob stores (which hit Vercel's per-plan store cap + token-per-tenant sprawl; single private store + this prefix-assert is the scalable, standard model, consistent with our single-DB tenant-scoping). **DECISION CONFIRMED (Florin 2026-06-17): single PRIVATE store + key-prefix isolation — NOT store-per-tenant.**
- **FILES-0** Define `StorageProvider` interface in `src/lib/storage/index.ts`: `put(key, data, opts) → {key, url}`, `get(key) → stream/url`, `delete(key)`, `list(prefix) → entries`. Add `BlobStorageProvider` (`@vercel/blob`, **`access: 'private'`**) as the implementation. `put` returns the KEY (persist that on the record); reads go through the FILES-ISO serving route, not a stored public URL. **Key scheme (tenant-isolated, flat):** `t_{tenantId}/{recordType}/{recordId}/{filename}` (e.g. `t_ten-gar163.../receipt/pg-abc/aldi-2026-06.pdf`). No folder objects — the prefix IS the namespace. **EXCEPTION — projects allow subfolders:** project keys may carry an arbitrary nested subpath: `t_{tenantId}/project/{recordId}/{subpath…}/{filename}` (e.g. `…/project/pg-7/permits/2026/bouwvergunning.pdf`). Blob keys are plain slash-delimited strings, so subfolders cost nothing at storage level — they are VIRTUAL, derived from key segments, never stored folder objects (so the "folder-per-item" bug still can't recur). Env: `BLOB_READ_WRITE_TOKEN` (Florin provisions in Vercel). **This task ships the seam with NO behavior change yet — provider exists, nothing calls it.**
- **FILES-1** Repoint **WRITE** paths to the seam: `api/drive/upload`, `TicketCaptureModal` (receipt capture), attachment hooks. Each upload calls `storage.put(...)` and **persists the returned `{key, url}` onto the owning record** (page property / attachment row) — this is what kills "saved-but-not-findable": the file is found by its stored key on the record, never by walking a folder tree.
- **FILES-2** Repoint **READ/LIST/DELETE** paths: `FileManager*`, `DriveFileExplorer`, `DocumentManager`, `api/drive/list|route`. In-app explorer renders from `storage.list(prefix)` + record-stored keys. Delete via `storage.delete(key)`. **Explorer behavior by record type:** all record types render a FLAT file list per record (no manual folders — keeps the no-mismanage guarantee). **Projects are the exception:** the project node renders its key subpaths as navigable virtual subfolders and supports create-folder / rename / move-within-project (a "folder" is created implicitly by uploading to/naming a new subpath — no folder object is persisted). Move = re-key (copy to new key, update record ref, delete old). Subfolder UI is scoped to `recordType === 'project'` ONLY.
- **FILES-3** 🩸 **DATA MIGRATION (treat like SCHEMA-1b — dry-run first, manifest, reversible).** Walk every tenant's existing Drive files, copy each into Blob under the new key scheme, update the owning record's stored URL/key. **Dry-run output:** per-tenant file count + total bytes + sample key mapping; Florin reviews before live. **Idempotent + resumable** (re-running skips already-copied keys). Keep Drive originals untouched until Florin verifies (Drive = the parachute; no snapshot needed since we don't delete source).
- **FILES-4** After Florin verifies files render in-app post-migration: retire dead Drive storage code (`api/drive/*` storage routes, `google-drive.ts` storage fns) — **but keep Calendar/Gmail OAuth.** Leave Drive files in place as cold backup (don't delete remote).
- **FILES-5** Quotations engine — **image insert must open the new in-app file explorer (picker mode), not a native dialog.** Current: `src/components/admin/quotations/FinancialRowRenderer.tsx:493` does `const url = prompt('Image URL:'); document.execCommand('insertImage', false, url)`. Replace the `prompt()` with the FILES explorer opened as a **picker**: user picks/uploads an image → explorer returns the file's served key/URL (via the FILES-ISO auth route) → insert that into the rich-text block. **Kill the native `prompt()` system window entirely.** Same native-dialog smell sits at `:496` (`prompt('Link URL:')`) — out of scope for "image" but flag it: ideally both move to in-app UI, not browser prompts. (Depends on FILES-0/1/2 explorer existing.)
- **SCAN-4 depends on FILES-0/1** — storing the original receipt file uses the same seam + key scheme.

## SCAN — Receipts/expense capture (v0.4, free-tier core)
- **SCAN-5** 🩹⏰ **VAT-URGENT — PDF capture broken for receipts & purchase invoices (Florin 2026-06-17, evidence + screenshot). THREE bugs, same modal (`TicketCaptureModal` is shared, `isInvoiceMode` flag):**
  1. **Receipts file input is IMAGE-ONLY.** `TicketCaptureModal` L501: `accept={isInvoiceMode ? 'image/*,application/pdf' : 'image/*'}` → receipts can't select a PDF. **FIX:** accept `image/*,application/pdf` in BOTH modes.
  2. **Server PDF parsing FAILS for ALL PDFs** (the "This PDF could not be opened. It may be corrupted." message). NOT the files — `api/scan/route.ts` `getDocument()` (L78) throws on every PDF (→ L86 `PDF_LOAD_ERROR` → L369 message). Cause: pdfjs serverless setup — `GlobalWorkerOptions.workerSrc = ''` (L76) + the legacy ESM build inside a Vercel Node function fails to initialise. **FIX:** make PDF text-extraction work in serverless — recommended: switch to a serverless-first extractor (**`unpdf`** / `pdfjs-serverless`) for the text layer; for scanned image-PDFs (no text) render page-1 for OCR via the same lib (canvas on Vercel is the second fragile point). MEASURE: log the real `getDocument` error first. This is the core bug behind BOTH "can't process PDF" reports.
  3. **Manual entry with NO document = illegal for VAT (Florin).** The "enter manually" path saves fields without the source file. **FIX:** manual entry MUST allow attaching the original photo/PDF, stored on the record like a scanned one (ties to SCAN-4 / FILES store-the-original). A receipt/invoice record without its document is not acceptable.
  **VERIFY (as a tenant, before VAT season):** upload a PDF receipt AND a PDF purchase invoice → both scan/parse with no "corrupted" error; manual entry lets you attach + stores the file; the record shows the document. (Pairs with SCAN-1 = the saved record actually lands in the tenant's grid.)
- **SCAN-1** 🩹 ⏰ **VAT-URGENT (Florin: trimester VAT declaration next month — manual receipt/invoice entry MUST work).** Manually adding a ticket from the scan modal produces NO result in the DB. ROOT-CAUSE HYPOTHESIS (Planner, measured): `TicketCaptureModal.handleSave` creates the page with HARDCODED bare ids — `createPageServerFirst('db-expenses', …)` (L261) / `createPageServerFirst('db-tickets', …)` (L274) — instead of the tenant-resolved id (`targetDatabaseId` is already a prop; use it / `resolveDbId`). On multi-tenant the real DB is `db-tickets-{suffix}` → the record lands on a non-matching base id and never shows in the tenant's grid. (SCHEMA-1a's server-side resolution MAY mask this — UNVERIFIED.) **FIX:** use the resolved `targetDatabaseId` in `handleSave`, not the hardcoded name. **VERIFY:** as a tenant, add a receipt AND a purchase invoice manually → both appear in their grids. **Florin: TEST this on the live app first — it may already work via SCHEMA-1a; if not, this fix.**
- **SCAN-2** 🩹 **Can't upload PDF receipts on desktop — ROOT CAUSE FOUND (Planner 2026-06-09): client-side Tesseract OCR gate chokes on PDFs (FREE tier).** `TicketCaptureModal.tsx` `runScan` (L133-156): on FREE tier it runs `recognizeReceipt` (client Tesseract, `@/lib/ocr`) on the file BEFORE the server call — but **Tesseract reads images only, not PDFs** → a PDF throws (L151) → catch sets error + **`return` (L154), so `/api/scan` is NEVER called** — even though the server route HANDLES PDFs (pdfjs text-extract + first-page render fallback, `api/scan/route.ts` L63-126). Images work; Pro works (skips client OCR). **FIX (two changes in `runScan`):** (1) **Gate client OCR to images only:** `if (isFree && file.type.startsWith('image/'))` — PDFs/non-images bypass Tesseract and go straight to `/api/scan` (server handles them). (2) **On client-OCR failure, fall THROUGH to the server scan** instead of `return` (don't let a Tesseract hiccup abort the upload — server is the fallback). **VERIFY:** as a FREE tenant on desktop, upload a PDF receipt → it scans via the server (pdfjs), pre-fills the form, no Acrobat hand-off / dead modal; image upload still works.
- **SCAN-3** Receipts: remove details view (GRID-6); in the modal — remove journal entry, ADD project-connection property, and replace the journal section with an **image/file component** that displays the receipt (img/pdf/etc.). (was B2 + Florin financials)
- **SCAN-4** Store the ORIGINAL receipt file (not just extracted data) — see SCAN-3 display depends on it. (was B2 core)

## SEND — Send invoice/quote (v0.4, happy-path)
- **SEND-1** 🩹 "Send to client" says no email though email exists — resolve email column by TYPE not name (locale-fragile). (was B5)
- **SEND-4** 🩹 **No tenant notification when a client signs a quote online — follow-up loop incomplete (Florin 2026-06-09).** MEASURED: the signature flow EXISTS and works — public viewer `/quote/[id]` captures draw/type/upload signature → `accept-quote.ts` sets `status:"ACCEPTED"` + `clientSignature/signatureMethod/consentName/signedAt` + auto-creates the project (`autoCreateProjectFromQuote`). BUT `acceptQuotation` sends **NO email** — tenant is never told their quote was signed. **FIX:** in `accept-quote.ts`, after the status update, send a Resend notification to **`tenant.email`** ("Quote {title} accepted & signed by {consentName} on {date}", link to the quote/new project). Source tenant email via `quote.database.tenantId` → tenant record (already fetched at L44-47 for the project step). Skip gracefully if no email. Optionally also send the client a confirmation copy. **ALSO (verify, likely bug):** the viewer writes the RAW string `status:"ACCEPTED"` but the system uses canonical option IDs elsewhere (code checks BOTH `'ACCEPTED'` and `'opt-accepted'`, L25) → the admin quotation grid may not show the quote as Accepted (status-ID-vs-string mismatch, SCHEMA-2 class). Write the canonical `opt-accepted` (or map on read) so the signed quote displays correctly. **VERIFY:** sign a quote as the client → tenant gets the email; the quote shows Accepted in the admin grid; the project was created.
- **SEND-3** 🩹 **No self-copy + no reply-to on sent quotes/invoices (Florin 2026-06-08).** MEASURED: `send-quote.ts` + `send-invoice.ts` set only `from: "${company} <noreply@coral-group.be>"` + `to: [clientEmail]` — NO `cc`, NO `bcc`, NO `replyTo`. So the tenant keeps no copy/record of what they sent, and a client hitting Reply lands on Coral's no-reply void (replies lost). Multi-tenant impact: every tenant sends from Coral's noreply with no path back to them. **FIX (both actions):** (1) add **`bcc: [tenant.email]`** (BCC not CC — client must not see the internal address) so the tenant gets a copy/delivery confirmation; (2) add **`replyTo: tenant.email`** so client replies reach the actual business. Source `tenant.email` from the profile (pass it into the action, same place `companyName` is already passed). Skip BCC/replyTo gracefully if `tenant.email` is empty. **VERIFY:** send a quote → tenant receives a BCC copy; reply to the received client email → goes to tenant.email, not noreply.
- **SEND-2** 🩹 **Credit notes sent over Peppol as INVOICES, not credit notes → `PEPPOL_INTERNAL_ERROR`.** Planner-VALIDATED coder plan (OK to proceed). ROOT CAUSES (both confirmed in code): (1) `ClientInvoiceEngine.tsx` `handleSendPeppol` (~L753 body) OMITS `isCreditNote` + `parentInvoiceId` → backend defaults `undefined` → builds doc as INVOICE. **Backend is ALREADY correct** (`api/peppol/send/route.ts`: L171 `document_type: isCreditNote?'CREDIT_NOTE':'INVOICE'`, L261 UBL `type: isCreditNote?'381':'380'`, L153 parent ref) — it just never gets the flag. FIX = pass the two fields in the body. (2) L332 reads `validateData` (`.json()`) BEFORE the `.ok` check → non-JSON error page throws → generic `PEPPOL_INTERNAL_ERROR`; guard the 3 `.json()` calls with try/catch→`.text()` fallback. **EVIDENCE:** Coral outbox (peppol debug 2026-06-07) shows every `CN-2026-xxx` as `document_type:"INVOICE"` — proves the bug. **⚠️ REAL-WORLD IMPACT:** the ~6 credit notes already sent to Reno-K went out AS INVOICES — recipient may book them as PAYABLES not credits → re-issue as proper credit notes after fix + confirm with Reno-K. **PLANNER ADDITIONS to coder plan:** (a) verify the UBL BillingReference uses the parent invoice NUMBER (e.g. "2026-19"), not the internal doc id; (b) verification must SEND a CN and confirm via `/api/peppol/inbox?debug=1` that outbox now shows `document_type: CREDIT_NOTE` (not just that the flag propagates).

## NOTIF — In-app notifications: persistent toast + bell menu (NEW, v0.4 toast / v0.5 bell)
**Scope:** invoice + quote events first. **SERVER-PERSISTED** (state survives reload + cross-device — a toast must reappear until the user actually checks the item). Toasts via the EXISTING `sonner` `<Toaster>` (already mounted `AdminLayout.tsx` L565, top-right). Bell lives in the `AdminLayout` header (L385 region). NO notification model/API exists yet — build foundation first. Coder: follow exactly; one NOTIF-x = one commit; `/pd` measure + tsc/lint green each step.

- **NOTIF-0 — PREREQUISITE (verify/patch first).** Confirm `session.user.id` is populated. `auth.ts` session callback (L218-224) sets role/tenantId/etc. but NOT `id` — add `(session.user as any).id = token.sub` (or equivalent) so the API's user-guard + mark-read scoping work. Without this, NOTIF user-scoping is broken. (For v1 we emit tenant-wide `userId=null`, but the read-guard still needs `session.user.id`.)
- **NOTIF-1 — Data model + API (foundation, build first).**
  - Prisma `Notification` model: `id String @id @default(cuid())`, `tenantId String` (FK→Tenant, **@index**), `userId String?` (target user; `null` = all users of the tenant / owner), `type String` (one of: `QUOTE_ACCEPTED`,`QUOTE_VIEWED`,`INVOICE_PAID`,`INVOICE_OVERDUE`,`INVOICE_SENT`,`CREDIT_NOTE`,`PEPPOL_RECEIVED`), `title String`, `body String`, `entityType String` (`quote`|`invoice`|`project`), `entityId String` (**@index** — the GlobalPage/record id the notif concerns), `href String` (deep link, e.g. `/admin/financials/income/invoices/{id}`), `readAt DateTime?`, `createdAt DateTime @default(now())`. Composite index `@@index([tenantId, userId, readAt, createdAt])`. Migration required.
  - Central helper `src/lib/notifications.ts` → `createNotification({tenantId, userId?, type, title, body, entityType, entityId, href})`. SINGLE insertion point — every emitter calls only this (never `prisma.notification.create` inline).
  - API (all session-guarded, tenant-scoped, NEVER cross-tenant; match `tenantId` AND (`userId === session.user.id` OR `userId === null`)):
    - `GET /api/notifications?status=unread|all&cursor=` → `{ items, unreadCount, nextCursor }`, newest-first, page size 20.
    - `POST /api/notifications/mark-read` body `{ ids?: string[], entityId?: string, all?: boolean }` → sets `readAt=now()`. **`entityId` form is the "checked the concerned item" trigger** (marks ALL notifs for that item read). All three forms supported.
  - Acceptance: create→list→mark-read round-trips; cross-tenant request returns nothing.

- **NOTIF-2 — Event emitters (invoice + quote).** Emit `userId=null` (tenant-wide — all tenant users see it; DO NOT resolve owner now). Call `createNotification` at each REAL state change (not on every save). **There is NO single invoice-status chokepoint — emit at BOTH transition sites (don't miss the manual one):**
  - `accept-quote.ts` after status update → `QUOTE_ACCEPTED`, `href`=quote (in-app twin of SEND-4's email; both fire here).
  - **Auto** status path `src/app/actions/pages.ts` (~L326 sets `newStatus='opt-paid'` from payment matching) → `INVOICE_PAID`.
  - **Manual** status path = the engine status-select (FIN-5, `ClientInvoiceEngine`) / its server write → `INVOICE_PAID`/`INVOICE_SENT`/`INVOICE_OVERDUE` as set; CN creation (`handleCreateCreditNote`) → `CREDIT_NOTE`.
  - `lib/dunning.ts` → `INVOICE_OVERDUE` when an invoice crosses due.
  - **STRONGLY PREFERRED:** centralize invoice-status writes into ONE helper and emit there, so both auto+manual paths share a single emit site. If not centralizing, emit at every site above — missing the manual path is a fail.
  - **Idempotency MANDATORY:** never emit twice for the same `(entityId, type)` — guard by comparing previous vs new status before emitting (no notif if status didn't actually change).

- **NOTIF-3 — Persistent toast: on app-open AND on status-change (the core behavior Florin specified).** Add a client component `NotificationWatcher` mounted once in `AdminLayout`.
  - **On app open (mount):** `GET /api/notifications?status=unread`. For EACH unread, fire a `sonner` toast with **`duration: Infinity`** (must NOT auto-dismiss), **`id: notification.id`** (dedupe — re-mounts/HMR must not stack duplicates), and an action button **"View"** → navigates to `href`, calls `mark-read {ids:[id]}`, dismisses. The toast's **close (×) dismisses WITHOUT marking read** (so it reappears next app-open until the item is genuinely checked).
  - **On status-change while app is open:** the watcher POLLS `GET /api/notifications?status=unread` every **30s** AND on window `focus`; any unread id not already shown this session fires the same persistent toast. (Polling now; SSE/Realtime = noted future upgrade, do NOT build now.)
  - **"Persists until the user checks the concerned item" — exact semantics:** a notif clears ONLY when its `entityId` is opened. Implement by having each invoice/quote detail page call `POST /api/notifications/mark-read {entityId}` on mount → server unread drops → toast won't refire on next open + bell badge decrements. Clicking "View" also clears. Dismissing the toast does NOT clear. Reloading the app with the item still unchecked → toast fires again.
  - Acceptance: trigger a status change → toast appears and stays; reload → still there; open the item → gone permanently.

- **NOTIF-4 — Bell menu + history (development plan; build AFTER NOTIF-1..3 verified).**
  - Bell icon button in the `AdminLayout` header (L385 region) with an **unread-count badge** (from `unreadCount`); 0 → no badge.
  - Dropdown panel: full notification HISTORY (read + unread), newest-first, infinite-scroll/paginated via `cursor` (`status=all`). Unread visually distinct (dot + subtle bg). Each row: type icon, title, body, relative time; click → navigate to `href` + mark that one read. Panel header action: **"Mark all read"** (`mark-read {all:true}`). History is NOT auto-deleted — user consults at own discretion (retention policy = later).
  - Mobile: surface the bell/badge in the mobile shell (`MobileShell`) too.
  - Acceptance: bell badge matches unread count; opening history + mark-all-read zeroes the badge; clicking a row deep-links correctly.
  - **Cross-refs:** SEND-4 (email channel for QUOTE_ACCEPTED — NOTIF is the in-app channel, same trigger), `lib/dunning.ts` (overdue), FIN status changes, PROFILE (owner/user scoping).

## PEPPOL — Receive (v0.4, happy-path; partly provider-side)
- **PEPPOL-2** ✅ RESOLVED 2026-06-04 (e-invoice.be support reply): Coral = approved RESELLER. Credits at ORG level, post-pay NET-10, **no per-tenant activation/fee, no per-tenant credit pack**. Reseller-created tenants are billed via the org account → **NOT subject to the pre-paid "blocked until payment" gate → they can RECEIVE from go-live.** 1 credit per Peppol doc BOTH directions; tiered €0.18→€0.05 by total monthly volume across all tenants. Per-tenant balance via `GET https://api.e-invoice.be/api/stats`. (was E1 — closed.)
- **PEPPOL-1** 🟢→ provider-handled: the `smp_registration: false` gate was the regular pre-paid model, which does NOT apply to reseller tenants. e-invoice.be is re-triggering Coral's SMP server-side. **Florin action:** confirm to support that Coral is the tenant to push first (done on ticket). **Verify:** once they confirm, (a) Coral shows `smp_registration: true`, (b) a real inbound doc lands in Coral's inbox, (c) a freshly reseller-provisioned test tenant comes up receive-enabled with NO gate. Then ✅. (Downgraded from blocker — structural cause resolved by provider.)

- **PEPPOL-3** 🩹 **Inbound docs not reaching ERP — ACCOUNT-SPLIT ROUTING (not a code bug).** Two separate e-invoice identities for the same company: (1) ORIGINAL direct account (self-signup) where Peppol ID `0208:1018865828` is SMP-registered / where inbound lands; (2) RESELLER tenant (`cmneyas2b0000veqvkgl2luz1`, key `api-454s…qj8b`) that the ERP reads. NOT linked → inbound (incl. yesterday's test invoice) lands in the original; ERP polls reseller inbox → empty. **CODE CORRECT.** ✅ CONFIRMED via `/api/peppol/inbox?debug=1` (2026-06-07): inbox/invoices/creditNotes all `total:0`, `raw_outbox total:7` (sending works, key valid) → inbound not routed to reseller tenant. **⚠️ DO NOT authorize support's "delete original account" until Peppol ID is re-registered to the RESELLER tenant** — deleting while original holds the SMP reg = inbound BOUNCES. **SAFE SEQUENCE:** support migrates/registers `0208:1018865828` to reseller tenant (smp_registration:true) → send test → verify in ERP → THEN delete original. Attach the debug output (empty inbox + 7-doc outbox) as evidence. [re-added after concurrent-write clobber 2026-06-07]
  - **IDENTIFIERS (confirmed live via `/api/peppol/onboard` 2026-06-08):** `eInvoiceTenantId: ten-gar163w6fd093s8w` · `peppolId: 0208:1018865828` (VAT BE1018865828) · company "BV CORAL ENTERPRISES". (NOT our Prisma id `cmneyas2b0000veqvkgl2luz1` — useless to e-invoice.)
  - **✅ PROVIDER ACTED — CORRECT STEPS (e-invoice reply 2026-06-12, Planner-verified vs expected sequence):** (1) production tenant `ten-gar163w6fd093s8w` (= the ERP's `eInvoiceTenantId`) now RECEIVES for `0208:1018865828`; (2) they REMOVED `0208:1018865828` from the original direct account ("your company in the management app" — the stale one) → duplicate/conflicting registration cleared; (3) a test tenant `ten-dh674tl9adf4dz5j` is also configured for the ID but test tenants can't receive (FYI, harmless). This is exactly the migrate-to-reseller-then-remove-original sequence we specified. **REMAINING = Florin verify with a REAL inbound send → `/api/peppol/inbox?debug=1` inbox `total > 0` → then ✅.** Then optionally delete the now-de-registered original account. If inbound STILL fails, re-check the prod-vs-test dual-config.
  - **REFINED DIAGNOSIS (2026-06-08): DUPLICATE/CONFLICTING REGISTRATION, not a simple split.** `/api/peppol/onboard` shows the RESELLER tenant ALREADY `peppolRegistered: true`, `peppolStatus: "ACTIVE (External SMP)"`. So reseller IS registered+active, yet inbox empty → the Peppol ID is likely registered on TWO SMPs (stale original account's AP + reseller's external SMP) and the network root pointer (SML) for `0208:1018865828` still resolves to the ORIGINAL account's access point → inbound routes there. **Cleanup must fix ROUTING, not just delete a row:** ensure SML/SMP for `0208:1018865828` points to the AP serving `ten-gar163w6fd093s8w`, remove the conflicting original registration, test delivery before/after. **Florin is transmitting this to e-invoice support 2026-06-08.** Safe order unchanged: confirm reseller owns the SML pointer → test inbound lands in ERP → THEN delete original.
- **PEPPOL-4** 🔴 **REOPENED — committed (PEPPOL-4 on main) but BROKEN IN PRACTICE (Florin 2026-06-17: 2 new invoices show "untitled" supplier, no original doc, sparse data). Deployment ruled out (main=develop). TWO confirmed root causes:**
  - **(A) Supplier "untitled" = WRONG STORE.** The purchase-invoice supplier relation targets the **`db-suppliers` GlobalDatabase** (`relationDatabaseId: 'db-suppliers'`), but `peppol/inbox/route.ts` auto-create does `prisma.supplier.create(...)` → a row in the **`Supplier` Prisma TABLE** → links the invoice to a Supplier-table id the relation CAN'T resolve → "untitled." Same mismatch on the VAT pre-match (`prisma.supplier.findMany`). **FIX:** match + auto-create the supplier as a **`db-suppliers` GlobalPage** (resolve via `getLockedDbId('db-suppliers')` + `createPage`, set name/VAT/address as its properties), and link the invoice relation to THAT page id. Stop using the `Supplier` table for this relation.
  - **(B) Original document silently skipped.** PDF fetch is gated `if (tenant.driveFolderId && process.env.GOOGLE_CLIENT_ID)` (L282) → if the tenant has no `driveFolderId` (or env missing), no original is stored, no error. **FIX:** don't silently skip — store the fetched PDF regardless (Blob once FILES lands; until then ensure Drive folder exists or log loudly). Drive dependency is the fragility FILES-Blob removes.
  - **(C) "sparse data" — VERIFY:** look at an actual record (`/api/peppol/inbox?debug=1` or DB) to see if totals/lines are empty because the e-invoice `DocumentResponse` for those docs is sparse (parse issue) vs the mapping. Don't assume.
  - **This means PEPPOL-4's earlier "complete" was code-present, not working. Re-verify on a REAL inbound from a new supplier: supplier resolves to a named db-suppliers page; original doc stored+viewable; totals/lines populated.**
  -- (original gap list, still valid for the rewrite below) --
  **Inbound invoices now arrive ✅ but the record MAPPING is incomplete (Florin 2026-06-15).** MEASURED in `api/peppol/inbox/route.ts` `globalPage.create` (L264-289): three gaps + a view gap.
  1. **Supplier relation empty for new suppliers.** `supplier: [matchedSupplierId]` is filled ONLY if `matchSupplier(vendorVat)` finds an EXISTING supplier contact by VAT (L187). New supplier → no match → empty relation (the `supplierName`/`supplierVat` TEXT is set, but nothing is linked). **FIX:** when no VAT match, **auto-create the supplier contact** from `vendor_name`/`vendor_tax_id`/`vendor_address` and link it (so the relation fills). Idempotent on VAT (don't create dupes — ties to FIN-10 identity rule).
  2. **No original document stored.** Record keeps only `peppolDocId` + parsed fields; `blocks: []`, no attachment → "I don't have the document." **FIX:** fetch the source document via `getInboxDocument(apiKey, doc.id)` (already in `e-invoice-inbox.ts`) and STORE it on the record (PDF/UBL), viewable in the detail view. Ties to **FILES (Blob)** + SCAN-4 (store-the-original).
  3. **Line items dumped, not structured.** Lines stored as `invoiceLines: JSON.stringify(...)` (a string property) + first line's text shoved into `betreft` (the subject) → "items go in the description." **FIX:** write the parsed `lines` as **structured line-item blocks** (the same `Block[]` the expense/invoice engine renders) so they show as proper rows; stop putting line text in `betreft`.
  4. **VIEW: extend GRID-10 to `db-expenses`.** Inbound invoices open in the generic `PageModal` (same fragmentation as projects) → make the type-aware modal route purchase-invoice/expense records to the proper `PurchaseInvoiceEngine` view, not the generic property dump. (Florin's hope is correct — GRID-10 is part of this, but it must cover `db-expenses` too.)
  5. **PDF/download must serve the ORIGINAL document, not a regenerated stub.** CONFIRMED via Florin's download (`purchase-invoice-2218404561.pdf`, 2026-06-15): the modal's "PDF" button generated a CoralOS doc with `FROM (SUPPLIER): —` (empty), `No line item detail available`, buyer=Coral — i.e. a sparse regenerated stub, "not a valid financial document." The download/view must serve the **stored source document** (UBL-rendered / the supplier's actual PDF from gap #2), never a regenerated stub.
  **VERIFY:** receive a Peppol invoice from a NEW supplier → supplier contact auto-created + linked (FROM no longer empty); the ORIGINAL document is attached + rendered (right pane) + downloadable as the source; line items render as structured ORDERLIJNEN rows (not a JSON blob / betreft / "no line item detail"); the record opens in the split-pane purchase view (GRID-10), not the generic modal.
- **PEPPOL-5** 🩸 **BACKFILL — re-hydrate ALREADY-received inbound invoices (Florin 2026-06-17).** Invoices received BEFORE the AGG-2 mapping fix are missing supplier link / original document / structured line blocks. The data is STILL re-fetchable: each record carries `peppolDocId`, and the tenant's `eInvoiceApiKey` + `getInboxDocument`/`getInboxDocumentPdf` re-pull the source — no need for the senders to resend. **Operation (treat like SCHEMA-1b — dry-run → Florin-approve → execute, idempotent):**
  - Scope: existing `db-expenses` records with `source: 'src-peppol'` (or `peppolDocId` present) that are MISSING data (empty `supplier` relation OR no `receiptUrl` OR empty `blocks`). Per tenant, using that tenant's key.
  - For each: re-fetch by `peppolDocId` → re-run the AGG-2 mapping in UPDATE mode: (a) match/auto-create + link supplier; (b) fetch the original PDF → store → set `receiptUrl`; (c) rebuild structured `financial-row` blocks from the lines.
  - **Idempotent + non-destructive:** fill only EMPTY fields — NEVER overwrite user edits (manual supplier/category/notes). Skip records already complete. Re-runnable.
  - **No double credit-count:** do NOT call `incrementPeppolReceived` (these were already counted); re-fetching a received doc must not consume a new credit — VERIFY with e-invoice that `getInboxDocument` on an already-received doc is free.
  - Delivery: an admin/superadmin script or a guarded API route (not auto). **Dry-run output:** per-tenant count of records to backfill + sample. **VERIFY:** after run, previously-received invoices show supplier + original document + ORDERLIJNEN; no credits consumed; user-edited fields untouched. (SUPERVISED — data op.)

## INBOX — Email (v0.5)
- **INBOX-1** 🩹 Inbox doesn't download mail though accounts are connected. **ROOT CAUSE FOUND (Planner 2026-06): OAuth scope mismatch.** The connect flow (`api/email/connect/google/route.ts`) requests Gmail **API** scopes — `gmail.readonly`, `gmail.modify`, `gmail.send`, `userinfo.*` — but the sync engine (`api/email/route.ts`) connects over **IMAP via XOAUTH2** (`imapflow`). Gmail IMAP requires the **`https://mail.google.com/`** scope, which is NOT requested. So OAuth succeeds + the account is stored + redirect says `connected=true` ("seemingly connected"), but every `client.connect()` is rejected for missing scope → 500 "Failed to sync with IMAP provider." **TWO FIXES — Florin decides (this is a launch-relevant fork):**
  - **(A) Switch Gmail sync to the Gmail REST API** (recommended). Use `googleapis` gmail.users.messages.list/get instead of imapflow for Gmail accounts — aligns with the scopes ALREADY granted, no restricted-scope verification. `gmail.readonly` is "sensitive" (lighter Google review) vs `mail.google.com` which is "restricted" (full CASA security audit, slow/costly). Keeps imapflow only for true generic-IMAP providers.
  - **(B) Add `https://mail.google.com/` to the connect scopes** (quick, keeps imapflow). BUT it's a RESTRICTED scope → Google requires a security assessment before production; until verified you're capped at 100 users + "unverified app" screen. Fine for your own/test accounts, a launch blocker at scale.
  → **Recommendation: (A)** for Gmail to dodge restricted-scope verification; design a provider adapter (Gmail API / Graph / generic-IMAP) so INBOX-2 multi-account is clean. **MEASURE first:** add the real IMAP error to the response/log to confirm the auth-scope rejection before changing.
  - Secondary (note, not the bug): OAuth `state` is the raw tenantId (no CSRF token); `connectedEmailAccount` upsert is keyed by `email` GLOBALLY, not per-tenant (two tenants, same address = collision). Tidy when touching this.
- **INBOX-2** All-inboxes unified view.
- **INBOX-3** Link a mail to sales/quote/project; a mail linked to a sales-pipeline item propagates to the connected quotation and onward to the project (cascade linking).

## CAL — Calendar (v0.5)
- **CAL-1** 🩹 UI wrong — white/invisible buttons.
- **CAL-2** 🩹 "Failed to create event" — connection error; fix.
- **CAL-3** Local calendar aggregates everything in the ERP that has dates; assignable to external calendars; manual event creation in external calendars.
- **CAL-4** ⬜ External calendar connection (Google/Outlook/Apple) missing from settings — add. **MEASURED STATE (Planner 2026-06): calendar is GOOGLE-ONLY.** All routes (`api/calendar/accounts|sync|events`) hardcode `provider:'google'` + hit `googleapis.com/calendar/v3`; the data model is Google-coupled (`googleEventId`, `googleCalendarId` columns). Outlook + Apple are NOT built. **To add them, fundamental first:** make the event model provider-agnostic — rename/add `provider` + `externalEventId` + `externalCalendarId` (migrate existing `googleEventId`→`externalEventId` w/ `provider='google'`), then add adapters: **Outlook = Microsoft Graph** (`/me/events`, MSAL OAuth) ; **Apple = CalDAV** (app-specific password; no OAuth) . Don't bolt provider #2 onto Google-named columns — that's the recurring "identity vs name" trap at the schema level. Settings UI then lists connected accounts per provider (mirror the email account-connect pattern).
- **CAL-5** Mobile calendar unusable — responsive toolbar, sensible mobile view, reachable actions. (was M5) **MEASURED: there is NO mobile calendar surface** — `src/app/[locale]/m/` has clients/expenses/invoices/purchases/quotes/settings but NO calendar, so mobile falls back to the desktop `CalendarModule` (the "mess"). Build a dedicated `m/calendar` view (agenda/day list, thumb-reachable add, no desktop grid).

## BANK — Bank connection / transaction feed (v0.5 feature; research done)
- **BANK-1** 📊 **RESEARCH (Planner 2026-06).** GOAL: pull bank transactions (AIS) into `db-payments-in`, then **auto-match payment→invoice by structured communication / OGM** (matching mechanism already designed in history). Read (AIS) first; payment initiation (PIS, pay suppliers) much later.
  - **KEY FEASIBILITY WIN:** use a LICENSED aggregator → THEY are the regulated AISP, so **CoralOS does NOT need its own PSD2/AISP licence.** That removes the biggest barrier.
  - **RECOMMENDED: Ponto (Isabel Group / Ibanity)** — Belgian-native (Isabel = the country's banking-rails incumbent), 1,800+ EU banks, **specializes in BUSINESS/corporate accounts** (most aggregators are retail-first — decisive for a B2B construction SaaS), AIS + PIS, **Partner-Paying pricing** (bill into your SaaS margin, volume-based, not public — get a quote), free sandbox + good docs/Slack. Best strategic + geographic fit.
  - **ALTERNATIVE (MVP/cost): GoCardless Bank Account Data** (ex-Nordigen) — free AIS tier, 2,200+ EU banks, fast to prototype; more retail-oriented, rate-limited free tier. Good for a cheap proof-of-concept.
  - **ADJACENT (accountant flow, not live API): CodaBox** — Belgian CODA/SODA delivery via fiduciaries; fits the accountant-export side, not real-time feed. Note for later.
  - **PSD2 constraints to design for (all aggregators):** 90-day SCA re-consent → build a reconnect/renew-consent UX (don't assume a connection lasts forever); per-tenant connection + token storage (same adapter pattern as email/calendar); read-only scope first.
  - **ARCHITECTURE:** provider adapter → normalize transactions → upsert into `db-payments-in` (idempotent on bank txn id) → OGM/structured-comm exact-match to invoice (identity match, NOT substring — recurring rule). Reuse the FILES-ISO-style per-tenant isolation.
  - **VERDICT:** feasible, clean via aggregator, but it's a **v0.5 feature with onboarding lead time** (sandbox immediately; production needs business verification with Ponto). RECOMMEND: open a Ponto sandbox to prototype the AIS→OGM-match loop in parallel while the free-tier launch proceeds — don't block launch on it. **Florin decision needed:** Ponto (strategic, paid) vs GoCardless (cheap MVP) for the first spike.
- **BANK-2** 📊 **PONTO DEEP-DIVE — implementation + costs (Planner 2026-06).**
  - **AUTH/INTEGRATION (Ibanity Ponto Connect):** OAuth2 **authorization-code + PKCE** for the user's initial bank-account link → then **refresh-token flow**. Access token = 30 min; refresh token valid 90 days but **refreshable indefinitely** (refresh before expiry → connection persists; if a tenant goes >90 days idle, re-consent). Scopes: **`ai`** (account info) or **`ai pi`** (+ payment initiation later). Per-tenant token storage (same adapter pattern as email/calendar).
  - **⚠️ IMPLEMENTATION GOTCHA (matters for our Vercel/Node stack):** Ibanity requires **mutual TLS (client certificate)** on EVERY server-to-server call. In Node = an `https.Agent({cert,key,ca})` (or undici dispatcher) built from a cert Ibanity issues you. On Vercel serverless: store cert+key as base64 env vars, build the agent per request (cold-start cost). Ibanity provides the application/eIDAS-level certs themselves (they're the licensed AISP) — so we do NOT chase PSD2 QWAC/QSEAL certs ourselves. SDKs exist (.NET confirmed; Node/others available) — or call REST directly with the mTLS agent. Sandbox + webhooks (transaction notifications) + open Slack support, all free.
  - **GO-LIVE:** sandbox is immediate + free; production = a "Go Live" verification with Ibanity/Ponto (business onboarding) — the lead-time item.
  - **COSTS:** integration is FREE; you pay only usage. **AIS ≈ €4/month per linked bank account**, **tiered down as account count grows**. PIS (payment initiation) billed **per executed payment** (later). Two billing models: **Partner-paying** (Ponto bills CoralOS, tiered; we mark it up / fold into a plan — more invoicing work) or **Customer-paying** (Ponto bills the tenant directly — ZERO invoicing work for us, cleanest for a SaaS). Ponto = payment institution licensed by the National Bank of Belgium.
  - **PRICING-FIT TAKE:** at ~€4/acct/mo, bank-feed is a natural **paid-tier feature** (one account per self-employed tenant ≈ €4 cost → price it inside a PRO add-on, or use Customer-paying so it never hits our P&L). Get a partner quote for the tiered rate before committing. [refs: myponto.com/en/pricing/partner-paying-model, documentation.ibanity.com/ponto-connect]

## TASKS — Tasks module (v0.5)
- **TASKS-1** 🩹 Side panel doesn't scroll; if window short, Save button unreachable. Fix scroll/sticky save.
- **TASKS-2** Make Tasks a mobile-first module.

## CONTACTS / SUPPLIERS / LIBRARY (v0.2, mostly GRID-6)
- **CS-1** Contacts: remove details view (GRID-6).
- **CS-2** Suppliers: remove details view (GRID-6).
- **CS-3** Articles: remove details view (GRID-6).
- **CS-4** Bestek details: remove stats + journal cards; connected-properties → **connected articles**; add a **pricing table** (a miniature quotations engine); properties stay in left column + populate DB.

## TT — Time-Tracker CONSOLIDATION (🔒 SUPERVISED — Florin 2026-06-17: do this FIRST, before mobile)
**MEASURED (Planner): the time-tracker is a half-migrated PARALLEL APP embedded in CoralOS, not a native module.** It carries its own duplicate `components/ui/*` (full shadcn set), own `i18n/` + `locales`, own `contexts/` (ThemeContext + a Supabase `AuthContext`), own `pages/`, and an `integrations/supabase` data layer. **SPLIT-BRAIN DATA:** part of it reads/writes **Supabase** (a SECOND database — announcements, documents, TimesheetView, LateEntry, ApprovalManager, UserDetailView, auth) while other hooks (`useClockEntries`, `useApprovalRequests`, `useScheduleAttachments`) use the app's Prisma/Neon API. Consolidation = fully absorb it onto CoralOS's stack and retire the parallel infra. **Phased, each phase its own commit; Florin-gated.**
- **TT-0 — AUDIT (Planner-led, partly done).** Exhaustive map: every Supabase read/write (table → feature), every duplicate `ui/` component vs the app's, the parallel i18n/theme/auth, and — CRITICAL — whether the Supabase data is **tenant-scoped** (if TT data has no `tenantId` isolation, it's a cross-tenant data-leak risk; treat as launch-blocking before TT ships to tenants). Output = migration map + isolation verdict.
- **TT-1 — DATA off Supabase → Prisma/Neon (the core).** Per feature (announcements, documents, timesheets, late entries, approvals, user detail), move storage to the app's Prisma schema + API routes; ensure tenant scoping. Then delete `integrations/supabase`. Kills the second database + the data fragmentation. Highest value, do per-feature.
- **TT-2 — AUTH.** Replace the Supabase `AuthContext` with the app's NextAuth session/tenant. One auth, tenant-scoped.
- **TT-3 — UI.** Replace `time-tracker/components/ui/*` with the app's shared components; delete the duplicate set.
- **TT-4 — i18n + THEME.** Fold its `i18n/locales` into next-intl (NL/FR/EN/RO) and its ThemeContext into the app theme.
- **TT-5 — Make it a tenant-scoped MODULE** behaving like the rest (gating via `PLAN_MODULES`, lockedDbIds where relevant). **VERIFY:** time-tracker runs entirely on CoralOS auth/data/UI; no Supabase; data tenant-isolated; HR timesheets/schedule/leave all work.

## MOBILE — App-wide mobile-friendliness (🔒 SUPERVISED — Florin: AFTER time-tracker consolidation, v0.4)
**MEASURED: mobile coverage is PARTIAL + inconsistent.** `/m` has only `clients, settings, purchases, invoices, quotes, expenses` — MISSING dashboard, projects, tasks, calendar, contacts, suppliers, library, inbox, files, time-tracker. So most modules fall back to desktop layouts on phone ("topsy turvy"). **FIRST DECISION (Florin's call — I'll recommend): strategy.** (A) keep building per-module `/m` routes (current path — duplicated UI, drifts) OR (B) **make the main app responsive** (one codebase, responsive layouts + a mobile shell/nav) — fewer surfaces, no drift, the modern default. **Recommend B**, with `/m` kept only where a genuinely different mobile flow is needed (capture/scan). Then a per-module pass.
**⚠️ PROTECT THE EXISTING `/m/` FREE-TIER SURFACE (Florin 2026-06-19, binding).** `/m/*` (own `MobileShell`, reuses the shared engines → already inherits FIN-NUM/INV-DUEDATE/SELECT-UNIFY/INV-PDF-POLISH) is in DECENT shape. DO NOT regress it. Any change to a SHARED component must be mobile-first (add width, never break `/m/`). After ANY MOBILE work, VERIFY the `/m/` pages (invoices, quotes, clients, expenses, purchases, settings) still look + work.
- **MOB-1** 🟡 PARTIAL (coder `8d5d31c`: manifest-mobile scope `/m` + a weak client-side AdminLayout redirect). **STILL the waitlisted problem (Florin 2026-06-20: "mobile routing still has the same problem").** See MOB-ROUTE for the real fix.
- **🟠 MOB-ROUTE — FREE mobile lands in the wrong place (desktop/WORKHUB) instead of `/m`. The proper server-side fix for the long-waitlisted routing bug. ⚠️ RE-VERIFY NEEDED: Florin's "still broken" report (2026-06-20) was from the CORAL account (FOUNDER/paid) — the redirect is `if (planType!=='FREE') return`, so paid tiers are intentionally NOT redirected; that symptom was expected. Florin is now re-testing on a FREE account. The server-side fix below is still valid for the GENUINE gaps regardless of that re-test (workhub branch has no redirect; PWA installed from desktop has start_url:/admin; client redirect flashes desktop) — but confirm the actual FREE behaviour before prioritising.** Why MOB-1 didn't solve it: (1) the only redirect is client-side in `AdminLayout` (`innerWidth<768 → router.replace('/m')`, L86-95) → it runs ONLY when AdminLayout mounts (APP subdomain), so it does NOTHING on the WORKHUB subdomain (where free mobile lands) + flashes desktop first; (2) the MOB-1 middleware change was only adding `manifest-mobile.json` to the matcher — NO routing; (3) the desktop `manifest.json` is served on `/admin`, so a PWA installed from desktop has `start_url:/admin` → never reaches `/m`. **`planType` IS in the JWT** (`auth.ts` L205/225) so middleware can gate. **FIX (server-side, in `middleware.ts`):** (a) detect mobile via `user-agent`; (b) read `token.planType`; (c) Branch B (app subdomain): if `planType==='FREE'` + mobile UA + path under `/admin` that HAS an `/m` equivalent (clients/expenses/invoices/quotes/purchases/settings/dashboard) + no `desktop-view` bypass cookie → `redirect` to the matching `/m` route (BEFORE render, no flash); (d) Branch W (workhub): FREE + mobile → redirect to `app` subdomain `/m`; (e) routes with NO `/m` equivalent fall through to the (responsive) desktop — do NOT redirect those. **Bypass:** MobileShell's "Desktop view" must set a `desktop-view` COOKIE (not just localStorage) so middleware honours it. Keep the AdminLayout redirect as a fallback. **VERIFY:** log in as a FREE tenant on a phone (and a phone PWA install) → lands in `/m`, NOT desktop/workhub; "Desktop view" escapes and sticks; PRO/ENT unaffected; desktop FREE unaffected. **Commit:** `MOB-ROUTE: server-side FREE-mobile routing to /m (app + workhub)`.
- **🔥 MOB-FREE — free-tier mobile bug batch (Florin 2026-06-19, testing a FREE account on mobile via Chrome devtools). This IS the launch surface → HIGH. All on `/m/*`.**
  - **MOB-FREE-1 — no scroll + no horizontal overflow.** `m/settings/page.tsx` has NO scroll container (overflow-y absent) → can't scroll, content cut off → add `overflow-y-auto` + proper height. App-wide: prevent ANY horizontal out-of-bounds — `overflow-x-hidden` on the mobile shell, everything STACKS when width exceeds screen (flex-col, flex-wrap, no fixed wide widths/min-widths). **Worst offenders: the invoice + quote engines go horizontally out of bounds on mobile — make their multi-pane/wide-row layouts stack vertically at mobile widths.** (Pairs with MOB-4 padding.) **Commit:** `MOB-FREE-1: settings scroll + no horizontal overflow (stack on mobile)`.
  - **MOB-FREE-2 — list items clickable + compact.** `m/clients` cards (L89 map) have tel/mailto links but no row onClick → add onClick to open a client DETAIL (mobile detail view/modal; create one if missing). `m/expenses` rows (L179) same → onClick to open the expense; AND make the list COMPACT: replace the `p-4 rounded-2xl border shadow-sm` cards with a tight `divide-y` list (small vertical padding, a thin divider line between items, minimal whitespace). **Commit:** `MOB-FREE-2: clickable + compact client/expense lists`.
  - **MOB-FREE-3 — invoice detail shows the client + alignment.** In `ClientInvoiceEngine` on mobile the client is no longer visible → restore the client display and fix the field alignments at mobile width. **Commit:** `MOB-FREE-3: invoice detail shows client + fix alignment (mobile)`.
  - **MOB-FREE-4 — `+` buttons icon-only.** Where a button shows a `+`/Plus icon AND a text label ("Add"/"Toevoegen"/"Ajouter"/"Nieuw"), drop the redundant text → icon-only (saves mobile width). **Commit:** `MOB-FREE-4: plus buttons icon-only`.
  - **👉 MODAL-HAZE — the open (record) modal is HAZY/unreadable on DESKTOP + mobile (Florin 2026-06-20; coder debugging live). SHARPENED ROOT (Florin's precise observation): on hover the text "descends ~1px and turns SHARP, then goes back up 1px and turns blurry" → this is a SUB-PIXEL RENDERING bug — the text is sitting on a HALF-PIXEL (fractional) boundary inside a GPU/compositing layer, so it renders blurry; the hover triggers a repaint that snaps it to an integer pixel (sharp), then it reverts. Caused by a compositing layer positioned at a fractional pixel — from a `transform` / `will-change` / `filter` / `backdrop-filter` on the panel OR an ancestor. SUSPECTS to check (in order): (1) residual `transform` left by the entry animation (`animate-in slide-in-from-right` / `zoom-in-95` at L599 — these libs can leave a non-`none` transform at rest → fractional position); (2) `will-change: transform` / `transform: translateZ(0)` forcing a layer; (3) `transform: translate(-50%,…)` centering on an odd-dimension element → `.5px`; (4) `backdrop-filter`/`filter` on an ancestor of the text. **FIX:** ensure the text-containing panel and its compositing ancestors rest on INTEGER pixels — `transform: none` at rest (clear animation residue / `will-change`), avoid fractional translate centering (use integer offsets or fl\`inset\` + even dimensions), drop `translateZ(0)`/`will-change` where not needed, AND remove the `backdrop-filter` overlays (below). **Confirm via DevTools:** toggling off the panel's `transform`/`filter` (and ancestors') makes text crisp without hover. Original backdrop-filter fix retained: ROOT ALSO involves the `backdrop-filter` repaint/compositing on the overlay/header.** Source: `PageModal` overlay `bg-black/20 dark:bg-black/60 backdrop-blur-sm` (L596) + translucent blurred header `bg-white/80 dark:bg-[#191919]/80 backdrop-blur-md` (L610); the panel (L599) ends up affected by the backdrop-filter compositing. **FIX (robust — `backdrop-filter` is the whole problem):** (a) REMOVE `backdrop-blur` from the modal OVERLAYS app-wide and use a solid dim instead (`bg-black/40`–`/50`, no blur) — eliminates the compositing artifact entirely; (b) make the sticky header SOLID (drop `/80` + `backdrop-blur-md` → `bg-white dark:bg-[#191919]`); (c) if the blur aesthetic is wanted, instead force the PANEL into its own GPU layer above the blur — `isolation: isolate` + `transform: translateZ(0)` + solid bg (but given the repaint bug, dropping the blur is the reliable choice). Apply to ALL modals using `backdrop-blur` (PageModal, TicketCaptureModal, mobile shell, etc.). **VERIFY on desktop Chrome + iOS Safari:** modal text crisp + stable (no haze, no hover-flicker); backdrop still dims the page. **Commit:** `MODAL-HAZE: drop backdrop-filter overlays (solid dim), crisp modal panel`.
  - **MODAL-LAYOUT — restructure the record (`PageModal`) into a vertical split (Florin 2026-06-20). Do AFTER MODAL-HAZE (fix readability first).** Today the body is ONE scroll column: Properties table (L647) + `BlockEditor` (L954) + "Connected Records" `LinkedRecords` (L969). **NEW layout (desktop):** two columns — **LEFT half = Properties** (the property table + BlockEditor blocks); **RIGHT half = stacked two quadrants:** TOP-RIGHT = **Connected databases** (`LinkedRecords` / relations), BOTTOM-RIGHT (quadrant under the connections) = **Journal entries** for this record. Each pane scrolls independently. **Journal quadrant** uses the existing record↔journal link (`JournalCard.linkedJournalEntries`, L138) → list the record's linked journal entries + add/open (ties to EDITOR-3; data path already exists). **RESPONSIVE:** on mobile collapse to a SINGLE column stacked (Properties → Connected databases → Journal) — no side-by-side at narrow widths. **VERIFY:** open a record → left = properties, right-top = connections, right-bottom = its journal entries; on mobile they stack; both engines/desktop look right. **Commit:** `MODAL-LAYOUT: split record modal — properties | connections + journal quadrant`.
  - **— MOB-FREE 2nd BATCH (Florin 2026-06-20, FREE tier, Chrome + Safari iOS) —**
  - **MOB-FREE-5 — engines break out of the mobile shell (BOTH quote + invoice; Florin 2026-06-20 reconfirmed the QUOTE engine still opens in the non-free/desktop app on mobile).** From `/m`, opening the quote OR invoice engine switches the bottom bar to the MAIN app, and the engine's **"Database" button** (+ other admin nav) routes to `/admin` (bottom bar replaced). **FIX:** when in mobile/free context, BOTH engines must keep the `MobileShell` bottom bar; HIDE/neutralise admin-only nav (the `Database` button + any `/admin` `router.push`) so nothing leaves `/m`. **VERIFY:** open quote AND invoice from `/m` → mobile bottom bar stays, no jump to the main app. **Commit:** `MOB-FREE-5: engines keep mobile shell, hide admin nav on /m`.
  - **👉 MOBILE-RECORD-MODAL — on mobile, records open the DESKTOP `PageModal` → "completely faulty" (Florin 2026-06-20). PER-TYPE routing matrix (Florin clarified — NOT all generic):**
    - **Contact/client** → generic `PageModal`, fine — just make it responsive (below).
    - **Invoice** → generic `PageModal` is OK, BUT under the Properties it must ALSO display the FILE/document so it can be consulted (show the invoice PDF/attachment in the modal, below properties).
    - **Expense** → must NOT use the generic `PageModal`; route to the DEDICATED expense detail (`PurchaseInvoiceEngine`) — mirror desktop AGG-1 routing. `m/expenses/page.tsx` currently renders `PageModal` (L288) → change it to open the expense engine (responsive for mobile).
    **Responsive PageModal (for the types that use it):** at mobile width render FULL-SCREEN, SINGLE-COLUMN stacked (NO side-by-side panes / fixed `w-*` / desktop split), scrollable, readable, clear back/close. (Coordinate with MODAL-LAYOUT: desktop = split [properties | connections+journal]; mobile = stacked full-screen.) **VERIFY:** `/m` → contact opens clean responsive modal; invoice opens modal WITH its document under properties; expense opens the PurchaseInvoiceEngine detail (not the generic modal); none broken at 380px. **Commit:** `MOBILE-RECORD-MODAL: per-type record open (responsive modal; expense→engine; invoice shows file)`.
  - **MOB-FREE-6 — engine mobile layout, BOTH engines (Florin 2026-06-20: invoice fixed partially, QUOTE not at all; both must be done). Multiple concrete faults:** (1) **bottom action button row** (`ClientInvoiceEngine` ~L1076 + quote equivalent) goes BEYOND 100% width, misaligned, text WRAPS inside buttons — `flex-wrap` + fixed child widths overflow. FIX: on mobile STACK the buttons (`flex-col`, ~`w-full`/≤100%, `whitespace-nowrap` not wrap, aligned, NEVER exceed 100% width). (2) **bottom TOTALS block** is cut off by container width + can't scroll inside it → the two halves must simply STACK (`flex-col`, full width, no horizontal overflow). (3) inside the invoice/quote LINE the stacked financial details are good BUT MISALIGNED — the UNIT price and BTW% values must align **RIGHT** (consistent `text-right`/`items-end`). (4) keep the earlier intent: collapse engine actions into the top-right "…" menu + stop the 100vh squeeze so LINES get room. **Apply to BOTH `ClientInvoiceEngine` AND `ClientQuotationEngine`.** **VERIFY (both engines, 380px):** nothing exceeds 100% width, no inner-scroll/cut-off, buttons stacked + readable (no wrap), totals halves stacked, line unit/BTW values right-aligned. **Commit:** `MOB-FREE-6: engine mobile layout — stack buttons/totals, no overflow, right-align line values (both engines)`.
  - **ENGINE-SIDEMENU — the engine side info panel (`DbPropertiesPanel`) is too narrow → info TRUNCATED (mobile AND DESKTOP), and reads from a DIFFERENT data source than the engine.** **FIX:** widen the panel so values aren't cut off; reconcile it to read the engine's LIVE data (not a separate/stale source). **Commit:** `ENGINE-SIDEMENU: widen panel + read engine's live data`.
  - **GHOST-HAMBURGER — stray top-left hamburger/X toggle that opens NO menu (appears in `/m` AND the main app on mobile).** The toggle flips icon but there's no drawer + it shouldn't be there (AdminLayout's sidebar is `hidden md:flex` so its mobile hamburger has nothing to open; and/or MobileShell L122 toggle opens nothing). **FIX:** identify which renders the ghost toggle on mobile and remove it (or wire it to a real menu if one is intended). **Commit:** `GHOST-HAMBURGER: remove dead mobile menu toggle`.
  - **RECEIPT-NOVAT — fully REMOVE VAT from RECEIPTS (tickets) (Florin confirmed 2026-06-20: VAT on receipts has ZERO bookkeeping impact, is legally non-deductible, and tickets always show VAT-INCLUDED amounts).** In `TicketCaptureModal` + the receipt detail, when NOT `isInvoiceMode`: remove the `vatAmount` field entirely (UI + OCR-extract mapping + persistence) — do NOT capture/compute/store any VAT for receipts. The receipt's single amount = the VAT-INCLUSIVE total. Keep VAT only for proper purchase invoices (`isInvoiceMode`). **Commit:** `RECEIPT-NOVAT: remove VAT entirely on receipts (amount = VAT-incl total)`.
  - **RECEIPT-THUMB — tap the document thumbnail → view the full image (lightbox); explain or remove the warning triangles.** The ⚠ icons next to field labels have zero explanation → add a tooltip (what's wrong/why) or remove them. **Commit:** `RECEIPT-THUMB: tappable thumbnail viewer + explain warning icons`.
  - **SCAN-TEXT-LOC — replace OCR progress text "Reading locally…/Tesseract.js is reading…" with a neutral, LOCALISED "Verwerken…/Processing…"** (don't expose "Tesseract"; localise nl/fr/en/ro). `TicketCaptureModal` L372/403/406. **Commit:** `SCAN-TEXT-LOC: neutral localised OCR progress text`.
  - **MOB-LOGOUT — no REACHABLE logout in the mobile app.** NOTE: `MobileShell.tsx` already HAS `signOut` wired (L12 import, L187 `signOut({callbackUrl:"/"})`) — it's just not accessible to the user. **FIX = surface it, don't rebuild:** expose the existing logout via a visible control (m/settings or the mobile menu). **Commit:** `MOB-LOGOUT: surface existing logout control in mobile app`.
  - **(also reported, already queued):** open saved receipt/expense detail → MOB-FREE-2 (ensure it covers receipts); PDF-from-storage scan → SCAN-FREE-OCR; clients clickable → MOB-FREE-2. **(Confirmed OK, no action):** Chrome settings scroll; engine back button → invoices list.
  - **SCAN-FREE-OCR — free-tier client-side OCR fails ("Free plan requires client-side Tesseract extraction").** `api/scan/route.ts` L290 rejects free scans that arrive WITHOUT `clientExtractedStr`; `TicketCaptureModal` only runs client OCR `if (isFree && file.type.startsWith('image/'))` (L134) → so a **PDF on free** (or Tesseract failing to load on mobile) hits the server with no text → error. **FIX:** make the free client OCR actually produce `clientExtractedStr` for what users upload: (a) ensure Tesseract.js loads + runs on mobile for images; (b) handle PDFs on free — render the PDF page to an image client-side (unpdf/canvas) then Tesseract it, OR if not feasible, give a clear "PDF scan needs a paid plan — enter manually" message instead of the opaque error. REPRODUCE with the exact file type Florin used. **VERIFY:** on a FREE account (mobile), upload a receipt image AND a PDF → image OCRs locally + fills fields; PDF either OCRs or shows a clear actionable message — never the raw "requires client-side Tesseract" error. **Commit:** `SCAN-FREE-OCR: free-tier client OCR works for image + handles PDF`.
- **MOB-2** Overall mobile optimization pass (legibility/layout verified, not just committed). (committed — re-verify)
- **MOB-3** MOBILE OPTIMISATION OVERALL — every module usable on phone (umbrella). Per-module responsive pass — incl. CAL-5 mobile calendar, projects, tasks, dashboard. Each module = its own commit + verify on a real 380px viewport.
- **👉 MOB-4 — reclaim horizontal width (Florin 2026-06-19: content squeezed to ~60% of screen by a CASCADE of inherited container margins/paddings). Do first within MOBILE; distinct from MOB-1 (routing).** ROOT: nested containers each add horizontal padding/max-width that compound — `AdminLayout` header/banners `px-6`, page wrappers (`projects-management`, invoices…) `px-6`/`px-8`, inner cards `p-6`/`p-8`/`rounded-2xl`, plus `max-w-*`/`mx-auto`. At ~380px these stack to eat ~40%. **FIX (mobile-first):** make every horizontal padding responsive (`px-3 md:px-6`, `p-3 md:p-6`); collapse DOUBLE padding (don't pad both wrapper AND card on mobile); drop `max-w-*`/`mx-auto` caps on mobile (`max-w-none md:max-w-7xl`). Applies to the AdminLayout + admin page wrappers AND the SHARED engines' own container padding (the engines render inside `/m/` too, so fix them responsively — helps `/m/`, never regresses it). **TARGET:** primary content ≥90% width at 380px. **VERIFY:** invoices/quotes/projects/databases/dashboard near-full width at 380px; desktop unchanged (`md:` preserves it); **`/m/` pages NOT regressed.** **Commit:** `MOB-4: responsive horizontal padding, reclaim mobile width`.

## LOCALE — Localization (FOUNDATION, not string-sweep) (v0.3/0.5)
> Principle (Florin): fix the MECHANISM, not each leaked string. The "Creditnota voor 2026-19" on a French doc is not a typo — it's a SYSTEMIC fault: text isn't bound to the document's language, and some text is HARDCODED/stored in one language. Fix foundations.
- **LOCALE-1** 🩸 ROOT FIX — document/engine text must resolve via `t(key, documentLanguage)`, never hardcoded literals. Measured scale: ~43 hardcoded NL strings across `ClientInvoiceEngine.tsx` (~30) + `ClientQuotationEngine.tsx` (~13), plus untranslated module/option names. Route all of them through the i18n layer keyed by the DOCUMENT's language (NL/FR/EN/RO). One mechanism; the strings follow.
- **LOCALE-2** 🩸 STORED-TEXT FIX — generated text frozen in one language. Concrete: `ClientInvoiceEngine.tsx:344` stores `betreft: \`Creditnota voor ${invoiceNum}\`` (Dutch) into the record → renders on a French credit note as-is forever. Fix: store such generated references as a KEY/structured ref resolved at render time in the document language (or generate in the doc language). Audit for other frozen-language stored fields (status labels, auto-betreft, etc.).
- **LOCALE-3** Coverage sweep — AFTER the mechanism (1+2) is solid: ensure NL/FR/EN/RO keys exist for every document/engine string + module/option names. This is the only "detail" pass, and only because there's no choice — keys must exist for the mechanism to resolve. Do NOT hand-fix individual strings before 1+2.
- **LOCALE-4** Leaked variable keys (e.g. `Admin.nav.pages.manualTicket` showing raw) = missing keys surfaced by the mechanism; fixed as part of LOCALE-3 coverage. (was CROSS-2)

## PROFILE — Tenant data stability (v0.1, underpins documents)
- **PROFILE-1** 🟢 DONE — awaiting Florin verify. **Two fixes:** (A) **Stale profile:** engines copied tenant into local state via `useEffect([])` — never re-consumed fresh context. Fixed: deps → `[tenant]`; `TenantContext` upgraded to state + `refreshTenant()`; all 8 settings pages call `refreshTenant()` after save; `AdminLayout` syncs branding via `tenantProfileUpdated` event. RSC read path (Prisma direct) was never cached — `revalidatePath`/`router.refresh()` always produced fresh data; the bug was downstream consumption. (B) **Language triple-write desync:** `environmentLanguage` was only written to JWT in-memory on `updateSession()` — not persisted to DB until next full JWT refresh; `NEXT_LOCALE` cookie relied on middleware round-trip. Fixed: auth.ts now persists `environmentLanguage` to DB immediately; both desktop + mobile set `NEXT_LOCALE` cookie client-side before navigating; Google OAuth first-login fallback changed from hardcoded `"fr"` to `"nl"` (matches routing.ts default). **Florin verify:** (1) change brand color → engine shows new color without reload; (2) switch interface language on desktop → switch device → same language persists; (3) mobile language switch navigates to correct locale path.

- **PROFILE-2** 🩸 **PROFILE RESET — branding + document numbering wiped (Florin, ongoing debug 2026-06).** ROOT CAUSE (measured, not stale-load — that was PROFILE-1): the company-info settings widgets **initialize local state to hardcoded fake defaults, then PUT those fields unconditionally on save.** Two offenders, same bug class:
  - `src/app/[locale]/admin/settings/company-info/page.tsx` — `profile` state seeds `invoicePrefix:'INV'`, `invoiceNextNumber:1`, `quotation*`, `creditnote*`, `documentTemplate:'t1'` (lines ~123-162); hydrates from `tenant` only in `useEffect([tenant])` (~168-211); `handleSave`→`proceedWithSave(profile)` sends the WHOLE `profile` via `JSON.stringify(dataToSave)` (~227-230).
  - `src/components/admin/settings/DocumentTemplatesModule.tsx` — seeds `primaryColor:'#d35400'`, `selectedTemplate:'t1'`, `documentLanguage:'nl'` (~160-162); hydrates in `useEffect([tenant])` (~177-187); `handleSaveSettings` PUTs `brandColor/documentTemplate/documentMode/logoUrl/stationeryUrl` unconditionally (~189-202).
  - **The reset:** if `tenant` (TenantContext) is null/stale at save, or a save fires pre-hydration, the route writes any field present in body (`if (field in body)`), so the DEFAULTS overwrite real DB values → numbering snaps to INV/1, color to #d35400, template to t1. `refreshTenant()` (PROFILE-1) only fixed display, not this write-back.
  **FUNDAMENTAL FIX (general measure — make it structurally impossible to persist a value you never loaded):**
  1. **No fake defaults in saved state.** Initialize the persisted fields to `null`/undefined (a "not loaded" sentinel), NOT to `'INV'`/`1`/`'#d35400'`/`'t1'`. Apply display defaults ONLY at render (`value={profile.invoicePrefix ?? 'INV'}`), never into the object that gets PUT.
  2. **Gate save on hydration.** Block save + disable the Save button until `tenant` has loaded (`if (!tenant) return;` / `disabled={!tenant || saving}`). A save can never run against un-hydrated state.
  3. **Send only owned + loaded fields.** Each widget PUTs ONLY the fields it owns and that were actually hydrated (skip any still-null). Drop `documentTemplate`/`documentLanguage` from company-info's payload — `DocumentTemplatesModule` owns those; overlapping writers is the smell. (Mirror the tight-partial pattern the other 5 settings pages already use.)
  **VERIFY (Florin):** set numbering to e.g. INV2026/00045 + a brand color + template; hard-reload; reopen company-info BEFORE it fully loads and immediately hit save → values must NOT reset; cross-check DB. Regression-check: editing only the company name must not touch numbering/branding.

- **PROFILE-3** 🟢 ROOT FIX LANDED — awaiting Florin deploy-verify. **Tenant info not populated into quote/invoice PDFs.** **FIX (commit `61e978a` "widen layout tenant profile select"):** layout server select was MINIMAL → tenant object starved → PDF blank. Now selects FULL set (companyName, commercialName, vatNumber, iban, bic, email, street/postalCode/city, logoUrl, brandColor, documentTemplate/Mode, stationery, fonts, documentLanguage, numbering). All 4 PDF build sites read the same tenant object → fixes every path at once. Combined with earlier fallback-removal: leak closed AND data flows. Planner code-confirmed. **RESIDUAL:** `phone` not populated (not a model field — add to schema or omit, don't hardcode); confirm `InvoicePDFTemplate` fallbacks removed + `QuotationDocumentHeader` L30/32 'Coral Remodeling Pro' not rendered live. **DEPLOY-VERIFY (Florin):** quote+invoice as Coral → all fields populate; then as a 2nd test tenant → that tenant's data, ZERO Coral leak (multi-tenant gate). [re-added 3rd time after board clobber 2026-06-08]

## SETTINGS / ADMIN (v0.5)
- **SET-1** Schema-edit screen back button → goes to global schema; should be navigational (back in history to where the user came from).
- **SET-2** Improve tenant management, especially licence management.
- **SET-3** Properly separate Superadmin / ERP config / Tenant settings.
- **SET-4** Website CMS must include BOTH the Coral Enterprises front site CMS and the ERP front CMS.

## CROSS — App-wide / general (mixed)
- **CROSS-1** Replace black crash screen with a themed screen. (note: pd.md said done — VERIFY; Florin still lists it)
- **CROSS-2** 🩹 Untranslated variable keys leak to UI (e.g. `Admin.nav.pages.manualTicket`). Fix missing i18n keys.
- **CROSS-3** Sales→Offerte→Project cascade: info populates one level up so the Project eventually holds everything. (the data-flow backbone; design carefully)
- **CROSS-4** 📊 Peppol credit tracking + unit-economics observability (FULL SPEC below). (was O1; now grounded in confirmed reseller pricing.)
- **CROSS-5** OpenAI import engine audit — regression check + accuracy (was OAI-1/L2). VERIFY current state first.
- **CROSS-6** My personal Notion module + anything in pro-hardening.md not yet done (sweep at the end).
- **CROSS-7** 🟢 **RUNTIME VERSION SYNC + service-worker auto-update (FOUNDATION).** Coded+committed 2026-06-06. 5 sub-items: **7a** `NEXT_PUBLIC_APP_VERSION` baked from `VERCEL_GIT_COMMIT_SHA` + `GET /api/version` (force-dynamic, no-store). **7b** `VersionWatcher.tsx` mounted in AdminLayout — polls `/api/version` every 3min + on focus + on route change; shows persistent toast on SHA mismatch. **7c** `sw-workhub.js` versioned cache names via `?v=SHA` registration param; purges all old caches on activate; `controllerchange` → reload; `registration.update()` on focus/visibility. **7d** ⏳ Vercel Skew Protection = Florin manual toggle (Project Settings → General). **7e** `EnvBadge.tsx` — `env · shortSHA` badge, superadmin-only in prod, always in non-prod. Also bumped sidebar store v18→v19 to force all browsers to reset stale sidebar.
  1. **CROSS-7a — version stamp + endpoint.** Bake deploy version into the client: `NEXT_PUBLIC_APP_VERSION` = `VERCEL_GIT_COMMIT_SHA` (Vercel system env var) at build. Add `GET /api/version` (`dynamic='force-dynamic'`, no-store) returning `{ version, environment }` of the CURRENTLY-deployed server.
  2. **CROSS-7b — client version watcher.** Small provider mounted in admin layout: on interval (~3 min) + on `window` focus + on route change, fetch `/api/version`, compare to baked-in `NEXT_PUBLIC_APP_VERSION`. On mismatch → non-blocking banner/toast "New version available — Reload" (optionally auto-reload on next route change). Works in ALL browsers, not just PWA. Throttle so it isn't chatty.
  3. **CROSS-7c — service-worker lifecycle fix (BOTH sw.js + sw-workhub.js).** Versioned cache names (include build id); `install`→`skipWaiting()`; `activate`→purge old caches + `clients.claim()`; on `controllerchange`→reload (or hand off to 7b's banner). Call `registration.update()` on focus/interval. This is what actually unsticks the pinned PWA shell. Confirm install scope/`start_url` so an old install isn't pinned to the wrong (ERP vs Workhub) shell.
  4. **CROSS-7d — enable + verify Vercel Skew Protection** (platform layer; complements, doesn't replace 7a-c). Pins a session's requests to the deployment it loaded → kills mid-session API/asset skew ERRORS. Default-on only for projects created after 2024-11-19; **Coral's project may predate it — check the toggle in Project Settings.** It does NOT refresh a stale PWA shell (that's 7b/7c). [docs: vercel.com/docs/skew-protection]
  5. **CROSS-7e — environment+version badge** (debug aid): small badge showing `environment · short-SHA` (non-prod, or superadmin-only) so "which build am I on?" is answerable at a glance — directly prevents today's confusion and protects verification.
  6. **CROSS-7f — icon wiring (assets DONE by Planner; only manifest edit remains).** Planner regenerated a consistent icon set in `public/` from `branding/Coral-New-Logo.svg`: `icon-512.png` (now truly 512, was mislabeled 1024), `icon-192.png`, `apple-touch-icon.png`, NEW `icon-maskable-512.png` (20% safe zone), NEW `favicon.ico` (was 404), NEW `icon.svg` (was referenced 4× but missing). **Coder TODO:** in BOTH `public/manifest.json` + `public/manifest-workhub.json`, split icons into separate `purpose:"any"` (192+512) and `purpose:"maskable"` (`/icon-maskable-512.png`) entries; correct the 512 `sizes` (file is now real 512); add `?v=2` cache-bust on the `src`s. Root cause of the original bug: Chrome read the manifest `any maskable` PNGs (cropped by squircle) while Safari read `apple-touch-icon` — now unified. Verify: reinstall PWA on Chrome → icon matches Safari, uncropped.
  **Also (separate concern surfaced by the same screenshot):** confirm `activeModules` gating isn't OVER-hiding for enterprise — Coral is enterprise and should see the full module set; the reduced Safari list may be newer code gating modules it shouldn't. Verify against `PLAN_MODULES`.

## DASH — Dashboard
- **DASH-1** ⬜ Future development (deferred — not now).

---

## 📋 CARRIED FORWARD from pro-hardening.md (status per git log; VERIFY, don't trust)
- Committed but UNVERIFIED (treat as shallow until Florin confirms): F1/F1-FIX (drive isolation), F2 (peppol health), F3/B/C (inbox shape), P8 (billing UI), P10 (trial parked), L2/L4 (extraction/decimal), L5 (totals), Q1 (authoring), Q2 (project P&L), M2/M3 (mobile/accountant), legal-texts, rich-text-block.
- **The `B1-B5, M4, M5` were shipped in ONE batched commit** — high regression risk, explicitly distrusted; folded into FIN/GRID/SCAN/SEND/MOB/CAL above for proper one-at-a-time re-verification.
- Genuinely NOT shipped: Q4 (OGM/payment matching), Q5 (→ QUOTE-6), U1 (sidebar sync), J1 (→ EDITOR), S1 (site visit), O1 (→ CROSS-4), E1 (→ PEPPOL-2).

---

## ▶️ PROPOSED FIRST INCREMENTS (foundation-first, financial-correctness north star)
Run strictly one-at-a-time, each ✅-gated by Florin:
1. **SCHEMA-1a** (stop duplicate creation — code only, safe) → then **SCHEMA-1b** (data-safe merge, dry-run → Florin-approve → execute). 
2. **FIN-1** — credit-note↔invoice correctness (financial integrity, highest).
3. **FIN-2** — numbering.
4. **FIN-3** — sent-doc edit-lock exemption (project/offerte editable).
5. **GRID-1** — fix core cell editing (unblocks daily use everywhere).
→ then GRID-5/6/7 (flyouts, kill detail-views, searchable selects) which clear large swaths.
**v0.1 tag** after FIN-1..6 + SCHEMA-1 + PROFILE-1 verified.

---

# ════════════ DETAILED TASK SPECS ════════════

## CROSS-4 — 📊 Peppol credit tracking + unit-economics observability (reseller model)
**State:** ⬜ not-built · `develop` · target v0.5 (but the METER part should land early — see note)
**Why:** As a reseller, Coral pays e-invoice.be **1 credit per Peppol document, both directions**, tiered **€0.18 → €0.05** by total monthly volume across ALL tenants, billed org-level post-pay NET-10. Coral must track this precisely — both to control cost and to price/gate FREE vs PRO on real numbers, not guesses. (Confirmed by e-invoice.be support 2026-06-04.)

### CONFIRMED FACTS (from support reply — use these, don't re-derive)
- Org-level credits, post-pay NET-10. No per-tenant activation/fee/pack.
- **1 credit per Peppol doc, SENT and RECEIVED** (inbound counts).
- Tiered rate by combined monthly volume (Appendix 1 of reseller agreement): **€0.18 → €0.05**. Exact breakpoints come from the agreement — Florin to supply the full grid; until then use a CONFIGURABLE tier table, not magic numbers.
- Per-tenant balance/stats endpoint: **`GET https://api.e-invoice.be/api/stats`** (per-tenant via Admin API). Use it as the source of truth to reconcile against our own counts.

### Build — meter → attribute → reconcile → display
1. **Meter every Peppol document at the source** (the one place that already increments `peppolSentThisMonth`/`peppolReceivedThisMonth` in `plan-limits.ts` — extend it). For each sent AND received doc, record an event: `{ tenantId, direction (sent/received), docType, timestamp, creditsConsumed: 1 }`. Persist as history (a `peppol_credit_events` table or equivalent), not just a rolling counter — we need monthly history + audit.
2. **Cost attribution via the tier table.** A configurable `PEPPOL_CREDIT_TIERS` (volume breakpoints → €/credit). Compute cost = credits × current tier rate, where the tier is determined by **total org volume this month across all tenants combined** (not per-tenant). Show both our cost and (later) what we charge the tenant, to derive margin.
3. **Reconcile against `GET /api/stats`.** Periodically (or on the dashboard's refresh) pull e-invoice.be's authoritative per-tenant stats and compare to our internal counts. Flag drift (our count vs theirs) — protects against silent over/under-billing. e-invoice.be is the billing source of truth; our meter is for real-time visibility + attribution.
4. **Superadmin "Peppol Credits & Cost" dashboard** (extend the existing superadmin Peppol-health panel / TenantsGrid): 
   - Org-wide: total credits this month (sent/received split), current tier rate, projected monthly cost, NET-10 due.
   - Per-tenant: credits consumed (sent/received), attributed cost, last reconcile vs `/api/stats`, drift flag.
   - FREE-tier aggregate: total monthly credit cost of the entire free base (THE number that proves free-forever scales — free users cost ~5 sent + up to 20 received = up to ~25 credits/mo each).
5. **Threshold alerts:** a tenant with abnormal credit burn (e.g. a free tenant far above expected); org approaching a tier breakpoint (so Florin sees volume-discount kick in); reconcile drift beyond tolerance.
6. **Feed pricing/gating:** expose these numbers so FREE caps / PRO overage / pricing are evidence-based. FREE caps already exist (5 sent / 20 received) — surface "this free user cost €X this month" against them.

### Premises to measure (Rule 2)
- `[MEASURED ✅]` `plan-limits.ts` already counts peppol sent/received per tenant per month — extend, don't duplicate.
- `[CONFIRMED ✅]` pricing model + `/api/stats` endpoint (support reply).
- `[ASSUMED ❓]` exact tier breakpoints — Florin/agreement Appendix 1. Configurable table until supplied.
- `[ASSUMED ❓]` `GET /api/stats` response shape — probe it (org key) before building the reconcile.

### Acceptance criteria
- Every Peppol send/receive recorded as a credit event with tenant + direction + timestamp (history retained).
- Cost computed via configurable tier table on combined org volume.
- Superadmin dashboard: org + per-tenant + FREE-aggregate credits/cost; reconcile vs `/api/stats` with drift flag.
- Alerts for abnormal burn / tier breakpoint / drift.
- Tier rates + breakpoints are config, not literals. `tsc`+`lint` green.

### Note on timing
The **meter** (step 1) should land EARLY (cheap, and you want history accumulating from now). The dashboard/reconcile can follow. Split into CROSS-4a (meter) and CROSS-4b (dashboard/reconcile) if running strict one-at-a-time.

### 🤖 AI FEEDBACK
- measured (plan-limits counters; /api/stats shape):
- credit-event meter (sent+received, history):
- tier table + org-volume cost calc:
- reconcile vs /api/stats + drift flag:
- superadmin dashboard (org/per-tenant/FREE-aggregate):
- alerts:
- premise updates appended to pd.md? (y/n):

---

*Board reset by Planner 2026-06-04 from Florin's state-of-the-union. pd.md rules inherited. Execution protocol above is binding.*
